import assert from 'node:assert/strict';import test from 'node:test';import {loadStateBusinessSourceAssessmentWave,validateStateBusinessSourceAssessment} from './state-business-source-assessment-wave.mjs';
test('official five-state wave is complete, non-acquiring and all HOLD',async()=>{const wave=await loadStateBusinessSourceAssessmentWave();assert.deepEqual(wave.scope,['KS','KY','TX','UT','WA']);assert.deepEqual(wave.decisions,{hold:5,connector_candidate:0});assert.ok(wave.states.every(s=>s.citations.length>=2&&s.controls.official_primary_sources_only&&s.controls.downloads===0&&s.decision==='hold'));});
test('assessment validation rejects acquisition, incomplete evidence and candidate escalation',async()=>{const wave=await loadStateBusinessSourceAssessmentWave();for(const change of [v=>v.controls.downloads=1,v=>v.connector_candidate=true,v=>v.citations=[],v=>v.decision='connector-candidate']){const row=structuredClone(wave.states[0]);change(row);assert.throws(()=>validateStateBusinessSourceAssessment(row));}});
