import path from 'node:path';
import {open,lstat,mkdir,readdir,link,unlink} from 'node:fs/promises';
import {randomUUID,createHash} from 'node:crypto';
import {isDeepStrictEqual as same} from 'node:util';
import {parse} from 'csv-parse/sync';
import {APP_ROOT} from './paths.mjs';
import {mnSelectionCanonical as canonical} from './mn-construction-retained-selection.mjs';
import {CENSUS_GEOCODER_OFFLINE_CONTRACT as CANDIDATE} from './census-geocoder-batch-contract.mjs';
const VERSION='census-geocoder-demo-prerequisite@1.0.0',CATALOG='https://geocoding.geo.census.gov/geocoder/benchmarks',ENDPOINT=CANDIDATE.endpoint;
const UUID=/^[a-f0-9]{8}-[a-f0-9]{4}-4[a-f0-9]{3}-[89ab][a-f0-9]{3}-[a-f0-9]{12}$/,sha=v=>createHash('sha256').update(v).digest('hex'),json=v=>Buffer.from(JSON.stringify(v)+'\n');
const check=v=>{if(!v)throw Error('Census public-demo prerequisite rejected.');};
const policy=()=>({profile:VERSION,publisher:'U.S. Census Bureau',documentation:'https://geocoding.geo.census.gov/geocoder/Geocoding_Services_API.html',scope:'One fixed official public demonstration address only; no user, CMS or business records.',address:{street:'4600 Silver Hill Rd',city:'Washington',state:'DC',zip:'20233'},benchmark:{id:'8',name:'Public_AR_ACS2025'},providerBudgetKey:'census-public-geocoder',requests:3,posts:1,parallelSessions:1,redirects:false,retries:false,credentials:'omit',catalogBytes:65536,responseBytes:65536,csvBytes:1024,multipartBytes:4096,requestTimeoutMs:30000,sessionTimeoutMs:100000,cleanupTimeoutMs:1000,providerRetentionGuarantee:null,broaderAddressDisclosureAuthorized:false,exportPolicy:'internal',publicExportAuthorized:false,attribution:'U.S. Census Bureau, Census Geocoder; approximate address-range interpolation, not structure or current-operations verification.'});
const root=synthetic=>path.join(APP_ROOT,synthetic?`data/tmp/census-geocoder-demo-${process.pid}`:'data/business-sources/census-public-geocoder');
const mode=s=>s?'synthetic-fixture-only':'native-public-demo';
const stable=(a,b)=>a.isFile()&&b.isFile()&&!a.isSymbolicLink()&&!b.isSymbolicLink()&&a.nlink===1n&&b.nlink===1n&&a.ino===b.ino&&a.dev===b.dev&&a.size===b.size&&a.mtimeNs===b.mtimeNs&&a.ctimeNs===b.ctimeNs;
function options(v){check(v&&Object.getPrototypeOf(v)===Object.prototype&&Reflect.ownKeys(v).every(k=>k==='signal'&&Object.hasOwn(Object.getOwnPropertyDescriptor(v,k),'value'))&&(v.signal===undefined||v.signal instanceof AbortSignal));v.signal?.throwIfAborted();}
async function read(file,max,signal){await canonical(path.dirname(file),{signal});signal?.throwIfAborted();const initial=await lstat(file,{bigint:true});check(initial.isFile()&&!initial.isSymbolicLink()&&initial.nlink===1n&&initial.size<=BigInt(max));const h=await open(file,'r'),parts=[];let bytes=0;try{check(stable(initial,await h.stat({bigint:true})));for(;;){signal?.throwIfAborted();const b=Buffer.alloc(65536),r=await h.read(b);if(!r.bytesRead)break;bytes+=r.bytesRead;check(bytes<=max);parts.push(b.subarray(0,r.bytesRead));}check(stable(initial,await h.stat({bigint:true}))&&stable(initial,await lstat(file,{bigint:true}))&&BigInt(bytes)===initial.size);}finally{await h.close();}const raw=Buffer.concat(parts);return {raw,bytes,sha256:sha(raw),identity:initial};}
function decode(raw){return new TextDecoder('utf-8',{fatal:true}).decode(raw);}
function catalog(raw){check(raw.length>0&&raw.length<=65536);const c=JSON.parse(decode(raw));check(c&&Array.isArray(c.benchmarks)&&c.benchmarks.length>0&&c.benchmarks.length<=100);const ids=new Set();for(const b of c.benchmarks){check(b&&typeof b.id==='string'&&!ids.has(b.id)&&typeof b.benchmarkName==='string');ids.add(b.id);}check(c.benchmarks.filter(b=>b.id==='8'&&b.benchmarkName==='Public_AR_ACS2025').length===1);return {id:'8',name:'Public_AR_ACS2025',catalogSha256:sha(raw)};}
function upload(id){check(UUID.test(id));return Buffer.from(`"${id}","4600 Silver Hill Rd","Washington","DC","20233"\r\n`);}
function multipart(id){const boundary=`census-demo-${id}`,csv=upload(id);const raw=Buffer.concat([Buffer.from(`--${boundary}\r\nContent-Disposition: form-data; name="addressFile"; filename="demo.csv"\r\nContent-Type: text/csv\r\n\r\n`),csv,Buffer.from(`\r\n--${boundary}\r\nContent-Disposition: form-data; name="benchmark"\r\n\r\n8\r\n--${boundary}--\r\n`)]);check(csv.length<=1024&&raw.length<=4096);return {raw,contentType:`multipart/form-data; boundary=${boundary}`};}
function observation(raw,id){check(raw.length>0&&raw.length<=65536);let rows;try{rows=parse(decode(raw),{bom:true,relax_column_count:true,max_record_size:65536});}catch{throw Error('Census public-demo response schema unresolved.');}check(rows.length===1);const c=rows[0];check(c.length>=3&&c.length<=8&&c[0]===id&&c.every(v=>!/[\u0000-\u001f\u007f]/u.test(v)));const status=c[2];check(['Match','No_Match','Tie'].includes(status));if(status==='Match'){check(c.length===8&&['Exact','Non_Exact'].includes(c[3])&&c[4]&&c[6]&&['L','R'].includes(c[7]));const xy=c[5].split(',');check(xy.length===2&&xy.every(v=>/^-?(?:\d+(?:\.\d+)?|\.\d+)$/.test(v)));const [x,y]=xy.map(Number);check(Number.isFinite(x)&&x>=-180&&x<=180&&Number.isFinite(y)&&y>=-90&&y<=90);}else check(c.slice(3).every(v=>v===''));return {status,fieldCount:c.length,matchType:status==='Match'?c[3]:null,rows:1,opaqueIdConserved:true};}
const claims=s=>({nativeTransportObserved:!s,observedVariantOnly:true,allResponseVariantsVerified:false,puertoRicoVariantVerified:false,broaderBatchReadiness:false,businessAddressesSubmitted:false,providerRetentionGuarantee:null,publicExportAuthorized:false,datum:'NAD83',datumRealization:null,method:'address-range-interpolation',currentOperationsVerified:false});
function abortable(promise,signal){if(signal.aborted){promise.catch(()=>{});return Promise.reject(signal.reason);}return new Promise((resolve,reject)=>{const abort=()=>reject(signal.reason);signal.addEventListener('abort',abort,{once:true});promise.then(resolve,reject).finally(()=>signal.removeEventListener('abort',abort));});}
async function cleanup(fn){let timer;try{return await Promise.race([Promise.resolve().then(fn).then(()=>true,()=>false),new Promise(resolve=>{timer=setTimeout(()=>resolve(false),1000);})]);}finally{clearTimeout(timer);}}
const time=v=>typeof v==='string'&&Number.isFinite(Date.parse(v))&&new Date(v).toISOString()===v;
async function verify(file,hash,synthetic,signal,pending=false){check(typeof file==='string'&&file===path.resolve(file)&&path.basename(file)===(pending?'manifest.pending':'manifest.json')&&UUID.test(path.basename(path.dirname(file)))&&path.dirname(path.dirname(file))===path.join(root(synthetic),'jobs'));const directory=path.dirname(file);await canonical(directory,{signal});const owner=await lstat(directory,{bigint:true}),saved=[];
 const load=async(name,max)=>{const r=await read(path.join(directory,name),max,signal);saved.push([name,max,r]);return r;},mraw=await load(path.basename(file),100000),m=JSON.parse(decode(mraw.raw));check(mraw.sha256===hash&&same(Object.keys(m).sort(),'schemaVersion runId executionMode createdAt completedAt opaqueId policy requests artifacts observation claims'.split(' ').sort())&&m.schemaVersion===VERSION&&m.runId===path.basename(directory)&&m.executionMode===mode(synthetic)&&UUID.test(m.opaqueId)&&same(m.policy,policy())&&same(m.claims,claims(synthetic))&&time(m.createdAt)&&time(m.completedAt)&&m.completedAt>=m.createdAt&&Date.parse(m.completedAt)-Date.parse(m.createdAt)<=100000);
 const intent=JSON.parse(decode((await load('intent.json',10000)).raw));check(same(intent,{schemaVersion:VERSION,runId:m.runId,executionMode:mode(synthetic),createdAt:m.createdAt,opaqueId:m.opaqueId,policy:policy()}));
 const names=['policy.json','upload.csv','multipart.bin','catalog-before.json','response.csv','catalog-after.json'];check(Array.isArray(m.artifacts)&&same(m.artifacts.map(a=>a.path),names));const bodies={};for(const a of m.artifacts){check(same(Object.keys(a).sort(),['bytes','exportPolicy','path','sha256'])&&a.exportPolicy==='internal');const r=await load(a.path,65536);check(r.bytes===a.bytes&&r.sha256===a.sha256);bodies[a.path]=r.raw;}
 check(same(JSON.parse(decode(bodies['policy.json'])),policy())&&bodies['upload.csv'].equals(upload(m.opaqueId))&&bodies['multipart.bin'].equals(multipart(m.opaqueId).raw));catalog(bodies['catalog-before.json']);catalog(bodies['catalog-after.json']);check(bodies['catalog-before.json'].equals(bodies['catalog-after.json'])&&same(m.observation,observation(bodies['response.csv'],m.opaqueId))&&m.requests.length===3);
 let previous=m.createdAt;for(let i=0;i<3;i++){const r=m.requests[i],req=JSON.parse(decode((await load(`request-${i+1}.json`,10000)).raw)),body=bodies[['catalog-before.json','response.csv','catalog-after.json'][i]],expected={sequence:i+1,url:i===1?ENDPOINT:CATALOG,method:i===1?'POST':'GET',credentials:'omit',redirect:'manual',maxResponseBytes:65536,timeoutMs:30000,requestBodyBytes:i===1?bodies['multipart.bin'].length:0,requestBodySha256:i===1?sha(bodies['multipart.bin']):null,contentType:i===1?multipart(m.opaqueId).contentType:null,startedAt:r.startedAt};check(same(req,expected)&&same(r,{...req,completedAt:r.completedAt,status:200,responseBytes:body.length,responseSha256:sha(body),responseContentType:r.responseContentType})&&time(r.startedAt)&&time(r.completedAt)&&r.startedAt>=previous&&r.completedAt>=r.startedAt&&r.completedAt<=m.completedAt&&Date.parse(r.completedAt)-Date.parse(r.startedAt)<=30000&&typeof r.responseContentType==='string'&&r.responseContentType.length<=256);previous=r.completedAt;}
 check(same((await readdir(directory)).sort(),[path.basename(file),'intent.json',...names,'request-1.json','request-2.json','request-3.json'].sort()));for(const [name,max,before]of saved.reverse()){const after=await read(path.join(directory,name),max,signal);check(after.sha256===before.sha256&&stable(before.identity,after.identity));}const final=await lstat(directory,{bigint:true});check(final.isDirectory()&&!final.isSymbolicLink()&&final.ino===owner.ino&&final.dev===owner.dev);return m;
}
async function run(opts,transport,synthetic,hook){options(opts);const signal=opts.signal?AbortSignal.any([opts.signal,AbortSignal.timeout(100000)]):AbortSignal.timeout(100000);const policyRead=await read(path.join(APP_ROOT,'config/source-policies/census-geocoder-demo-prerequisite.json'),10000,signal);check(same(JSON.parse(decode(policyRead.raw)),policy()));const jobs=path.join(root(synthetic),'jobs');await canonical(jobs,{create:true,output:true,signal});const runId=randomUUID(),opaqueId=randomUUID(),directory=path.join(jobs,runId);await mkdir(directory);const owner=await lstat(directory,{bigint:true}),createdAt=new Date().toISOString(),artifacts=[],requests=[];let published=false,manifestSha256,uncertain=false;
 const intact=async()=>{await canonical(directory);const now=await lstat(directory,{bigint:true});check(now.isDirectory()&&!now.isSymbolicLink()&&now.ino===owner.ino&&now.dev===owner.dev);};
 async function write(name,raw){await intact();const file=path.join(directory,name),h=await open(file,'wx');try{const first=await h.stat({bigint:true});await h.writeFile(raw);await h.sync();const after=await h.stat({bigint:true});check(after.size===BigInt(raw.length)&&after.ino===first.ino&&after.dev===first.dev&&stable(after,await lstat(file,{bigint:true})));}finally{await h.close();}return {path:name,bytes:raw.length,sha256:sha(raw),exportPolicy:'internal'};}
 const descriptor=inspectionRequired=>({runId,directory,executionMode:mode(synthetic),...(published?{manifestPath:path.join(directory,'manifest.json'),manifestSha256}:{}),inspectionRequired,sourceLeaseRetained:uncertain});
 try{await write('intent.json',json({schemaVersion:VERSION,runId,executionMode:mode(synthetic),createdAt,opaqueId,policy:policy()}));artifacts.push(await write('policy.json',policyRead.raw),await write('upload.csv',upload(opaqueId)),await write('multipart.bin',multipart(opaqueId).raw));
 async function request(sequence,name){signal.throwIfAborted();const post=sequence===2,url=post?ENDPOINT:CATALOG,mp=multipart(opaqueId),req={sequence,url,method:post?'POST':'GET',credentials:'omit',redirect:'manual',maxResponseBytes:65536,timeoutMs:30000,requestBodyBytes:post?mp.raw.length:0,requestBodySha256:post?sha(mp.raw):null,contentType:post?mp.contentType:null,startedAt:new Date().toISOString()};await write(`request-${sequence}.json`,json(req));requests.push({...req});const scoped=AbortSignal.any([signal,AbortSignal.timeout(30000)]);let response,reader,complete=false,submitted=false;const pending=Promise.resolve().then(()=>{scoped.throwIfAborted();submitted=true;return transport(url,{method:req.method,credentials:'omit',redirect:'manual',signal:scoped,...(post?{body:mp.raw,headers:{'Content-Type':mp.contentType}}:{})});});
 try{try{response=await abortable(pending,scoped);}catch(error){if(!await cleanup(()=>pending.then(r=>r.body?.cancel(),()=>{})))uncertain=true;throw error;}check(!response.redirected&&(synthetic||response.url===url));reader=response.body.getReader();const chunks=[];let bytes=0;for(;;){const part=await abortable(reader.read(),scoped);if(part.done){complete=true;break;}bytes+=part.value.length;check(bytes<=65536);chunks.push(Buffer.from(part.value));}const raw=Buffer.concat(chunks);const responseContentType=response.headers.get('content-type')??'';Object.assign(requests.at(-1),{completedAt:new Date().toISOString(),status:response.status,responseBytes:bytes,responseSha256:sha(raw),responseContentType:responseContentType.length>256?'[over-limit]':responseContentType,...(responseContentType.length>256?{responseContentTypeOverLimit:true}:{})});artifacts.push(await write(name,raw));check(responseContentType.length<=256&&response.status===200&&bytes>0);return raw;}
 finally{if(!await cleanup(()=>reader?reader.cancel():response?.body?.cancel()))uncertain=true;try{reader?.releaseLock();}catch{uncertain=true;}if(post&&submitted&&!complete)uncertain=true;}}
 const before=await request(1,'catalog-before.json');catalog(before);const response=await request(2,'response.csv'),after=await request(3,'catalog-after.json');catalog(after);check(before.equals(after));const observed=observation(response,opaqueId);signal.throwIfAborted();check(!uncertain);const finalPolicy=await read(path.join(APP_ROOT,'config/source-policies/census-geocoder-demo-prerequisite.json'),10000,signal);check(finalPolicy.sha256===policyRead.sha256&&stable(policyRead.identity,finalPolicy.identity));
 const manifest={schemaVersion:VERSION,runId,executionMode:mode(synthetic),createdAt,completedAt:new Date().toISOString(),opaqueId,policy:policy(),requests,artifacts,observation:observed,claims:claims(synthetic)};manifestSha256=(await write('manifest.pending',json(manifest))).sha256;await hook?.('before-publication',{directory});await verify(path.join(directory,'manifest.pending'),manifestSha256,synthetic,signal,true);signal.throwIfAborted();await intact();await link(path.join(directory,'manifest.pending'),path.join(directory,'manifest.json'));published=true;await unlink(path.join(directory,'manifest.pending'));await hook?.('after-publication',{directory});await verify(path.join(directory,'manifest.json'),manifestSha256,synthetic,signal);signal.throwIfAborted();return descriptor(false);
 }catch{if(!published)try{await write('failure.json',json({schemaVersion:VERSION,runId,executionMode:mode(synthetic),createdAt,finishedAt:new Date().toISOString(),status:signal.aborted?'CANCELLED':'FAILED',requests,artifacts,published:false,submissionOrCleanupUnresolved:uncertain,error:'Public demonstration did not complete; inspect immutable evidence. No retry.'}));}catch{/* Preserve evidence if ownership/storage is uncertain. */}const error=Error('Census public-demo run requires inspection.');error.recovery=descriptor(true);error.sourceLeaseRetained=uncertain;throw error;}
}
async function leased(opts,transport,synthetic,hook){options(opts);const base=root(synthetic);await canonical(base,{create:true,output:true,signal:opts.signal});const file=path.join(base,'.source-lease');let h,id;const leasePayload=json({provider:'census-public-geocoder',profile:VERSION,ownerPid:process.pid,leaseId:randomUUID(),createdAt:new Date().toISOString()});try{h=await open(file,'wx');id=await h.stat({bigint:true});await h.writeFile(leasePayload);await h.sync();}catch{await h?.close();throw Error('Census provider lease unavailable; no automatic takeover.');}let result,failure;try{result=await run(opts,transport,synthetic,hook);}catch(e){failure=e;}try{await h.close();await hook?.('before-lease-cleanup',{leasePath:file});if(!failure?.sourceLeaseRetained){const retained=await read(file,10000);check(retained.identity.ino===id.ino&&retained.identity.dev===id.dev&&retained.raw.equals(leasePayload));await unlink(file);}}catch{const error=Error('Census public-demo lease requires inspection.');error.recovery={...(result??failure?.recovery),inspectionRequired:true,sourceLeaseRetained:true,leaseCleanupFailed:true};error.sourceLeaseRetained=true;throw error;}if(failure)throw failure;return result;}
export async function runCensusGeocoderDemo(opts={}){return leased(opts,(url,options)=>fetch(url,options),false);}
export async function runCensusGeocoderDemoFixture(transport,opts={},hook){check(typeof transport==='function'&&(hook===undefined||typeof hook==='function'));return leased(opts,transport,true,hook);}
export async function verifyCensusGeocoderDemo(file,hash,opts={}){options(opts);return verify(file,hash,false,opts.signal?AbortSignal.any([opts.signal,AbortSignal.timeout(100000)]):AbortSignal.timeout(100000));}
export async function verifyCensusGeocoderDemoFixture(file,hash,opts={}){options(opts);return verify(file,hash,true,opts.signal?AbortSignal.any([opts.signal,AbortSignal.timeout(100000)]):AbortSignal.timeout(100000));}
