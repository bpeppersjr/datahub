# Narrow nursing-home TAB recovery

The one authorized native run `ffb1fac4-4eb4-4ea4-b11c-875ebff4de41` retained all five responses but failed projection. Offline inspection found 14,690 CSV records, 99 headers, all required selected fields, no invalid/duplicate CCNs, and exactly one rejected text value: `Provider Address` at logical source row 14600 contains U+0009 TAB. No address value was printed. The original failure, request intents and raw responses remain untouched; no retry occurred.

Projection contract `cms-nursing-home-prerequisite@1.0.1` permits TAB only in Provider Address, preserving the exact string with `textQuality` status `source-tab-preserved`, code point U+0009, and `normalizationPerformed:false`. Every other control/selected field stays strict. Other source addresses receive `no-controls-observed`. JSONL escapes the tab without losing it. Acquisition schema is now 1.0.1 so future projection semantics are distinguishable; the historical failed run still declares its original 1.0.0. No new acquisition is needed or performed for the fix.

`runner/cms-nursing-home-recovery-pins.mjs` closes native recovery to that one source UUID and all twelve exact file byte/hash pins, including the original failure SHA256 `1cece3c9e809fc8ffa4bc271ee5472f62c66a1a2780825511f474c557132d1a7` and source CSV SHA256 `a5808fcded1ca73a629fde88fc23dcba3b6ff8ed75199883062a262e14ef8fe0` (9,151,628 bytes). It is not a generic failed-run bypass. No source path/pin override exists in the native API or CLI.

`recoverCmsNursingHomes({signal})` / `scripts/recover-cms-nursing-homes.mjs` performs only app-contained retained reads and new immutable writes. It validates the exact failed-native intent/profile/limits, five request URLs/methods/caps, completed internal body hashes, documentary pins, before/after metadata equality, failed/nonpublished/resolved-network state and source dates. Each input is stable, single-link, bounded and reverse-rehashed. It reprojects all source rows with v1.0.1. Neither source coordinates nor the TAB become verified business points.

Output is a new UUID beneath `data/business-sources/cms-nursing-home-provider-information/recoveries`, containing selected JSONL and a manifest published last. The manifest depends on every failed-source file/hash and source clock, identifies `retained-native-source-recovery`, and explicitly states `nativeTransportThisRun:false` and `historicalFailureRewritten:false`. The independent reader revalidates the exact original source and complete projected output, not just counts. Unknown identity/current-operation/completeness and local-review-only policy remain unchanged.

Cancellation uses a 180-second local deadline plus caller signal. Prepublication cleanup removes only files with matching ownership; changed ownership stays for inspection. Postpublication failure/cancellation retains manifest/run/hash with inspection-required status. This is local CPU/disk recovery, not a provider request, so it does not acquire the network source lease. It writes no production pointer, modifies no old receipt and performs no external call.

The synthetic-only API uses a different fixed fixture source/output root and cannot produce native claims or satisfy native verification. Seven recovery fixtures plus eleven prerequisite fixtures pass: TAB conservation, unknown points, immutable failed-source hashes, tampered inputs/rehash attacks, native/fixture confusion, cancellation, changed ownership and manifest-last/postpublication inspection. Independent review added a final selected-output hash/identity reread and output-directory identity check, with an explicit mutation-after-first-read regression. The 15 acquisition regression tests also passed after the projection version change, including actual 30-second request timeout. Owned modules pass ESLint. Full native retained recovery, empirical 14,690-row corrected projection and its independent receipt proof remain held for root review; fixture success does not claim those results.
