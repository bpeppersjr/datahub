# Independent service development checkpoint

This checkpoint supplements the chronological service queue. It does not promote isolated code or authorize source acquisition. Main executable/configuration baseline remains `f7f28e1`; the additive production plan `production-cms-directories-20260912-01` still awaits its exact approval.

## Active ownership

- Dedicated app coding/testing agent: isolated `data/worktrees/heatmap-app-integration`, disclosed Astra fallback while Spark is unavailable. Owns app changes and tests, not source discovery.
- National source-service agent: isolated `data/worktrees/maine-provider-preflight`. Owns standalone PECOS acquisition, streaming projection and independent verification.
- State source-service agent: isolated `data/worktrees/retained-feed-integration`. Owns Florida retail-food source contract and standalone connector development.
- Root integrator: reviews contracts and changes, coordinates integration and preserves the production approval boundary.

These are three active specialist slots plus the integrator, not ten simultaneous agents. The ten-feed development backlog is distinct from live concurrency. Routine accepted acquisition belongs to standalone application workers; agents should move to the next development task after a persisted handoff.

## Reviewed and in-progress outputs

### Nursing-home managed adoption

Isolated commit `ae165f8db0e973758da58a8a106875f147b5e353` adds a fixed retained-recovery adoption alongside hospital adoption. Root reviewed source replay, closed source selection, managed verifier routing, non-success source identity and UI summaries without finding a blocking issue in that scope. Original acquisition failure and recovery clocks remain separate. No new download or native adoption was performed.

Focused verification reports 31 top-level passes plus 15 lifecycle child cases, with TypeScript, targeted lint and audit passing. Full `npm run check` stopped at the discovery gate because this isolated checkout lacks `data/business-coverage-views/current.json`; tests were not reached by that command. Separate unit, lint and build checks were dispatched afterward and remain pending at this checkpoint. This is not a full-check success. Additional UI assertions were requested for two simultaneous source cards and a newer failed adoption hiding an older successful count.

### PECOS full-cohort service

The separate streaming lifecycle is under review, not committed or approved for native execution at this checkpoint. It proposes nine sequential responses: three metadata before, three CSVs, three metadata after. Reviewed retained PDFs are copied and hashed rather than downloaded again. The source-native enrollment, organization-owner and additional-NPI relations remain distinct from canonical business identity and present operating status. Individual owner fields are excluded from selected output while exclusion markers conserve source rows.

Root requested a final live policy recheck immediately before manifest publication, after local projection and the pre-publication hook, with a negative drift fixture. Root also requested actual malformed-date preservation assertions, chunk/backpressure and cancellation tests, and configured row-limit boundary evidence. Passing the 11,000-row fixture demonstrates removal of the old 10,000-row limit, not proof of every full-cohort boundary. The native guard and approved download budget remain disabled/zero.

### Florida retail-food service

The earlier three-response metadata preflight succeeded and was independently replayed; it acquired no business records or counts. Subsequent source research found a nonempty public portal-item usage notice distinct from the blank layer item notice. The source agent is retaining that exact evidence and incorporating it into the new contract; empty layer metadata is not treated as a blanket prohibition.

The next lifecycle is a separate isolated implementation using fixed retail-layer membership, bounded counts/OID inventory and selected-field batches, followed by membership and metadata rechecks. Exact FE_TYPE labels remain unresolved: preserve source-native codes, do not infer groceries from risk labels. The proposal allows at most 50,000 rows and 100 batches; metadata/inventory requests must be included in its total request budget. An aggregate byte limit can reject a run before the page limit; it must never silently truncate and claim completeness. Native record/count execution remains disabled. Business point coordinates require explicit source/CRS conformance; a geometry-free first version does not satisfy the final address-geocode requirement.

## Integration boundary

No changes described here were merged into the running application. No production pointer, source quarantine, native acquisition approval or export permission changed. Combine reviewed code only after resolving the pinned production plan's disposition, then run the complete integration gate and the required stop/relaunch checks. Preserve all retained evidence and original failed receipts.
