# Nursing-home retained reporting input

`runner/cms-nursing-home-reporting-input.mjs` is an isolated retained loader and aggregate helper, not registry enrollment or source acquisition. `config/cms-nursing-home-retained-selection.json` is closed to recovery `9fe3aa54-dd38-42be-b28e-b1d363300a45`, manifest SHA256 `89ee608067aa5b97833957b753be61003dc2efcfa22cfaedbb3b78f020396d7e`. The selected artifact is exactly 30,397,131 bytes / 14,690 rows, SHA256 `677b1dc7b294f72feb0d6a0803d27c9f0f074887e5b9c5a59a6837c87a2320d8`.

`loadCmsNursingHomeReportingInput({signal})` independently verifies the recovery and its twelve-file failed-native-source chain, reads the selected JSONL with stable ownership/byte/hash checks, validates its typed membership, replays recovery verification again and rehashes the selection before returning. Source and verifier overrides are rejected. No fetch, copy, recovery execution, output artifact, current pointer, or fallback download exists. Full retained replay belongs to an explicit load, not every UI render.

Snapshots are immutable and registered in a private WeakSet. `cmsNursingHomeReportingFixture` produces only synthetic snapshots; copied/forged snapshots cannot enter aggregation. Native mode is specifically `native-retained-recovery-replayed`, not a successful-acquisition claim. Evidence separates original observation `2026-09-12T14:21:58.087Z`, historical failure `2026-09-12T14:21:59.786Z`, and recovery creation `2026-09-12T14:28:56.881Z`; the historical failure is not rewritten. All failed-source file pins remain exposed as metadata lineage, without raw source bodies.

Rows preserve CCN strings, source identity, original source address (including the flagged TAB), separate ZIP5/ZIP4, dated publisher currently-active assertion, source dates, raw latitude/longitude and footnote, coordinate-quality status, and false facility-point eligibility. Independently verified current operations remain null. No source point becomes a business/site location, no county assignment is inferred, and public export remains false/local-review-only.

`summarizeCmsNursingHomeReporting(snapshot,{states?,zip5s?,signal?})` intersects optional closed state and ZIP filters. Omitted means all; an empty list produces zero selected rows. Fifty states/DC and five territories are valid state filters. Unknown states and missing ZIPs remain explicit unfiltered groups; the same ZIP reported in two states is not merged. Output supplies selected state/state+ZIP counts, missing-ZIP count, TAB-quality count, coordinate-quality counts and zero eligible facility points. These are directory rows, not unique businesses or independently verified sites.

Denominators always describe the full retained cohort: all 14,690, state/DC 14,680, territories 10, unknown states zero. Percentages use these fixed denominators even after filtering. Territory/unknown groups receive null state/DC percentages. There is no nationwide completeness estimate, business count, site count or current-operating count.

## Verification

Ten fixture tests and owned-file ESLint pass: exact selection/path/hash closure; genuine immutable snapshots; typed identity and postal preservation; duplicate/invalid membership; strict dense filters; territory/unknown/zero-result accounting; pre-abort; TAB/source-date/coordinate-quality preservation; and cross-state same-ZIP grouping. The focused cancellation test establishes pre-abort behavior, not a new empirical midstream cancellation guarantee; underlying retained readers/parser forward cooperative signals.

After fixtures, one authorized read-only load of the existing isolated retained cohort completed with global fetch forbidden, a 180-second signal deadline and 185-second process guard. It reproduced all pinned bytes/rows, one preserved flagged TAB, all 14,690 ZIP4 values null, 13,878 publisher-estimate-quality-unspecified plus 812 publisher-zip-based-estimate rows, and zero eligible facility points. Illinois selected 666 rows while retaining all four national denominators. No new files were copied or recovered during this read.

## Explicit downstream admission seam

A subsequent `runner/cms-nursing-home-registry-input.mjs` should consume this fixed loader and declare a distinct nursing-home directory artifact, count, and recovery dependency. Its declaration must pin the recovery manifest/selected artifact and all original failed-source evidence, use recovery creation for registry chronology, and retain original observation/source dates separately. Full ordered membership and local-review policy must be independently verified. Optional absence must leave historical registry imports, counts, identity profiles and artifacts unchanged. Do not pass these rows through existing generic business/site reconciliation or reuse hospital successful-acquisition validation for this recovery. Planner, registry, coverage, UI and export changes are deliberately outside this slice.
