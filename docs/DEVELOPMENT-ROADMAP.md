# Co*Tive Collector development roadmap

Wisconsin prerequisite ordering (September 10, 2026): the [fixed-policy-bound transport path](WI-CHILDCARE-TRANSPORT.md#fixed-policy-prerequisite-before-provider-requests--september-10-2026) checks the full development-policy digest and all five notice fingerprints before provider IDs or rows. Its original synthetic seam and closed native entry remain distinct; policy matching is not acquisition authorization. Full checks passed: 1,825 tests passed, 11 skipped, zero failures, plus lint, builds and desktop smoke; TypeScript and audit passed. No Wisconsin facility records were requested. Next implement durable intent/observation handling and app receipts while the separately requested scoped source-use decision remains pending.

Maryland map enrollment (September 10, 2026): the [PA + MD saved-point layer](RETAINED-CHILDCARE-COUNTY-MAP.md) now uses the verified successor. State/county values follow assigned geography; the right-hand panel separates Pennsylvania and Maryland source contributions, selected/assigned denominators and percentages. Native checks verified 67 PA counties/4,930 rows and 24 MD county equivalents/1,772 rows. Full checks passed: 1,821 tests passed, 11 skipped, zero failures, plus lint, builds and desktop smoke; TypeScript and audit passed. No new source acquisition or national production promotion was performed. The [Maine empty-page prerequisite](states/ME-CHILDCARE-CLIENT-PREREQUISITE-2026-09-10.md) identified linked client metadata but did not establish a provider request contract; no provider search was submitted.

Maryland county successor (September 10, 2026): the [separately governed internal v2 derivative](MD-CHILDCARE-COUNTY-SUCCESSOR.md) is published and independently replayed: 6,702 county relationships, including 1,772 Maryland rows across 24 county equivalents and the unchanged 4,930 Pennsylvania relationships. Full checks passed with 1,817 tests passed, 12 skipped, zero failures, plus lint, builds and desktop smoke; separate native v2 tests, TypeScript and audit passed. No downloads, map enrollment or production-pointer changes occurred. Next add source-specific map denominators and PA/MD geographic scopes before enrolling this successor; the current map remains PA-only.

Earlier geographic prerequisite (September 10, 2026): [Maryland's successor design](MD-CHILDCARE-COUNTY-DERIVATION-DESIGN.md) established the now-implemented metadata binding and preserved PA-only replay. [Iowa's bounded client diagnostic](IA-CHILDCARE-CRS-DIAGNOSTIC.md) made one metadata GET and matched the retained client fingerprint; no explicit CRS markers were found and no provider records were requested. Iowa remains unknown-CRS; do not repeat this unchanged client check or infer datum from map defaults.

County map integration (September 10, 2026): the [retained PA source-point layer](RETAINED-CHILDCARE-COUNTY-MAP.md) and right-hand summary are available, with 67 county features totaling 4,930 assigned points verified against the current release and live authenticated API. Coverage 2.11/registry 2.15 compatibility preserves Tennessee origin and excludes Ohio from county assignment; 19 retained Ohio names for ZIP 43215 were verified locally. Selection-bound responses prevent stale layer labels. Full checks passed: 1,808 tests passed, 11 skipped, zero failures, plus lint, builds and desktop smoke. TypeScript and audit passed; local preview is restored. No business-data download, national promotion or production-pointer change was made. Next implement Maryland's separately versioned derived-evidence policy and successor without altering historical releases, and continue measured source coverage work.

Retained county relationships (September 10, 2026): the [offline point-to-county derivative](RETAINED-CHILDCARE-COUNTY-RELATIONS.md) accounts for all 12,206 retained childcare candidates and assigns 4,930 eligible PA source points to 66 Census counties without new downloads. Missing points, Iowa's unknown CRS and Maryland's not-yet-enabled internal overlay remain distinct gaps; the MD limit is not an identified publisher ban. This is not production county reporting or a verified-business count. Next integrate the versioned relationship evidence into county consumers and review a separate MD derived-evidence policy without changing historical receipts.

Latest NH prerequisite (September 10, 2026): [six retained visible records](states/NH-RETAINED-VISIBLE-03755-2026-09-10.md) have [six offline-normalized addresses](states/NH-VISIBLE-NORMALIZATION-2026-09-10.md), without another download. The [parameterized ZIP-query contract](states/NH-PARAMETERIZED-QUERY-CONTRACT-2026-09-10.md) distinguishes settled empty results from incomplete/over-limit evidence and preserves query/address ZIP differences. It is not enrolled for collection; pagination, broader source-use scope, geocoding and production integration remain unresolved. An [unsent public-register request draft](states/NH-PUBLIC-LICENSE-REGISTER-REQUEST-DRAFT.md) provides a possible single-release alternative.

Current production status: run `production-childcare-retained-20260910-01` subsequently **SUCCEEDED** at `2026-09-10T15:11:10.653Z`, with all eight stages successful, as recorded in the [post-publication report](RETAINED-CHILDCARE-NATIONAL-INTEGRATION.md). The planning paragraph below is historical, not a pending approval. Separately, [Oklahoma's approved batch](states/OK-BATCH-DISPATCH-2026-09-10.md) failed on its first search with HTTP 502 and remains inspection-required; it has not been retried. Neither status establishes national completeness or permission to repeat acquisition.

Retained childcare national integration (September 10, 2026): [versioned candidate reporting and replacement production planning](RETAINED-CHILDCARE-NATIONAL-INTEGRATION.md) now reuse 12,206 PA/CT/MD/VT/CO/UT/IA records without new acquisition or identity/site inference. Record-level registry artifacts and national/state/ZIP coverage fields preserve separate ZIP4, source coordinates, provenance, and 1,979 unknown reported states; county assignment remains explicitly unimplemented. Full checks passed with 1,706 tests passed, 11 skipped and zero failures, including native retained registry/coverage replay, plus lint, builds and desktop smoke. TypeScript and dependency audit passed. New plan `production-childcare-retained-20260910-01` is prepared for review only; the old plan and current production pointers are unchanged. Execution is not authorized by the code-update approval.

Current Overture status (September 10, 2026): [receipt-bound normalization and app management](OVERTURE-NORMALIZATION-SESSION.md) now reuse pinned acquisition/Census evidence behind the standalone managed-operation API. The approved acquisition attempt `a8ff9f6d-b2be-4d56-905b-17984788b1d5` failed; a native API check rejects it without changing its receipt or allocating normalization. No successful production normalization or new promotion is claimed. The management form adds retained input choices and accurate operation-kind/readiness labels; full validation passed with 1,699 tests passed and 11 skipped, plus lint, builds and desktop smoke. Native authenticated listing shows three baseline candidates and zero eligible acquisitions. Older enrollment and authorization statements below are historical. A further large acquisition requires new approval; UI work does not authorize one.

Overture normalization identity check (September 9, 2026, release verified): [disk-backed canonical ID validation](OVERTURE-IDENTITY-CHECK.md) streams IDs into a bounded local database and rejects duplicates without a whole-source JavaScript Set. Six focused checks include a 20,000-ID native fixture and cancellation draining; full checks passed (1,652 passed, 11 skipped, zero failures), with TypeScript, audit and all 82 protected pins unchanged. This component is not yet wired into a managed normalizer; bounded input/output, source-bound receipts and separate promotion remain required. No source acquisition or dataset promotion occurred.

Current Overture handoff: [app-owned bounded acquisition](OVERTURE-ACQUISITION-SESSION.md) is implemented and release-verified (1,645 passed, 11 skipped, zero failures), with retained metadata/runtime prerequisites. No live places acquisition has been authorized or dispatched. [Normalization handoff review](OVERTURE-NORMALIZATION-HANDOFF.md) identifies the remaining provenance, separate-promotion, cancellation and resource contracts. Earlier implementation-stage notes below are historical, not current enrollment status.

Overture numeric evidence (September 9, 2026, release verified): transformation `overture-us-places@1.0.1` no longer coerces missing/nonnumeric coordinates, versions or reported confidence into numbers. Missing coordinates are quarantined and optional missing source confidence remains null; real zero stays zero. The release verifier rejects rehashed out-of-range geocodes. Full checks passed (1,646 passed, 11 skipped, zero failures), with TypeScript, audit and all 82 protected pins unchanged. No production data was changed; managed local normalization remains the next implementation step.

Overture bounded engine composition (September 9, 2026, release verified): [retained runtime and fixed engine setup](OVERTURE-BOUNDED-ENGINE.md) combine owned directories, settings readback, the shared selection query and bounded output behind one lifecycle. Full checks passed (1,617 passed, 11 skipped, zero failures), including direct native invocation and a rejected-capability failure with no upstream calls. TypeScript and production dependency audit passed; all 82 protected pins remain unchanged. Source preflight/authorization, bridge/journal ownership and final managed-operation receipt binding remain next; no remote acquisition or national promotion occurred.

Overture shared selection query (September 9, 2026, release verified): [one projection for COPY and streaming](OVERTURE-STREAMING-QUERY.md) preserves the legacy SQL while adding strict indexed bridge inputs and a capability-free query fingerprint. Full checks passed (1,616 passed, 11 skipped, zero failures), including the expanded native fixture selecting three of six synthetic records, conserving exact fields and U.S./status filters, excluding geometry/contact fields and normalizing ZIP5/ZIP4 separately. TypeScript and production dependency audit passed; all 82 protected pins remain unchanged. Engine/worker admission and final operation binding remain next; no remote acquisition or national promotion occurred.

Overture bounded selected output (September 9, 2026, release verified): [chunked JSON and gzip output](OVERTURE-SELECTED-OUTPUT.md) adds row/raw/compressed limits and sampled disk headroom without an unbounded export destination. Full checks passed (1,613 passed, 11 skipped, zero failures), including native local composition of retained httpfs, bridge, transport, accounting and output writer, with independent gzip/content/hash checks. TypeScript and production dependency audit passed; all 82 protected pins remain unchanged. This does not migrate the production preparation command or prove a process-wide memory quota; fixed query/engine setup and managed final-receipt binding remain next. No source download or national promotion was dispatched.

Overture durable request accounting (September 9, 2026, release verified): [operation-bound journal](OVERTURE-ACQUISITION-JOURNAL.md) syncs reservations before transport dispatch and separately records completion. Offline replay preserves pending-request uncertainty and never infers acquisition success or restart permission. Full checks passed (1,604 passed, 11 skipped, zero failures), including a local native Parquet query composing the retained runtime, bridge, transport and journal without external data requests. TypeScript and production dependency audit passed; all 82 protected pins remain unchanged. Worker enrollment, contained extraction resource controls and final receipt/output binding remain next; no national data changed.

Overture local asset bridge (September 9, 2026, release verified): [controlled loopback protocol](OVERTURE-ASSET-BRIDGE.md) connects the serial asset transport to native retained httpfs in a two-row local Parquet test. Ephemeral capability paths, exact Host and range admission, backpressure and cancellation keep this an indexed internal bridge rather than an arbitrary URL proxy. Full checks passed (1,596 passed, 11 skipped, zero failures), including native local compatibility, plus TypeScript and a clean production dependency audit; all 82 protected pins remain unchanged. No source request or dependency refetch occurred. Production preparation and managed acquisition are not wired yet; next compose retained-runtime admission, selected-field query contracts and durable operation/resource accounting. No national data or production pointer changed.

Overture asset transport core (September 9, 2026, release verified): [fixed-asset serial transport](OVERTURE-ASSET-TRANSPORT.md) adds redirect rejection, HEAD/ETag-pinned byte ranges, request/payload reservations and cooperative cancellation that drains late fetches and sinks. Sixteen focused synthetic tests and full checks passed (1,588 passed, 11 skipped, zero failures), plus TypeScript and a clean production dependency audit. All 82 protected production pins remain unchanged. The core is not yet wired to DuckDB or managed acquisition; no place assets were requested and no acquisition readiness or national coverage gain is claimed. Next implement the controlled bridge, retained-runtime consumption and operation-bound resource accounting.

Overture runtime prerequisite (release verified and app-completed): [app-owned bounded dependency preparation](OVERTURE-HTTPFS-RUNTIME.md) adds a fixed Windows httpfs worker, contained storage, core-signature loading, hash-checked receipts and managed cancellation/restart handling. Operation `b4ae8318-e786-4ac8-bf7b-e673c6d00ae1` succeeded and its retained artifacts were independently hash-verified without another download. Full checks passed (1,572 passed, 11 skipped, zero failures), plus TypeScript and a clean production dependency audit. All 82 protected production pins remain unchanged. Next wire retained-runtime consumption and extraction resource controls; this is not Overture place acquisition or national promotion.

Overture acquisition lifecycle (release verified): [handle cleanup and setup-cancellation protection](OVERTURE-US-PLACES.md#extraction-lifecycle-hardening-september-9-2026) now prevent preparation from silently succeeding after database cleanup failure. Full checks passed: 1,557 tests passed, 11 skipped, zero failures, plus lint, web/desktop builds and desktop smoke (`data/tmp/overture-lifecycle-full-check.log`). TypeScript and production dependency audit passed; all 82 protected production pins remain unchanged. App resource budgets, contained extension/storage setup, managed-operation enrollment and large-acquisition authorization remain unresolved; no place records were downloaded or national pointers changed.

Oklahoma profile/address semantics (September 9, 2026, verified): [bounded detail validation and pure field helpers](states/OK-CHILDCARE-PROFILE-FIELDS-2026-09-09.md) establish an observed two-line address pattern and separate string-valued notice flags missing from search results. The diagnostic followed one displayed center link, retained no provider values and requested no reports/maps. Conservative offline helpers preserve source address lines, split ZIP5/ZIP4, and keep unrecognized formatting and operating status unknown. These helpers are not yet wired into a statewide collector; notice absence is not active-business verification. Full checks passed (`data/tmp/ok-profile-fields-full-check.log`): 1,549 passing tests, 11 skipped, zero failures, lint, web/desktop builds and desktop smoke. TypeScript passed, production audit found zero vulnerabilities and all 82 protected production pins remain unchanged. The idle development service was stopped for checks and restored afterward; no collection operation was dispatched.

Oklahoma managed schema prerequisite (September 9, 2026, verified): [app operation contract and native handoff](states/OK-MANAGED-SCHEMA-PREREQUISITE.md) add authenticated fixed dispatch, operation-bound aggregate manifests and an independent receipt reader. Operation `ec134ac1-f1ea-4a2f-b6d8-6b5bd8c3735f` succeeded with a separately verified manifest: four center rows and four numeric coordinate pairs. The supervisor verifies byte integrity and structural/count consistency before prerequisite success, preserves committed references on failure/cancellation and does not retry source requests on restart. Full checks passed (1,540 passing tests, 11 skipped, zero failures), plus seven focused reader/operation tests including a late-added restart regression. Lint, builds, desktop smoke, TypeScript and production audit passed; all 82 protected production pins remain unchanged. This enrolls only the fixed schema check; source-wide collection, temporal verification and refresh scheduling remain incomplete.

Oklahoma executable schema prerequisite (September 9, 2026): [bounded delivery evidence and standalone CLI](states/OK-CHILDCARE-BOUNDED-SCHEMA-2026-09-09.md) now replace the untested search-route assumption. A native three-request validation persisted aggregate-only receipt `28593b49-d3bf-47f2-be3e-b38bc867afb1`: four center rows and four in-range numeric coordinate pairs, with unchanged client pins. [Source-use review](states/OK-SOURCE-USE-2026-09-09.md) supports this narrow public lookup without inventing a broad licensing prohibition. Source-wide coverage, operating status, address/ZIP parsing and managed-queue enrollment remain incomplete. This is not a production collection or national coverage gain. Next add operation-bound verification and validate broader delivery semantics before routine app-owned acquisition.

Oklahoma prerequisite release verification: `data/tmp/ok-schema-prerequisite-full-check.log` records 1,529 passing tests, 11 skips, zero failures, lint, web/desktop builds and desktop control-plane smoke. TypeScript passed and the production dependency audit found zero vulnerabilities. All 82 protected production pins remain unchanged. The idle development service was stopped for checks and restored; no production operation was launched. The only native prerequisite run is identified above; its receipt is local evidence, not a public provider dataset or managed-operation handoff.

Nebraska prerequisite hold (September 9, 2026, release verified): the [PDF-specific policy assessment](states/NE-PDF-INTERNAL-USE-2026-09-09.md) found a concrete linked copying/automation restriction with unresolved applicability to DHHS's separately hosted public roster. The [app prerequisite gate](SOURCE-ACQUISITION-PREREQUISITE-GATES.md) exposes the reason in the authenticated catalog and rejects dispatch with HTTP 409 before allocation, receipts, workers or source requests. Capture is not implemented or approved. A [publisher clarification draft](states/NE-PDF-CLARIFICATION-DRAFT-2026-09-09.md) is prepared but unsent. This replaces the earlier assumption that PDF capture could be the immediate next implementation; other authorized-source work can continue independently. No statewide legal prohibition, PDF acquisition or national coverage gain is claimed.

Prerequisite-gate release evidence: `data/tmp/source-prerequisite-gates-full-check.log` records 1,516 passing tests, 11 skips and zero failures, plus lint, web/desktop builds and desktop control-plane smoke. TypeScript passed and the production dependency audit found zero vulnerabilities; all 82 protected production pins remain unchanged. After restoring the local app, an authenticated catalog read exposed the hold and the exact prerequisite POST returned HTTP 409 with unchanged operation IDs. No source download was dispatched. This is an API/catalog hold, not a capture implementation or universal policy gate for legacy jobs.

Delivery validation (September 9, 2026): [Nebraska's official PDF roster](states/NE-CURRENT-EDITION-2026-09-09.md) has a September 8, 2026 HTTP modification signal and 1.26 MB size, providing a newer free delivery lead than the stale GIS cohort. Next build the app-owned PDF edition/schema prerequisite after mixed-content internal-use review; HTTP freshness alone is not a verified current roster. [New Hampshire's normal UI](states/NH-BOUNDED-UI-VALIDATION-2026-09-09.md) returned six licensed-group results for one ZIP, but its generated CSV remains unvalidated because the available browser interface lacks a controlled download destination. No export failure or current-business status is inferred. Source validation is progressing; production acquisition is not enabled by these notes.

[Oklahoma's static filter contract](states/OK-CHILDCARE-FILTER-CONTRACT-2026-09-09.md) now resolves the exact center value `childcare-center`, ZIP key `zip-code`, default omission rules and finite current quality-rating domain from hashed publisher assets. This is client syntax, not verified server filtering or delivered records. Next use an app-controlled bounded center-only ZIP validation with ancillary-request limits; no source acquisition was launched by the static review.

Delivery-validation release evidence: `data/tmp/ne-nh-ok-delivery-validation-check.log` records successful full checks (1,513 passed, 11 skipped, zero failures), lint, web/desktop builds and desktop smoke. Production audit found zero vulnerabilities; 82 protected production pins remained unchanged. Only documentation changed. The idle local development service was stopped for checks and restored; no production job was interrupted or launched.

Parallel source-contract discovery (September 9, 2026): [Nebraska's official statewide service](states/NE-SOURCE-FOLLOWUP-2026-09-09.md) is now resolved through NebraskaMAP. A single bounded center aggregate returned 699 center and 51 provisional-center source rows, all dated October 15, 2025; these are historical cohort counts, not current businesses or an acquired dataset. Next resolve a current edition and implement durable metadata/date-aware preflight. [New Hampshire](states/NH-SOURCE-FOLLOWUP-2026-09-09.md) has a concrete public CSV serializer/export lead needing ordinary-UI delivery, redaction and status validation. [Oklahoma](states/OK-CHILDCARE-CONTRACT-DISCOVERY-2026-09-09.md) exposes a center/home UI model but no verified statewide delivery contract. No facility acquisition, paid list, source enrollment or national promotion was dispatched. These findings prioritize implementation without occupying agents with routine downloads.

Discovery release verification: `data/tmp/ne-nh-ok-source-discovery-check.log` records successful full repository checks (1,513 passed, 11 skipped, zero failures), lint, web/desktop builds and desktop control-plane smoke. The production dependency audit found zero vulnerabilities; all 82 protected production pins remained unchanged. This increment changes documentation only and does not establish executable connector readiness. The idle local development service was stopped for verification and restored afterward.

Read-only childcare map comparison (release verified): the [right-hand source panel](RETAINED-CHILDCARE-MANAGED-OPERATION.md#read-only-map-side-comparison) consumes the pinned successful app snapshot without another operation or download. State/reported-ZIP scope, source-denominator percentages and unknown address-state labels remain separate from map shading and national completeness. The authenticated live route returned all seven cohorts. Full release checks passed: 1,513 tests passed, 11 skipped, zero failures; lint, web/desktop builds, desktop smoke, TypeScript and production audit passed. All 82 protected production pins remain unchanged.

Live snapshot completed and verified: operation `bafb683b-f4ae-4355-983a-d2a3c85e7d9b` succeeded with seven source cohorts. [Terminal evidence and pinned enrollment](RETAINED-CHILDCARE-MANAGED-OPERATION.md#verified-completion-and-reusable-enrollment) allow downstream reuse without rebuilding or source replay. Two enrollment tests pass. Historical implementation milestones below retain their acceptance-time proof boundaries; the latest status is above.

Live snapshot handoff accepted: Co*Tive operation `bafb683b-f4ae-4355-983a-d2a3c85e7d9b` has a persisted receipt after HTTP 202. [Acceptance evidence](RETAINED-CHILDCARE-MANAGED-OPERATION.md#accepted-live-application-handoff) is not terminal success. The app owns the offline build; inspect the same operation before any retry. No source refresh or national pointer change was requested.

Managed childcare snapshots (release verified): [app operation and authenticated endpoint](RETAINED-CHILDCARE-MANAGED-OPERATION.md) dispatch the fixed offline snapshot worker with app-chosen path and operation identity. Thirty-nine integrated tests and full release checks passed (1,508 passed, 11 skipped, zero failures); TypeScript and production audit passed. Catalog/UI discovery, live receipt handoff and map integration remain pending.

Retained childcare snapshot storage (in validation): [builder, hash-pinned reader and CLI](RETAINED-CHILDCARE-SNAPSHOT-DESIGN.md) implement immutable local view storage and cheap reads without source replay. Eleven snapshot tests and eight comparison tests passed, including postcommit faults and enrollment drift. Native standalone snapshot `31f83308-3393-4c6c-9475-22f6b9c67d61` was independently read and verified; it is not a managed-operation handoff. Parent recovery integration, full release checks and managed-operation/map wiring remain pending; no new source acquisition occurred.

Retained childcare comparison (verified): the [seven-source backend view](RETAINED-CHILDCARE-COHORT-VIEW.md) combines verified reporting enrollments without source downloads or national pointer changes. Seven focused tests and full release checks passed (1,490 passed, 11 skipped, zero failures); TypeScript and production dependency audit passed. The [durable app snapshot contract](RETAINED-CHILDCARE-SNAPSHOT-DESIGN.md) is reviewed but unimplemented, and map integration remains pending. Source-cohort rows are not unique active businesses or national completeness.

Iowa retained local reporting (verified September 8, 2026): [offline publisher-cohort reporting](IA-CHILDCARE-REPORTING.md) binds the completed app receipt to 1,476 accepted candidates across 404 reported ZIP5 values. Local reporting preserves null address-state and separate observation/processing clocks without repulling. Full repository checks passed (1,483 passed, 11 skipped, zero failures), TypeScript passed and production dependency audit found zero vulnerabilities. National evidence totals and heatmap integration remain separate.

Iowa completed app collection (September 8, 2026): Co*Tive operation `8681405a-0034-435a-bbe9-96263816381d` independently acquired and normalized the source. [Offline receipt/release verification](IA-CHILDCARE-APP.md#verified-app-owned-collection--september-8-2026-local) confirms 1,476 center/preschool candidates, zero quarantine, 404 reported ZIP5 values and 1,476 source points of unverified accuracy; all ZIP4 values remain null. Next integrate retained local cohort reporting without repulling or converting map membership into verified current operation or national completeness.

Iowa standalone processing (September 8, 2026): [offline normalization](IA-CHILDCARE-NORMALIZATION.md) preserves selected-row identity, duplicates, separate postal fields, source points and quality gaps. The [app lifecycle](IA-CHILDCARE-APP.md) composes acquisition and normalization with durable linked receipts and explicit retained reuse. Iowa-only industry enrollment does not imply measured national coverage or a submitted job. Actual handoff evidence must be recorded separately before releasing acquisition to the app.

Iowa selected acquisition (September 8, 2026): the [bounded center/preschool source layer](IA-CHILDCARE-ACQUISITION.md) adds selected-only projection, release-scoped ordinals, duplicate conservation, awaited checkpoints and immutable offline verification. The reviewed internal policy does not grant public export or assert current operation. No native facility collection was run; normalization and app-worker enrollment remain next so Co*Tive, not a download-polling agent, owns the first collection.

Iowa schema prerequisite (September 8, 2026): the [bounded map-interface assessment](IA-CHILDCARE-SCHEMA-PROBE.md) pins the published client and fixed empty-form request, retaining sanitized schema/count evidence rather than provider values. It separates center display class from licensed/current-business scope and does not enroll routine collection. Native results and verification are recorded in the linked evidence document. Parallel [Kentucky follow-up](states/KY-CHILDCARE-FOLLOWUP-2026-09-08.md) identifies a forthcoming inspection-system migration, not a verified bulk directory. Validated connectors must hand routine acquisition to Co*Tive rather than occupy state agents with downloads.

Utah retained local reporting (September 8, 2026): [source-candidate reporting and enrollment](states/UT-CHILDCARE-REPORTING.md) connect the completed offline app receipt to state/ZIP cohort summaries and explicit quality gaps. Original observation, normalization and adoption clocks remain separate. Local evidence does not change national totals, active-business claims or completeness; heatmap UI integration and reviewed future-edition refresh remain outstanding.

Utah app-owned retained adoption (September 8, 2026): the [dedicated offline app worker](states/UT-CHILDCARE-APP.md) enrolls the verified September release in the existing industry/managed operation lifecycle, with new linked execution receipts and unchanged historical source evidence. It performs no download or normalized rebuild. Future-edition acquisition and state reporting remain separate work; no national production pointer or recurring schedule is changed.

Utah source-linked normalization (September 8, 2026): the [retained-origin verifier and normalized release workflow](states/UT-CHILDCARE-NORMALIZED-RELEASE.md) bind recorded download provenance to full local PDF replay and center-only internal records. The registered offline contract preserves separate ZIP fields, source/license clocks and unknown operating/geocode/address-role facts. Manifest-last publication and verification compare complete records plus source/configuration/artifact fingerprints, including late same-size rewrites. Next managed-operation enrollment and state reporting; no refetch or national promotion is part of this increment.

Utah standalone-runtime increment (September 8, 2026): the [app-owned Python environment and supervised PDF process](states/UT-CHILDCARE-PDF-RUNTIME.md) validate the retained report without Codex or a new download. Hash-pinned dependencies, Windows committed-memory limits, cancellation/closure, bounded redacted process output and internal manifest-last receipts are implemented. Actual-source tests conserve 1,961 rows and 422 center observations. Next add acquisition-origin linkage/replay, normalized output and managed-operation enrollment; this local prerequisite is not a production collection or national coverage promotion.

Utah PDF implementation prerequisite (September 8, 2026): the [offline decoder and layout gate](states/UT-CHILDCARE-PDF-PREREQUISITE.md) now process the retained document without refetching it. Actual integration conserves 1,961 rows and selects 422 center-category observations, with source page/row provenance, separate ZIP fields and no inferred operating status or geocodes. Synthetic failures exercise geometry, headings, row identity, categories, dates and cancellation. Next provision the app-owned decoder environment and tested supervised execution/receipts, then acquisition and normalization enrollment. The test interpreter is not a production dependency configuration.

Utah retained-document prerequisite (September 8, 2026): a [bounded local PDF assessment](states/UT-CHILDCARE-PDF-ASSESSMENT-2026-09-08.md) resolved the web-reader limitation and retained the unmodified 332,869-byte report with provenance. Independent coordinate-based passes agreed on 31 pages, 1,961 report rows and 422 exact center-category rows. The [versioned layout evidence](states/UT-CHILDCARE-LAYOUT-2026-09-08.json) documents column geometry, category conservation and missing-field observations; generic table extraction is unsafe. Next build tested PDF decoding/layout validation against this retained document, then app-owned acquisition/normalization. No operating-status, geocode, national coverage or production acquisition claim follows from document inspection. Parallel Nebraska catalog follow-up confirmed an environment rendering limitation, not publisher denial.

Parallel missing-state discovery (September 8, 2026): [Utah](states/UT-CHILDCARE-TRIAGE-2026-09-08.md) publishes a current linked statewide regulated-childcare PDF; a native header check confirmed its small object size, but schema inspection remains incomplete. [Nebraska](states/NE-CHILDCARE-TRIAGE-2026-09-08.md) has official mapping/roster leads but no verified statewide machine-readable layer yet. [Kansas retail food](states/KS-RETAIL-FOOD-TRIAGE-2026-09-08.md) mixes grocery and other establishment types and has an inspection-platform transition, not a verified current roster contract. [Iowa follow-up](states/IA-CHILDCARE-TRIAGE-2026-09-08.md) distinguishes vacancies/non-referral status from operating status and identifies filtered favorites export as a bounded delivery lead. These findings select concrete next validation actions; they do not establish business counts, new source policies, collection enrollment or national coverage.

Discovery-increment verification: `npm run check` passed with 1,377 tests (1,366 passed, 11 skipped, zero failures), lint, builds and desktop smoke; type checking passed and the production dependency audit reported zero vulnerabilities. All 82 pending production pins remained unchanged. Log: `data/tmp/ut-ne-ks-ia-discovery-full-check.log`. Changes are documentation only, and the idle development service was restored after checks. No new agreement, paid service, bulk collection or national launch was initiated.

Colorado retained local reporting (September 8, 2026): the app-owned collection completed and its full retained chain verified offline. [Pinned local reporting](CO-CHILDCARE-REPORTING.md) exposes 1,648 accepted source candidates, zero quarantine, 321 distinct reported ZIP5s, three separate ZIP4s and explicit point/status/capacity gaps. All reported states are CO; cohort percentages do not establish industry completeness. Reporting reuses retained releases, does not repull the source, and leaves national production pointers and completeness unchanged.

Colorado standalone processing (September 8, 2026): [offline normalization](CO-CHILDCARE-NORMALIZATION.md) preserves precise provider identifiers, sparse provenance, separate ZIP5/ZIP4, source-address semantics and explicit missing operating status/geocodes. The [standalone app lifecycle](CO-CHILDCARE-APP.md) passed focused and full validation and adds native publisher exclusion, durable child checkpoints, explicit offline reuse and strict industry enrollment. Co*Tive accepted Colorado-only operation `7b75574d-cdda-4262-9932-e7639e965204` with a persisted receipt and owns ongoing collection. Handoff is not terminal success or national coverage; agents do not poll ordinary download progress. Next verify the eventual terminal chain for downstream retained reporting without repulling.

Colorado selected acquisition (September 8, 2026): [two bounded delivery samples](states/CO-CHILDCARE-SELECTED-DELIVERY-2026-09-08.md) confirmed integer-text IDs, selected fields and cross-page ordering between paired preflights. The [bounded acquisition engine and immutable offline verifier](CO-CHILDCARE-ACQUISITION.md) now implement baseline/selected/final membership reconciliation, durable journals and cancellation boundaries. Synthetic verification is not a completed native acquisition: no full download or app operation was dispatched. Next implement normalization and the standalone Co*Tive lifecycle; routine downloads must belong to an accepted app operation, not an agent waiting on requests. Existing verified releases remain reusable and national production pins are unchanged.

Colorado executable prerequisite (September 8, 2026): [the bounded preflight and offline verifier](CO-CHILDCARE-PREFLIGHT.md) now pin the exact source contract and retain paired sanitized metadata/category/count evidence. The native six-request receipt independently replayed with 1,648 center rows, matching distinct-license and nonnull address/ZIP/state counts, without facility records or IDs. Next validate selected delivery and implement bounded acquisition/normalization before Co*Tive app handoff. Current operations, exact geocodes, national integration and record-level export remain unverified; no production pin or source selection changed.

Next source implementation (September 8, 2026): [Colorado's official childcare contract discovery](states/CO-CHILDCARE-ACCESS-2026-09-08.md) found PDDL metadata, a current 27-field schema and 1,648 center-category source rows in a bounded aggregate. Build paired metadata/category preflight next, then selected delivery and app-owned acquisition. Do not infer coordinates from older geospatial documentation or license dates from quality-rating dates. [Iowa](states/IA-CHILDCARE-TRIAGE-2026-09-08.md) and [Maine](states/ME-CHILDCARE-TRIAGE-2026-09-08.md) public searches remain bulk-contract gaps, not proof of absent businesses or prohibited access. Minnesota's retained registration failure was reviewed again; its exact-version malformed-CSV hold remains appropriate without offending source bytes or a verified publisher correction. Successful residential data remains reusable. No new acquisition or national production launch resulted from these reviews.

Source prerequisites resolved further (September 8, 2026): [Monroe County's use review](states/IN-MONROE-FOOD-USE-REVIEW-2026-09-08.md) found a personal/noncommercial limitation and written-permission requirement for reuse; acquisition stays un-enrolled pending source-specific clarification. [Its category definitions](states/IN-MONROE-FOOD-CATEGORIES-2026-09-08.md) identify Multi-Market as farmers-market operations, not groceries. In parallel, [Vermont's fresh metadata and anonymous aggregate queries](states/VT-CHILDCARE-METADATA-2026-09-08.md) establish 503 center-cohort source rows, separate family-home categories, ODbL licensing and deliberately jittered publisher coordinates. Next build a paired Vermont preflight and source-specific app pipeline; do not expose jittered points as exact address geocodes or count the monthly cohort as current unique businesses. No facility records or new app jobs were acquired by these checks.

Parallel source discovery (September 8, 2026): [Indiana retail-food research](states/IN-RETAIL-FOOD-TRIAGE-2026-09-08.md) traces Monroe County's official dashboard to a public facility layer and aggregate category/status evidence, distinct from a joined inspection layer and statewide coverage. Category definitions, current-license semantics and applicable use notices remain prerequisites. [Kentucky childcare](states/KY-CHILDCARE-TRIAGE-2026-09-08.md) requires an exact Type I predicate because Type II centers are home-based; no bulk delivery contract was verified. [Illinois retail food](states/IL-RETAIL-FOOD-TRIAGE-2026-09-08.md) establishes local regulatory scope and a Chicago-only inspection lead, not a statewide current-business roster. These peer workstreams requested no business records and enabled no downloads or schedules. Next validate the county source contract before building app-owned acquisition; do not infer a grocery category from undocumented labels.

Credential category comparisons are app-visible (September 8, 2026): [the 51-jurisdiction credential matrix](CREDENTIAL-COVERAGE-VIEW.md) now consumes the exact verified Minnesota reporting release through the existing authenticated retained-evidence endpoint. State/category filters preserve within-state and category-across-state denominators, outside/unresolved rows and unknown business coverage. The dashboard retains postal browsing and never acquires data on a read. This is separate reporting evidence, not a national business-count or physical-site promotion; all pending production pins remain unchanged.

Minnesota credential reporting input published (September 8, 2026): [the new source-row projection and immutable derivative](MN-CONSTRUCTION-CREDENTIAL-INPUT.md) preserve all 11,456 retained residential credential rows, separate ZIP fields, source dates and explicit non-site/non-matching eligibility. Release `30cd9c0e-0a8d-467c-b416-150453e1513f` independently replays the original verified app acquisition without downloads. Source-job output overlap and pre-acquisition derivative timestamps are rejected. National consumers still require explicit credential-evidence integration; no current national pointer or pending production pin changed.

Minnesota residential credentials enrolled in local state reports (September 8, 2026): [the generated 51-jurisdiction report](MN-CONSTRUCTION-STATE-REPORT.md) now includes checksum-verified retained credential evidence with 11,456 rows across 34 reported-address states. This separate section preserves published national statuses and cohort-only percentages, and does not manufacture sites, businesses or independent state-publisher access. Missing data is unavailable, not zero. No dashboard or national production integration is claimed; existing pins remain unchanged.

Minnesota residential evidence verified and reportable offline (September 8, 2026): [11,456 accepted credential rows](MN-CONSTRUCTION-REPORTING.md) are retained from the successful cohort despite the parent run's registration failure. A reusable offline summarizer verifies the full evidence chain and returns reported state/ZIP/category counts with cohort-only percentages and null business-completeness claims. Registrations remain unavailable with `source-csv-invalid`. No successful source was repulled; national integration remains next.

Minnesota selected-stream 1.1 implemented (September 8, 2026): [byte-preserving CSV framing and explicit unresolved-text row dispositions](MN-CONSTRUCTION-BYTE-FRAMING.md) now allow valid selected fields past undecodable excluded data without guessed charsets. Strict CSV structure, original-byte hashing, complete acquisition gates and legacy 1.0 replay remain enforced. Rejected rows are counted as gaps, not businesses. Existing production pins and earlier failed data remain unchanged.

Minnesota registrations encoding incompatibility confirmed (September 8, 2026): [a bounded 1 MiB range check](MN-CONSTRUCTION-ENCODING.md) found invalid UTF-8 at byte 478,182 under the failed job's recorded source ETag. No full CSV or raw diagnostic body was retained. Replacement encoding and the residential source remain unproven. Next work is explicit byte-preserving framing and unresolved-text row dispositions, not guessed decoding or blind download retries.

Minnesota streaming failure investigated (September 8, 2026): [both native cohort jobs failed before completed acquisition](MN-CONSTRUCTION-FAILURE-20260908.md). Their partial selected evidence is preserved, excluded from coverage, and not repulled. Privacy-safe finite diagnostic categories now survive the app's streaming/error boundaries; historical generic errors cannot prove the actual cause. Strict UTF-8/CSV/source checks remain enabled. Source-boundary investigation is next; national production pins are unchanged.

Minnesota collection handed to the live app (September 8, 2026): [managed operation `cd9c3c93-ab0c-429e-9553-ef6252625ac0`](MN-CONSTRUCTION-HANDOFF-20260908.md) was accepted with persisted history for exactly two MN construction cohorts. Co*Tive owns source checks and downloads; agents are not monitoring routine transfer progress. Dispatch is not acquisition success or national coverage. Verify and reuse retained evidence for the next reporting integration; no national rebuild was relaunched.

Minnesota app lifecycle and construction enrollment implemented (September 8, 2026): [two fixed native entry points, durable start/prerequisite/acquisition/terminal history and independent app verification](MN-CONSTRUCTION-APP.md) now join the source checks, selected data and shared publisher gate. Competing cohort jobs wait locally with a bounded cancellable window instead of becoming immediate industry failures. Completed acquisition survives cancellation and finalization failure for offline reuse. Tests use injected responses; no native Minnesota operation, new full download or national coverage gain is claimed. Existing national production pins remain unchanged.

Minnesota shared publisher gate implemented (September 8, 2026): [one fixed cross-cohort lock and one-second handover pacing](MN-CONSTRUCTION-PUBLISHER-LOCK.md) now support app-worker exclusion, with same-process and separate-process contention tests. Ownership checks preserve substituted, linked, corrupt and crash-left evidence; there is no automatic takeover or download retry. This primitive must still be wired around the native app operation lifecycle and cohort entry points before claiming runtime enrollment. No new download or national coverage is claimed; pending production pins remain unchanged.

Minnesota durable acquisition evidence implemented (September 8, 2026): [immutable parent receipts and offline verification](MN-CONSTRUCTION-ACQUISITION-RECEIPT.md) bind the schema preflight, before/after policy evidence, measured transfer and exact verified child manifest. The standalone verifier replays without network requests. Concurrent writers and caller-mutation/cancellation boundaries are tested. The app operation lifecycle, shared publisher lock and cohort entry points remain next; a durable injected-evidence receipt is not native app enrollment. Existing data and pending national production pins remain intact.

Minnesota streaming acquisition composition implemented (September 8, 2026): [exact HEAD/GET/HEAD transport, before/after notice checks and selected-bundle verification](MN-CONSTRUCTION-TRANSPORT.md) now withhold successful EOF until source identity, measured length and final policy evidence pass. A shared monotonic clock covers cross-stage ordering. Injected integration tests use synthetic CSV and retained internal notice evidence without network requests. Parent acquisition evidence persistence and two app entry points with shared publisher pacing remain required before actual native dispatch; no full Minnesota download or national coverage gain is claimed. Pending national code/configuration pins remain unchanged.

Minnesota source-use prerequisite implemented (September 8, 2026): [bounded publisher-notice capture and conditional internal-use binding](MN-CONSTRUCTION-SOURCE-USE.md) now reject changed or stale complete article evidence before future native enrollment. A saved two-page receipt supports historical offline replay; no full CSV was requested. Native transport and an actual app-operation receipt remain required before calling acquisition handed off. Routine downloads belong to Co*Tive workers, not agent polling. The pending national memory plan remains unchanged.

Minnesota durable selected bundles implemented (September 8, 2026): [UUID-isolated selected frames, receipt, normalized records and manifest-last acceptance](MN-CONSTRUCTION-RETAINED-SELECTION.md) now support disk replay, conservation checks and reuse without a CSV repull. Precommit rehashing catches tested same-size mutations; ordinary failures retain incomplete evidence and cancellation removes only owned unpublished files. Native source-use/transport binding and app enrollment remain next; no Minnesota acquisition or national coverage is claimed. The pending national memory plan stays unchanged.

Minnesota streaming selection and replay implemented (September 8, 2026): [bounded CSV-to-selected-frame accounting](MN-CONSTRUCTION-SELECTED-STREAM.md) hashes consumed source bytes, conserves accepted/rejected row dispositions without retaining rejected personal values, and reconstructs accepted normalized records without another CSV download. Stalled-input and replay cancellation are covered. This is not yet durable acquisition or an app job; next add run-isolated storage, source-use binding and verified release publication before native dispatch. The pending national memory plan remains unchanged.

Minnesota credential normalization implemented (September 8, 2026): [pure privacy-selected business-credential records](MN-CONSTRUCTION-NORMALIZATION.md) distinguish IR registrations and residential business licenses, exclude personal/unknown/non-Issued records, preserve reported-address uncertainty and separate ZIP4. Missing address-role evidence no longer blocks credential normalization; physical-site/matching eligibility remains false. Next build conserved, replayable selected-source acquisition and native app enrollment. No Minnesota full acquisition or national coverage is claimed; the pending national memory plan remains unchanged.

Production failure repair (September 8, 2026): Ohio retained-data run `production-oh-childcare-20260908-01` is terminal FAILED, not running or promoted. The registry child exhausted its implicit JavaScript heap; downloads and all four earlier production pointers remain intact. [Explicit app-owned memory profile](PRODUCTION-MEMORY.md) adds pinned 12 GiB old-space settings with prelaunch capacity checks. A new retained-data plan is required; preserve the old failure and staging. No repull is needed.

Replacement `production-oh-memory-20260908-02` is [planned but not launched](PRODUCTION-MEMORY.md#replacement-handoff-planned-not-launched): all 996 tests pass and retained selections match, but the session execution policy rejected background launch. No replacement receipt or controller lock exists. The linked handoff includes the exact standalone terminal command; do not claim app acceptance until a real receipt and live handle exist.

Minnesota aggregate code prerequisite (September 8, 2026): [schema-2 bounded code profile](MN-CONSTRUCTION-PREFLIGHT.md#aggregate-code-prerequisite-schema-2) retains finite code counts only, with no personal fields or source rows. Both prefixes contain literal Business records and mixed status buckets; these are nonrepresentative observations, not business totals or a complete codebook. Existing schema-1 receipts remain supported. Next resolve address/code semantics and source policy, then implement durable native app acquisition and hand off with an actual operation receipt. No Minnesota full download or schedule was enabled; Co*Tive continues to own already accepted production work.

Minnesota schema prerequisite implemented (September 8, 2026): [bounded native header check](MN-CONSTRUCTION-PREFLIGHT.md) verified the same 18-column contract in both DLI construction exports with six serial requests and 8,192 prefix bytes consumed; only headers were retained, not business records. The source exposes `Bus_Pers`, credential/status/address fields and contacts, but value codebooks and address roles remain unverified. Next resolve those semantics and source policy, then implement a privacy-selected native app handoff. Earlier [HEAD-only discovery](states/MN-CONSTRUCTION-ACCESS-2026-09-08.md), Minnesota's SOS hold and food-license assessment remain historical/separate evidence.

Parallel [NC/VA childcare triage](states/NC-VA-CHILDCARE-TRIAGE-2026-09-08.md) found a Virginia VQB5 reporting download candidate but an explicit commercial-use permission gate in its linked VDOE policy; no workbook was requested. North Carolina's dashboard points 2022-current requests to a data-request portal, not a verified current row feed. Preserve both broad-registry holds and do not infer current licensed-business coverage from those sources.

Current handoff (September 8, 2026): [Ohio national rebuild](states/OH-APP-HANDOFF-2026-09-08.md#production-execution-handoff--accepted-not-complete) is app-owned under run `production-oh-childcare-20260908-01`, using retained Ohio/MA/NJ/TN plus the 25-source production cohort. Acceptance is not completed promotion; do not edit its pinned files or repeat downloads. Earlier Ohio entries below are historical implementation milestones, including failed operation `0a4175ed-81ed-4ed9-a846-44a972c8ba9c`, not current running-job claims.

Parallel next-state work: [Wisconsin's refreshed source-use evidence](states/WI-CHILDCARE-USE-REVIEW-2026-09-08.json) confirms unchanged notices and a 2,382 licensed-group count at 09:50Z with no facility requests. [Bounded Wisconsin transport](WI-CHILDCARE-TRANSPORT.md) now passes synthetic source-to-release tests with no default network or native enrollment. Operator source-use decision, current-notice binding, durable acquisition retention and app enrollment remain pending. Alaska's declaration migration is deferred while that connector configuration is pinned by the active production rebuild; existing retained Alaska evidence is already part of production and does not need a duplicate download.

Ohio acquisition handed to the app: [accepted managed operation `0a4175ed-81ed-4ed9-a846-44a972c8ba9c`](states/OH-APP-HANDOFF-2026-09-08.md) persisted a RUNNING receipt at 07:48:42Z September 8. The app owns the single Ohio childcare task, including its current source-use gates; no agent is supervising routine progress. Acceptance is not completed acquisition or national integration. Continue other source/state work, reuse the completed result when available, and investigate only concrete failures from retained evidence.

Ohio native app enrollment implemented: [fixed standalone app jobs](OH-CHILDCARE-APP.md) persist identity before requests and verify the acquired/normalized dependency pair before atomic success publication. Childcare/OH now plans one state-publisher task; historical source-use and offline policy bytes remain unchanged. Native wrapper provenance is separate from injected-engine evidence, and later processing can reuse a completed acquisition after failure. All 925 repository tests, source/connector checks, lint, builds and desktop smoke pass; dependency audit is clean. The state ledger keeps Ohio unmeasured, not artificially covered. Next dispatch one needed managed Ohio job and retain its accepted operation receipt without agent download supervision. National integration remains separate.

Ohio durable acquisition retention implemented: [before-row checkpoints, validated page journaling, acquired-bundle replay and standalone reuse](OH-CHILDCARE-ACQUIRED-RELEASE.md) preserve ordinary-failure evidence and let normalization consume a verified retained snapshot without another request. Independent review tightened aggregate read limits and exact retained-byte checkpoint binding. Synthetic execution is explicitly distinguished from native acquisition; old offline releases and production pointers are unchanged. Next finish the fixed native runtime enrollment and app operation handoff, then dispatch without occupying a download-supervision agent.

Ohio source-use-gated transport implemented: [fresh notice collection and awaited prerequisite retention](OH-CHILDCARE-TRANSPORT.md#required-source-use-execution-mode) bind the same preflight before IDs/rows, recheck freshness before attempts and compare notices again at completion. Configuration drift fails before requests; a peer-found completion-clock reversal is covered by regression. The internal persistence hook is not a durable operation by itself. Next persist/replay the source-use packages in the acquired-release and app operation lifecycle, enroll the native path and dispatch. No live facility acquisition or schedule was enabled.

Ohio conditional internal source-use decision recorded: [fresh notice/preflight binding](OH-CHILDCARE-SOURCE-USE.md) pins complete unchanged public item notices and four unresolved availability responses, with a 15-minute prerequisite age ceiling. New internal-use policy is separate from unchanged development/offline profiles; legal approval, public export and app dispatch remain false. Actual refreshed preflight and binding verify 4,237 Open-center count evidence with zero facility rows. Next implement app-owned current-notice checks and operation/acquired-release lifecycle, then enroll and dispatch without agent download polling.

Ohio bounded transport implemented and tested with synthetic source-to-release evidence: [request pacing, deadlines, cancellation and cumulative consumed-byte limits](OH-CHILDCARE-TRANSPORT.md) cover metadata, inventories and pages. Error pages are cancelled unread while preserving provider Retry-After; native collection remains explicitly un-enrolled under the unchanged development policy. Next bind current notice/use evidence and implement app-owned lifecycle receipts before dispatch. No live source requests, policy approval, production pointer change or schedule activation occurred.

Ohio offline normalization and immutable release implemented: [standalone build/verify](OH-CHILDCARE-RELEASE.md) conserve every selected source row in accepted/quarantine artifacts, split ZIP5/ZIP4, retain nullable points and source county text, and preserve observation time independently of processing time. The four-artifact release replays source and transformed bytes with pinned development policy, bounded reads, ownership checks and cancellation-safe publication. Reprocessing preserves prior releases and stable source snapshot identity without downloading again. Live transport, reviewed current acquisition use and app-job enrollment remain next; no production pointer or schedule changes occurred.

Ohio offline acquisition/replay contract implemented: [bounded ID/page reconciliation and WGS84 point evidence](OH-CHILDCARE-PREFLIGHT.md#remaining-acquisition-work) enforce selected-field privacy, exact page membership, paired metadata/count inventories, chronology and byte ceilings. Seven synthetic tests retain missing ZIP/point evidence and native program-number anomalies without fabricating identity or authorization. Next implement bounded transport, normalization and immutable-release verification, then enroll an app-owned operation. No live Ohio facility records, schedules or production pointers were changed.

Ohio notice evidence and development scope bound: [current item notice matches retained content; XML and linked department notices are explicitly unavailable](states/OH-CHILDCARE-NOTICES-2026-09-08.md). A development-only policy fixes the ten-field Open-center cohort, internal/raw versus local-review output and point/ZIP boundaries. It grants no live acquisition, agreement acceptance or redistribution. Missing XML is not a permanent automatic veto. Next build bounded deterministic acquisition/replay offline, then bind reviewed current notice evidence and hand a validated job to the app. No Ohio facility records or production changes occurred.

Fresh Tennessee export and standalone production selection implemented: [explicit fresh/recovered handoff](TN-CHILDCARE-CONNECTOR.md#fresh-export-and-standalone-production-selection) preserves local-review export and split postal fields, pins verified ordinary 1.1.0 input, and rejects duplicate/mixed selections and drift. Offline eight-stage controller receipt evidence is distinct from live production. The full fresh compatibility chain is implemented; next evaluate a needed source refresh or proceed with another state's acquisition prerequisites. Reuse retained releases, hand downloads to the app, and require exact post-publication reassessment. No live download, production run or schedule was launched.

Fresh Tennessee coverage/map compatibility implemented: [coverage 2.10.0 paired with registry 2.14.0](TN-CHILDCARE-CONNECTOR.md#fresh-coverage-and-map-compatibility) preserves recovered 2.13/2.9 and conserves source/state/point-county/missing-ZIP totals. The map supports the exact fresh pair for category shares and bounded names, without inferred postal geography or matching eligibility. Next complete flat export and production CLI/planner enrollment before an app-owned refresh/promotion handoff; no source download, schedule or production pointer change occurred.

Fresh Tennessee resolution compatibility implemented: [exact registry 2.14/schema 1.1 acceptance](TN-CHILDCARE-CONNECTOR.md#fresh-resolution-compatibility) retains strict recovered 2.13 behavior, disjoint matching identities and unavailable-ZIP conservation. Offline source-to-registry-to-resolution-to-benchmark integration verifies mixed/all-unavailable ZIP cases without new matching profiles. Benchmark labels and accuracy remain unproven. Next migrate coverage, map, export and fresh production selection; no provider download, schedule or production pointer change occurred.

Fresh Tennessee registry stage implemented: [explicit internal fresh selection and registry 2.14.0 verification](TN-CHILDCARE-CONNECTOR.md#fresh-registry-enrollment-preparation) reject simultaneous recovered/fresh inputs and conserve reporting-only evidence even when every source ZIP is unavailable. Tests compare all matching-profile hashes against the no-Tennessee baseline and replay tamper checks for both versions. Production CLI/planner enrollment remains closed pending exact-version resolution, coverage, map and export migration. No source acquisition, production pointer change or schedule activation occurred.

Fresh Tennessee geographic reporting prepared: [separate schema 1.1 creation/validation and verified reporting input](TN-CHILDCARE-CONNECTOR.md#fresh-release-geographic-reporting-preparation) conserve source ZIP/point gaps and compare their complete summary with verified release quality. Recovered exports remain strict 1.0; all 1,863 retained recovered geographic rows match the previous implementation byte-for-byte. Next migrate explicit registry/dependency selection and downstream consumers for one fresh-or-recovered source, preserving history and rejecting duplicate source enrollment. No county assignment, source download, automatic promotion, schedule or production rebuild occurred.

Fresh Tennessee candidate input prepared: [the separate verified loader and versioned pure adapter](TN-CHILDCARE-CONNECTOR.md#verified-fresh-release-candidate-input) preserve ZIP/point gaps and source lineage without fabricated recovery metadata, matching eligibility or publication. The full recovered 1,863-record cohort produces byte-identical candidate output under the refactored shared projection. Next migrate geographic/reporting evidence and explicit registry/dependency selection for one fresh-or-recovered source release; production consumers deliberately remain recovery-only until that chain is verified. No acquisition, schedule or production rebuild was launched.

Tennessee fresh missing-ZIP handling implemented: [connector 1.1.0 ordinary release processing](TN-CHILDCARE-CONNECTOR.md#fresh-acquisition-missing-zip-repair) now uses the verified missing-ZIP normalizer, retaining explicit ZIP/point gaps and replay-derived accepted-record quality. Legacy 1.0 releases remain byte-verifiable, with a fixture generated by the actual previous implementation; recovered production releases remain unchanged. App-worker tests exercise missing-ZIP publication without AI or live network. No new provider download, schedule or automatic national promotion is claimed. Next support separately verified fresh-release reporting transitions and finish Ohio's policy/acquisition prerequisites while reusing current retained data.

Tennessee retained-data production completed and reviewed: [the terminal eight-stage receipt and exact MA/NJ-to-Tennessee transition](TN-CHILDCARE-CONNECTOR.md#completed-retained-data-production-and-exact-coverage-reassessment) preserve 1,863 reporting rows, all 172 unavailable ZIPs and three missing points without repulling sources. Matching profiles remain 8,011,835; reporting-only evidence rises to 8,945. Readiness 1.2.0 and the state ledger recognize only scoped childcare evidence; recursive pinned reassessment preserves original historical eligibility, broad-registry holds and authorization boundaries. Seven configured industry/state cells now have direct publisher evidence, not seven completely covered industries. Next repair fresh Tennessee acquisition's missing-ZIP path before refresh scheduling, and continue Ohio's acquisition prerequisites; nationwide completeness and independent identity validation remain unproven.

Ohio executable prerequisite: [metadata/status preflight and immutable receipt publication](OH-CHILDCARE-PREFLIGHT.md) now enforce fixed source identity, full field/CRS schema, ten paired observations and aggregate/independent count conservation. The live receipt retained 4,237 publisher-open source rows as count evidence only; zero facility rows or IDs were requested. Negative tests cover status/source drift, nested metadata, malformed/stalled responses, retries, cancellation, claims and storage confinement. Complete linked-notice/XML availability review, source-use policy, acquisition/release implementation and app enrollment remain required. The repository check discovered that the app-owned Tennessee production run has now succeeded, so the next production task is reviewing its exact new coverage transition rather than bypassing the older reassessment guard or repulling sources.

Ohio source-status scope corrected: [the follow-up specification and exact aggregate evidence](states/OH-CHILDCARE-STATUS-2026-09-08.md) distinguish 4,237 publisher-open center rows from 102 inactive and five enforcement rows. The proposed initial filter selects publisher-open centers only, without claiming independently verified operation; excluded statuses remain gaps. Ten selected attributes plus publisher-reprojected WGS84 points replace unverified coordinate attributes. XML unavailability and the separate email/code-gated CSV route are explicit. This is research/specification progress, not facility acquisition or coverage growth. Next implement the bounded preflight and validated app-owned connector; original assessments and production jobs remain unchanged.

Wisconsin offline release lifecycle: [standalone immutable bundle assembly and verification](WI-CHILDCARE-RELEASE.md) now retain five exact artifacts with policy/gap/chronology evidence and independently replay them before publication. Review-only/all-quarantine outputs remain distinct from approved business coverage. Canonical storage, exclusive ownership, final verification, cancellation cleanup and nested-output rejection are tested; no publisher records were downloaded. Source-use decision and bounded live acquisition/app enrollment remain pending. Default pointer is isolated under the Wisconsin source's `offline` folder.

Wisconsin normalized-record contract: [pure feature and acquisition normalization](WI-CHILDCARE-NORMALIZATION.md) now separates ZIP5/ZIP4, preserves typed source identifiers and page observation times, records processing/policy/field lineage, and conserves source rows across accepted/quarantine outputs. Unusable ZIP/point values remain explicit gaps on otherwise usable premises. This is offline synthetic verification, not a live release or acquisition approval. Source-use decision, bounded acquisition, immutable release verification and app enrollment remain next; existing business data is untouched.

Wisconsin selected-record contract: [offline acquisition replay](WI-CHILDCARE-ACQUISITION.md) now validates exact ID batches, selected fields, metadata/count drift and WGS84 point quality while preserving source values and gaps. A development policy binds the retained notice/XML hashes and keeps acquisition disabled; the operator's source-use decision has been requested, not assumed. No network downloader, normalized release or app enrollment is claimed. Next complete the source-use decision and bounded acquisition/normalization/immutable release lifecycle before handing actual collection to Co*Tive.

Wisconsin full notice retention: preflight v1.1.0 now retains the linked disclaimer's complete markdown content, layer iteminfo notice and exact XML bytes, with paired drift checks and unchanged v1.0 receipt replay. The live 18-request receipt observed 2,382 selected source rows and no facility records. A parallel coordinate review specifies publisher-returned EPSG:4326 points rather than unverified coordinate attributes. Next bind a reviewed local-use policy and implement selected-ID acquisition/normalization/release verification before app handoff. See [evidence and remaining gates](WI-CHILDCARE-PREFLIGHT.md). No production pointers, downloads or schedules changed.

Wisconsin prerequisite implementation: [standalone contract preflight](WI-CHILDCARE-PREFLIGHT.md) now checks distinct service/layer item identities, schema, native CRS, selected counts and paired metadata consistency with bounded transport and immutable receipt publication. A live run observed 2,382 licensed-group source rows without requesting facilities. Complete linked notices/XML, coordinate handling and verified acquisition/app enrollment remain required; no new coverage or acquisition approval is claimed. Tennessee retained-data processing and its pinned implementation remain untouched.

## Objective

Wisconsin evidence correction: the [new metadata/count recheck](states/WI-CHILDCARE-RECHECK-2026-09-07.md) supplements, rather than replaces, its original source assessment. The original one-row probe, separate public portal item and disclaimer findings are preserved unchanged. Wisconsin is an existing candidate revalidated in parallel with Ohio research, not newly discovered coverage.

Parallel state-source validation (September 7 local / September 8 UTC): [Wisconsin licensed-group childcare](states/WI-CHILDCARE-ACCESS-2026-09-07.md) and [Ohio app-linked childcare centers](states/OH-CHILDCARE-ACCESS-2026-09-07.md) have public metadata/count routes, returning 2,382 and 4,344 scoped source rows respectively. No individual records were acquired, no workers enrolled and no coverage added. The Wisconsin/Ohio corporate-registry holds remain separate. Next implement fixed metadata/notice/schema/count preflights and offline fixtures, then validated app-owned acquisition. The refreshed [state inventory](STATE-ACCESS-COORDINATION.md#september-7-evidence) pins completed MA/NJ coverage and 459 configured cells, not unfinished Tennessee. Sixteen focused ledger tests passed; this evidence-only increment changes no application code and does not repeat or claim a new full-suite/build result. Tennessee controller 7416 and child 34300 were confirmed live at the initial inspection and left to the app.

ZIP-unavailable map browsing completed: [the right-hand name panel](BUSINESS-HEAT-MAPS.md#browsing-records-without-a-source-zip) switches between selected-ZIP and state-wide source-ZIP-unavailable records, preserves separate postal fields and distinguishes empty results from unavailable evidence. Current published percentage semantics remain visible; no whole-country completeness claim is introduced. All 793 test cases and check components passed across the full run and exclusive lifecycle reruns, with TypeScript, zero-vulnerability audit and read-only review. The standalone production job's 40 code pins were unchanged and the management preview restored. Next verify the app's eventual terminal output and exact Tennessee source/ZIP/county conservation before source reassessment; do not repull data or occupy agents with routine processing progress.

Tennessee retained-data processing handed to the standalone app at `2026-09-08T03:39:27.109Z`: run `production-tn-childcare-20260908-01`, controller 7416 and registry child 34300 were confirmed live with a durable RUNNING receipt. [The handoff](TN-CHILDCARE-CONNECTOR.md#standalone-retained-data-production-enrollment) retains all existing source releases and adds Tennessee without downloads. Seven later stages were pending; do not call this completed promotion. Leave processing to the app and preserve its pinned code. Next independent work is the missing-ZIP frontend control; after terminal publication, inspect exact source/ZIP/coordinate conservation before reassessment. Verification passed 787 tests and all check components, with exclusive lifecycle/desktop checks rerun after briefly stopping and then restoring the preview.

Tennessee app-owned production enrollment: [the planner and registry CLI](TN-CHILDCARE-CONNECTOR.md#standalone-retained-data-production-enrollment) now select the retained recovered release alongside existing MA/NJ inputs, with seven source artifacts, full static stage-import pins and unchanged historical recovery guards. The prepared eight-stage plan reuses local data; no acquisition is needed. Source records with unavailable ZIPs remain explicit rather than discarded or assigned invented ZIPs. Whole-chain production verification, exact source reassessment and the separate missing-ZIP frontend control remain outstanding; plan creation is not publication evidence.

Tennessee map/export backend preparation (2026-09-07 local): [the map, state-name API and local-review export consumers](TN-CHILDCARE-CONNECTOR.md#map-and-export-backend-preparation) preserve the missing-ZIP cohort in state/national shares, use valid points for county additions and never infer ZIPs. The authenticated state-name endpoint exposes bounded ZIP-unavailable records with suite/status/recovery evidence; a dedicated frontend control remains pending. Flat-file exports conserve the full Tennessee source cohort and reject public release by default. All 783 tests/full checks, TypeScript and zero-vulnerability audit passed with independent review. No new download or production promotion occurred. Next add the frontend missing-ZIP browsing control and standalone production planner dependency pins, then run and verify the retained-data promotion before reviewing the exact new state/source transition.

Tennessee resolution and geographic coverage preparation (2026-09-07 local): [exact registry 2.13 resolution and conditional coverage 2.9 support](TN-CHILDCARE-CONNECTOR.md#resolution-and-geographic-coverage-preparation) conserve available/unavailable ZIPs, source/state/national totals and independently point-assigned counties. Identity matching excludes Tennessee source IDs and both entity endpoints. An internal retained registry declaration binds derived totals to dependency evidence; all-null-ZIP sources remain visible and no ZIP is inferred. All 780 tests/full checks, TypeScript and zero-vulnerability audit passed with independent review. No production promotion or download occurred. Next implement map percentages/non-ZIP browsing and policy-filtered exports, then app orchestration dependency pins and a verified retained-data production run with explicit source reassessment.

Tennessee registry enrollment preparation (2026-09-07 local): [conditional registry 2.13.0 support](TN-CHILDCARE-CONNECTOR.md#national-registry-enrollment-preparation) preserves all recovered source contributions, including ZIP-unavailable records in explicit canonical/reporting partitions, while builds without Tennessee remain 2.12.0. Actual retained input was reverified: 1,863 records, 1,691 source ZIPs and 172 missing ZIPs; no download or production rebuild occurred. All 778 tests/full checks, TypeScript and zero-vulnerability audit passed, with independent review. Next implement resolution compatibility, ZIP-unavailable coverage/map/export accounting, and app planner dependency pins before a production run; Tennessee remains absent from current national reporting. Do not infer ZIPs or let ZIP-only percentages omit the missing-postal cohort.

Release verification for the completed recovery reassessment and Michigan offline release preparation: all 776 tests passed with zero failures/skips, source discovery/assessment and connector checks, lint, web/desktop builds and desktop control-plane smoke passed. TypeScript passed and the production dependency audit reported zero vulnerabilities. Independent review covered immutable Michigan publication and the exact MA/NJ transition/historical queue eligibility boundary. Failed review-gate and stale-policy-expectation checks remain in ignored local logs; the final successful log is `data/tmp/mi-offline-release-full-check-4.log`. No recurring schedule was enabled. Roll back code only after stopping any affected app workers; preserve immutable releases and terminal receipts rather than deleting or repulling data.

Childcare app-owned recovery completed (2026-09-07 local): [the terminal six-stage receipt and published releases](CHILDCARE-REPORTING-INTEGRATION.md#completed-app-owned-recovery) confirm 7,082 MA/NJ reporting-only records are now in national coverage alongside unchanged 8,011,835 matching profiles. No source was repulled and the completed registry was reused. An explicitly pinned coverage reassessment preserves historical broad-registry holds while readiness policy 1.1.0 recognizes scoped MA/NJ childcare evidence. Benchmark independent labels and nationwide completeness remain unproven. Routine downloads and processing belong to standalone application workers after validation/enrollment, not live AI agents; schedules must be explicitly implemented and evidenced.

Michigan offline release preparation (2026-09-07 local): [immutable retained-evidence release builder and verifier](states/MI-CHILDCARE-ACCESS-2026-09-07.md#offline-immutable-source-releases) preserve selected rows, normalization/quarantine, observation evidence and restricted publisher XML with atomic publication and replay verification. The separate pending-review policy does not authorize acquisition, export, geographic assignment or national integration. No Michigan facility download or recurring worker enrollment occurred in this step.

Childcare resolution recovery dispatched (2026-09-07 local): [failure, fix and recovery evidence](CHILDCARE-REPORTING-INTEGRATION.md#resolution-compatibility-failure-and-retained-registry-recovery) confirms the original app run successfully built/verified registry 2.12, then failed a legacy resolution compatibility guard. Exact-version support now conserves matching plus reporting-only sites without admitting childcare to identity matching. A narrowly validated six-stage recovery reuses the verified registry and all original sources; controller PID 12644 and resolution worker PID 27088 were confirmed live at dispatch, with partition processing progressing. All 766 tests/full checks, TypeScript and zero-vulnerability audit passed after fixing a reproduced Windows receipt-sharing error. Final downstream publication and independent-label accuracy approval remain unproven. No source was downloaded again; do not replay the completed registry build or edit running pins.

Michigan bounded acquisition implemented (2026-09-07 local): [metadata and acquisition contracts](states/MI-CHILDCARE-ACCESS-2026-09-07.md#bounded-acquisition-contract) add exact-byte publisher XML evidence, explicit caller-review/preflight binding, scoped ID batches, before/after metadata/count/ID reconciliation, bounded transport and offline replay. XML contacts remain in an ignored local fixture; no facility records were downloaded and no app enrollment/publication occurred. Independent review and all 754 tests passed with zero skips using that fixture, plus full checks, TypeScript and a zero-vulnerability production dependency audit. Next implement immutable source release/verification and reviewed app enrollment before an approved first acquisition; do not confuse structural caller assertions with legal approval or test fixtures with production coverage. Existing national processing remains app-owned with all 29 pinned implementations unchanged.

Michigan normalization and metadata prerequisite review (2026-09-07 local): [pure transformation and evidence limits](states/MI-CHILDCARE-ACCESS-2026-09-07.md#normalization-and-complete-metadata-prerequisites) preserve source license strings, separate ZIP5/ZIP4, missing ZIP/point gaps and original observation lineage without downloading facility records. Attribute coordinates remain datum-unverified (`crs: null`) and ineligible for governed geographic assignment. Official XML is available but contains inherited identifiers/dates and must be retained with explicit discrepancies during future acquisition; its narrative cannot establish current operation or supersede current terms. All 743 tests, full checks, TypeScript and zero-vulnerability dependency audit passed with independent review. No app acquisition/enrollment, legal approval or national coverage increase is claimed; the running production implementation pins are unchanged.

Michigan metadata preflight implemented (2026-09-07 local): [standalone preflight and verified receipt](states/MI-CHILDCARE-ACCESS-2026-09-07.md#metadata-only-software-boundary) confirm fixed public item/layer identity, exact terms/schema/CRS and paired counts of 4,549 publisher-listed center rows. Zero facility records or IDs were downloaded. Full checks passed with 737 tests, TypeScript, independent review and a zero-vulnerability production dependency audit. Acquisition, legal approval, enrollment, scheduling and exports remain explicitly unauthorized by this preflight; complete publisher metadata, a bounded acquisition/normalization/release implementation and separate policy review remain necessary. The ongoing national production run retains all 29 code pins and is not restarted.

Tennessee geographic reporting preparation (2026-09-07 local): [verified projection and loader](TN-CHILDCARE-CONNECTOR.md#geographic-reporting-preparation) reuse the retained recovered release to produce 1,863 reporting rows offline, conserving 172 explicit missing-ZIP records and three missing coordinate pairs. Source contribution membership, recovery lineage and local-review restrictions are checked before projection; no identity matching or national integration is claimed. All 730 tests, full repository checks, TypeScript and a zero-vulnerability production dependency audit passed, with independent review. National integration remains a later change after the existing app-owned production run releases its pinned code.

Childcare production intermediate publication (2026-09-07 local): registry construction in `production-childcare-20260907-01` succeeded at `2026-09-08T01:03:45.396Z` and the app advanced to independent registry verification. [The checkpoint](CHILDCARE-REPORTING-INTEGRATION.md#standalone-production-handoff) binds the new registry pointer/manifest to the stage receipt; 7,082 MA/NJ reporting-only rows are declared, with matching profiles unchanged at 8,011,835. This is not whole-chain completion: resolution, benchmark and coverage stages were still pending at inspection, and all 29 code pins remained unchanged. Do not restart, repull or add Tennessee mid-run.

Tennessee registry conversion preparation (2026-09-07 local): [verified loader and pure adapter](TN-CHILDCARE-CONNECTOR.md#registry-conversion-contract) convert the retained recovered release without network requests into 1,863 contributions, 3,726 provisional site/establishment entities, 16,595 assertions and 1,863 relationships. Missing ZIPs remain in the data: 172 rows emit no ZIP assertion; three coordinate pairs remain missing. Recovery lineage, original observation time and local-review restrictions survive conversion; matching profiles remain empty. All 723 tests, lint, builds, desktop smoke, TypeScript and zero-vulnerability dependency audit passed, with independent review and actual-release conversion. National writer/verifier and geographic coverage integration remain pending; the running production rebuild retains all 29 pinned code dependencies. No new source acquisition or national publication occurred.

Tennessee offline source recovery completed (2026-09-07 local): [the independently verified seven-artifact release](TN-CHILDCARE-CONNECTOR.md#verified-recovered-source-release) contains all 1,863 acquired rows with zero quarantine, 172 explicit missing-ZIP reasons and three missing coordinate pairs. The app reused retained evidence with no source request, preserved the failed receipt/log and source bytes, kept original observation time and published a separate immutable recovered pointer. All 715 tests, lint, builds, desktop smoke, TypeScript and zero-vulnerability dependency audit passed. Existing production code pins remain unchanged. Next build Tennessee's verified registry loader/adapter and reporting integration from this retained release; do not repull or confuse source publication with nationwide coverage/completeness.

Tennessee retained-data recovery preparation (2026-09-07 local): the first app acquisition retained all 1,863 selected rows but failed publication because 27 empty and 145 zero-placeholder ZIPs exceeded the historical quality gate. [The offline recovery inspector](TN-CHILDCARE-CONNECTOR.md#first-acquisition-outcome-and-offline-recovery-boundary) now verifies exact failed-run/log/artifact provenance, complete acquisition replay and reproducible legacy failure without requests or writes. Explicit normalization 1.0.1 retains all 1,863 rows with 172 unavailable ZIPs labeled as gaps; no ZIP is inferred and three missing coordinate pairs remain missing. Legacy 1.0.0, the builder and five-percent gate are unchanged. All 706 tests and full repository checks passed, alongside TypeScript, zero-vulnerability production audit and independent review. Next build a separately versioned offline recovered-release publisher/verifier using these exact retained inputs; do not repull or claim a published Tennessee source yet. Existing production reconciliation remains live and advancing with all 29 code pins unchanged.

Tennessee standalone acquisition handoff (2026-09-07 local): the [immutable release builder and independent offline verifier](TN-CHILDCARE-CONNECTOR.md#standalone-release-and-app-worker-contract) are enrolled as the Tennessee-only childcare worker. All 698 repository tests, lint, web/desktop builds, desktop smoke, TypeScript and the zero-vulnerability production dependency audit passed. Independent review closed pre-commit mutation, prior-pointer integrity and partial pointer-write cleanup edges. App run `tn-app-acquisition-20260907-01` was dispatched independently of Codex; inspect its durable receipt before claiming completion. Enrollment does not enable recurring refreshes or national reporting coverage. The existing production rebuild remains live with all 29 implementation/script pins unchanged.

Tennessee acquisition/normalization implemented (2026-09-07 local): [bounded app-side modules](TN-CHILDCARE-CONNECTOR.md#acquisition-and-normalization-modules) enforce complete retained XML prerequisites, fixed center-only fields, exact ID batches, before/after source consistency, response/cumulative byte caps and pure offline replay. Normalization preserves missing provider IDs/coordinates, split postal fields, source status and restrictive policy without ownership/operating inference. All 684 repository tests, lint, builds, desktop smoke, TypeScript and zero-vulnerability dependency audit passed; independent review closed a response-stream cleanup fault. No live facility acquisition or source publication occurred. Next implement the immutable source-release builder/verifier and managed worker enrollment before dispatching a download. The existing production run's 29 implementation/script pins remain unchanged.

Tennessee prerequisite implemented (2026-09-07 local): [standalone metadata preflight](TN-CHILDCARE-CONNECTOR.md) now validates fixed identity/schema/center scope, unchanged metadata/counts and retained terms/XML before writing an immutable receipt. The first real execution retained ten observations and confirmed 1,863 selected source rows with zero facility requests, source publications or schedules. All 669 tests, lint, web/desktop builds, desktop smoke and TypeScript passed; production dependency audit reported zero vulnerabilities. The active childcare reporting rebuild remains live with all 29 implementation/script pins unchanged. Next build Tennessee's bounded selected-record acquisition, normalization and independently replayable immutable release; metadata readiness is not acquisition/export approval.

Parallel state childcare validation (2026-09-07): independent Michigan and Tennessee peers plus the Wisconsin integrator inspected official public sources while the existing app-owned rebuild continued unchanged. [Tennessee](states/TN-CHILDCARE-ACCESS-2026-09-07.md) has 1,863 records under an exact active-center filter and a concrete bounded preflight contract. [Wisconsin](states/WI-CHILDCARE-ACCESS-2026-09-07.md) has 2,382 licensed-group service rows, with source freshness/identifier stability still unverified. These are source-view counts, not acquired records or verified unique businesses. Implement Tennessee metadata/terms preflight next; keep family/home categories excluded and leave historical corporate-source holds intact. No new download job, schedule or production source was added by these assessments.

[Michigan](states/MI-CHILDCARE-ACCESS-2026-09-07.md) returned 4,549 center rows through count-only requests; no individual records were fetched. Missing source-status dates and material dataset terms remain explicit integration gates. Full repository check passed all 661 tests, lint, web/desktop builds and desktop smoke; TypeScript and the zero-vulnerability production audit passed. These checks protect the existing app, not the three unimplemented connectors.

Standalone childcare production handoff (2026-09-07): `production-childcare-20260907-01` is [dispatched to the app](CHILDCARE-REPORTING-INTEGRATION.md#standalone-production-handoff), with receipt-confirmed registry-build running at 22:55 UTC. It reuses the 25-source baseline and two verified retained childcare releases; no downloads or live AI supervision are required. Eight stages must complete before claiming final reporting publication; source-roster reassessment remains a post-run review.

Childcare reporting integration (2026-09-07, implementation verified): [the integration contract](CHILDCARE-REPORTING-INTEGRATION.md) separates geographic reporting from identity matching. The retained 3,007 MA and 4,075 NJ accepted records all passed local-only geographic-evidence conversion with network access disabled. Registry, coverage, app-owned optional-input orchestration, map-name drill-down and policy-filtered flat-file consumers passed all 661 tests, full repository checks, TypeScript and a zero-vulnerability production dependency audit. These source records are not yet included in completed production reporting; an explicitly pinned app-owned run must complete before claiming that change.

D.C. reporting refresh completed (2026-09-07): [terminal evidence](DC-CANDIDATE-COHORT-REVIEW-20260907.md#verified-production-reporting-completion) confirms all eight stages of `production-dc-refresh-20260907-01` succeeded at 22:30:39.435 UTC. Post-run checks matched eight logs, four output pointers and four output manifests, and confirmed process/lock release. Registry coverage matches the previously verified candidate. No source was downloaded again; independent-label accuracy approval and all-business completeness remain unresolved.

New Jersey registry preparation (2026-09-07): [verified conversion](NJ-CHILDCARE-CONNECTOR.md#verified-registry-conversion) reuses the reprocessed 4,075-row release without network requests. Source-scoped provisional site/establishment contributions preserve split postal fields, point evidence, observation/processing times and parent lineage; no ownership or matching is inferred. National writer/verifier and coverage integration are still required. The active production reconciliation retains its pinned 25-source cohort; no running inputs were changed for this additive preparation.

New Jersey offline recovery (2026-09-07): [versioned reprocessing](NJ-CHILDCARE-CONNECTOR.md#versioned-offline-reprocessing) recovered all 123 LF-session quarantines from retained evidence, producing a verified six-artifact release with 4,075 accepted records and zero quarantine. Explicit transformation 1.0.1 permits LF only in sessions; legacy 1.0.0 remains unchanged and verifiable. Raw evidence/source dates and the original pointer are preserved, with parent-manifest lineage and separate processing time. All 633 tests/checks passed. No repull, new source freshness, national coverage claim or reporting integration is implied; the ongoing D.C. reporting rebuild remains app-owned.

D.C. source promotion (2026-09-07): [committed cutover](DC-CANDIDATE-COHORT-REVIEW-20260907.md#source-promotion-after-completion) reverified 539 retained artifacts across 25 sources; only D.C.'s source selection changed. All production source-pointer hashes match the fresh promoted plan. Reporting rebuild plan `production-dc-refresh-20260907-01` is prepared; inspect its app receipt before claiming updated reporting. No repull occurred, and independent benchmark labels remain required for match-accuracy approval.

New Jersey first app acquisition (2026-09-07): [verified local release](NJ-CHILDCARE-CONNECTOR.md#verified-first-acquisition) retained all 4,075 source rows and passed independent five-artifact verification with 3,952 accepted and 123 quarantined records. All quarantine causes are control characters in the optional source `sessions` description. Preserve this release and investigate a versioned field-specific offline reprocessing path; do not repull the source or change old transformation semantics. App collection completed and released its publication lock; no national reporting promotion or completeness increase is claimed.

New Jersey standalone release and app enrollment (2026-09-07): [managed childcare handoff](NJ-CHILDCARE-CONNECTOR.md#managed-industry-handoff) adds immutable five-artifact source releases with exact publisher XML retention, offline acquisition/normalization replay, scoped cancellation and a separate NJ industry worker. Registry coverage remains unmeasured pending integration; public-school scope differs from MA, and no recurring refresh schedule is enabled. Windows file-ownership checks now use bigint identities in NJ releases, NJ preflight receipts and the matching MA release path. First live acquisition remains a separate app operation; do not repull existing national or MA data.

New Jersey acquisition/normalization implementation (2026-09-07): [bounded source stages](NJ-CHILDCARE-CONNECTOR.md#acquisition-handoff-contract) preserve raw publisher XML and complete JSON evidence in memory, verify ID/date/metadata consistency, enforce resource budgets and exclude private fields. Pure normalization preserves separate ZIP5/ZIP4, lat/lon-only business geocodes, source FOIPS/date semantics and local-review provenance. Fifteen focused tests and 610 full-suite tests passed. These modules are not yet an enrolled downloader: immutable release publication, independent verification and source-policy/industry contracts remain necessary before app-owned live collection. Production is unchanged.

New Jersey preflight implementation (2026-09-07): [standalone metadata checks](NJ-CHILDCARE-CONNECTOR.md) validate fixed NJDEP layer/item identity, selected schema, counts and source-date aggregates with bounded requests and immutable receipts. No business rows, industry enrollment or coverage increase is implied. Full XML metadata retention and the acquisition/normalization/release contract remain prerequisites to app-owned collection.

New Jersey childcare source handoff (2026-09-07): [bounded production-service validation](states/NJ-CHILDCARE-ACCESS-2026-09-07.md) confirms public NJDEP metadata/count access, 4,075 source rows and a successful one-row selected-field/WGS84 probe. Public-school inclusion differs from MA. Preserve publisher metadata/terms and explicit source-date semantics; build the connector preflight and offline contract before app enrollment or bulk acquisition. No NJ dataset or reporting coverage increase is claimed yet.

Massachusetts local integration preparation (2026-09-07): [verified registry conversion](MA-CHILDCARE-CONNECTOR.md#verified-local-registry-conversion) reuses the acquired immutable release, producing source-scoped provisional site/establishment assertions without downloads, ownership inference, automatic matching or reporting publication. The loader verifies membership and checksums before conversion. National writer/verifier, coordinate reporting and measured coverage integration remain explicit gates while the existing candidate reconciliation runs against pinned implementations.

Massachusetts first live acquisition (2026-09-07): [independent release verification](MA-CHILDCARE-CONNECTOR.md#verified-first-acquisition) confirms app run `ma-app-acquisition-20260907-02` succeeded with 3,007 accepted center-based childcare program records and 9 quarantined rows out of 3,016. The standalone worker finished and released its locks. Reuse this immutable local-review release for subsequent registry integration; no repull, national completeness claim, or production promotion is needed to preserve the newly obtained evidence.

Massachusetts app enrollment (2026-09-07): [managed childcare acquisition](MA-CHILDCARE-CONNECTOR.md#managed-industry-handoff) uses the existing standalone industry worker, source reservation, IPC cancellation and durable receipts. The new versioned source/policy contracts match the verified release builder. MA is acquisition-enabled; national reporting evidence remains unmeasured pending registry integration. No recurring cadence or production reporting promotion is implied.

Massachusetts release workflow (2026-09-07): [standalone build and verification](MA-CHILDCARE-CONNECTOR.md#standalone-release-commands) adds local artifact publication, source-bound normalization/quarantine reproduction and app IPC cancellation. This remains separate from managed industry enrollment and live acquisition; no new provider records or production reporting pointers are claimed by the implementation.

Massachusetts connector implementation (2026-09-07): [bounded acquisition and conservative normalization](MA-CHILDCARE-CONNECTOR.md) now have offline request, consistency, privacy, ZIP/coordinate/status and integration tests. They produce in-memory source/record results without AI, but immutable publication, independent release verification and managed enrollment are still required. No live childcare records or source pointers changed.

Massachusetts source-policy follow-up (2026-09-07): [indexed official scope/reuse evidence](states/MA-CHILDCARE-ACCESS-2026-09-07.md#scope-and-reuse-recheck--september-7-follow-up) corroborates center-based licensed-only locations and MassGIS public-data attribution guidance, despite direct-page 403 responses. Public item metadata was independently rechecked without row requests. Proceed to the bounded acquisition/normalization implementation; do not treat blank item license metadata as an indefinite development hold or metadata success as completed enrollment.

Alaska cancellation handoff (2026-09-07): [cooperative cancellation](AK-ACTIVE-BUSINESS-LICENSES.md#cooperative-cancellation-handoff--september-7-2026) now connects CLI IPC to local I/O, verification and the pre-publication boundary. Owned cancelled staging is distinguished from ordinary failures and resumed staging. Source-contract/catalog enrollment remains separate while reconciliation pins current configuration; no repull is needed for existing releases.

Alaska handoff preparation (2026-09-07): [network safeguards](AK-ACTIVE-BUSINESS-LICENSES.md) now bound CSV header/body execution, propagate stream/parser errors and cancel rejected bodies. Seven additional offline tests include successful fixture acquisition and failure-before-publication checks. Complete CLI/writer/verification/publication cancellation remains required before industry enrollment; no Alaska repull or production mutation occurred.

Minnesota food-source assessment (2026-09-07): [MDA location-contract findings](states/MN-FOOD-ACCESS-2026-09-07.md) identify a legacy/current license-classification mismatch and one-license/multiple-location semantics. Resolve selected-view scope and premises identity before building the app-owned connector. Only public guidance and form metadata were inspected; no rows, enrollment or coverage increase is claimed. The separate SOS hold remains unchanged.

Latest cohort review (2026-09-07): [D.C. candidate comparison](DC-CANDIDATE-COHORT-REVIEW-20260907.md) confirms only D.C. differs among 25 source manifests. Its candidate adds 120 activity rows and 18 accepted provisional sites, with quarantine still below the declared limit. App-owned isolated reconciliation `candidate-dc-refresh-20260907-01` completed all eight stages successfully at 19:30:15.929Z. A subsequent read-only review matched all eight log byte counts/checksums and the four build-manifest checksums against its receipt; this review did not rehash every data artifact. The controller's successful build/verify chain is candidate evidence, not production promotion or independent accuracy approval. Production remains unchanged; governed cohort promotion is still pending.

Latest candidate handoff (2026-09-07): the verified app-owned D.C. refresh was imported without repull into its isolated migration candidate root, retaining all 21 artifacts and source provenance. Import receipt `5e87206a-acbb-4f59-98d3-6cb793016e30` succeeded; production still selects its prior D.C. release. Adapter verification passed 523 tests. Next is cohort change review and governed reconciliation/promotion; 25/25 candidate schema readiness alone is not permission to claim a new reporting cohort or accuracy approval.

Latest app handoff (2026-09-07): D.C. connector contract 1.0.2 is enrolled in the cross-industry `local-business-licenses` bucket, with publisher-jurisdiction scope and local-review-only record outputs. [Coverage reassessment](STATE-COVERAGE-REASSESSMENT.md) resolves the post-rebuild historical-catalog gate using pinned old/new state evidence and current rank calculations without renewing source-policy decisions. Combined verification: 521 tests passed. The next D.C. operation is an isolated app-owned refresh; production import/promotion and recurring scheduling remain separate actions.

Build a standalone, local-first data hub that can acquire, validate, normalize, reconcile, catalog, and publish mixed public and licensed datasets with durable workflows and operator-visible provenance.

Latest source prerequisite (2026-09-07): [Massachusetts childcare preflight](MA-CHILDCARE-PREFLIGHT.md) is implemented as a standalone metadata/count-only CLI with immutable receipts. Final live observation: 3,016 source rows; no row acquisition, export approval or scheduler enrollment. Full check: 511 tests passed. Acquisition/normalization and governed industry enrollment remain next work for this source.

Production reconciliation follow-up (2026-09-07): original run `production-20260907-durable-01` retains its terminal FAILED receipt for the benchmark-status classification bug. [The corrected contract and narrow recovery](PRODUCTION-BENCHMARK-RECOVERY.md) passed 516 tests; `production-20260907-benchmark-recovery-01` subsequently SUCCEEDED in all three remaining stages. Current coverage is `national-business-coverage-views-20260907-174411739Z-4169d204`, verified across seven artifacts, without source repulls or replaying completed builds. State-access report `20260907174457-a6f317c8-a877-45a7-9af8-8969183abb61` now uses this release. Refresh the older assessment catalog explicitly; it still cites September 2 coverage. Independent-label approval, coordinate gaps and unmeasured/missing state-industry coverage remain outstanding.

The present application is a useful pre-alpha job runner:

```text
Electron shell
  -> React management interface
    -> loopback Node HTTP runner
      -> in-memory worker-thread queue
        -> browser, API, map, Places, pharmacy, download, parse, OCR, transform
          -> JSON control files and filesystem outputs
```

The next architecture remains one deployable application but enforces module boundaries:

```text
UI
  -> authenticated control plane
    -> durable orchestrator and scheduler
      -> supervised connector/operator execution
        -> raw source layer
          -> normalized assertions
            -> entity resolution and quality gates
              -> versioned datasets and policy-filtered exports

SQLite metadata: jobs, workflows, steps, attempts, leases, events,
source releases, artifacts, datasets, lineage, quality, and retention
```

Large artifacts stay on the filesystem; SQLite owns transactional metadata and coordination. PostgreSQL, object storage, remote workers, and multi-tenancy are deferred until measurements justify them.

## Release 0.2: foundation and recovery

This is the first implementation milestone. New nationwide connectors should wait until its P0 items are complete.

State-source discovery is tracked separately from implementation. The first ten-candidate official-source queue and its acquisition gates are recorded in [the 2026-09-03 state business-source discovery report](STATE-BUSINESS-SOURCE-DISCOVERY-2026-09-03.md). A parallel data-gathering-manager run evaluated [a second exact ten-state queue](STATE-BUSINESS-SOURCE-DISCOVERY-QUEUE-2-2026-09-03.md), recommending a bounded California rights-and-sample preflight with Kentucky as the contract-gated fallback. A third ten-state queue is recorded in [the 2026-09-03 Queue 3 report](STATE-BUSINESS-SOURCE-DISCOVERY-QUEUE-3-2026-09-03.md). Queue 4 completes the remaining 13 jurisdictions in [wave 1](STATE-BUSINESS-SOURCE-DISCOVERY-QUEUE-4-2026-09-03.md), [wave 2](STATE-BUSINESS-SOURCE-DISCOVERY-QUEUE-4-WAVE-2-2026-09-03.md), and [wave 3](STATE-BUSINESS-SOURCE-DISCOVERY-QUEUE-4-WAVE-3-2026-09-03.md). Eleven candidates remain `HOLD`; Alaska and D.C. have official machine-readable sources approved only for bounded connector/preflight and offline-fixture implementation. Full acquisition, live-source-derived or production source publication, registry/coverage/Heatmap admission, and pointer changes remain unauthorized. All three waves have machine-checkable decision artifacts. The first resulting implementation is the [offline Illinois Business Registry connector](IL-BUSINESS-REGISTRY.md): it accepts five operator-supplied official daily files, performs zero network requests, and publishes only local-review artifacts. Registry publisher 2.11.0 adds its optional organization-only reconciliation adapter, but no Illinois source release or production dependency has been activated.

Queue 4 now has two bounded implementations: [Alaska Corporations](AK-CORPORATIONS.md) and [D.C. Corporate Registration](DC-CORPORATE-REGISTRATION.md). Alaska permits only a timed HEAD plus capped prefix/schema preflight; D.C. permits only exact metadata and aggregate/count forms. Their acknowledged offline fixture paths are Datahub-confined, privacy-selected, decoded/record bounded where compression applies, independently reproduced and checksum-verified, and promoted only to non-overwriting local-review releases with no pointer. Neither connector downloads a complete live source, creates sites or business geometry/geocodes, contributes completeness, or enters Heatmap Builder.

A [five-state source-contract revalidation](STATE-BUSINESS-SOURCE-REVALIDATION-2026-09-03.md) rechecked California, Georgia, Oklahoma, Nebraska, and Vermont through parallel non-overlapping state assignments. All remain `HOLD`; no row-bearing preflight or offline-fixture connector is justified from current public contracts. A versioned machine artifact now preserves the exact evidence, authorization boundaries, coverage-release pin, unresolved gates, privacy exclusions, and strongest written next action. The management States view displays these current source gates beside production coverage while explicitly flagging a future stale coverage pin.

The [Overture U.S. Places connector](OVERTURE-US-PLACES.md) adds a separately governed nationwide place-evidence path. Its metadata-only preflight is implemented and verified; the large U.S. source extraction remains blocked pending explicit operator authorization. Business/place records store only address-associated latitude and longitude. U.S., state, county, and ZIP/ZCTA polygons remain confined to the governed geography layers, and ZIP5/ZIP+4 remain separate.

The [Texas Active Franchise Taxpayers connector](TX-ACTIVE-FRANCHISE-TAXPAYERS.md) enforces the previously recorded [source preflight contract](TX-ACTIVE-FRANCHISE-TAXPAYERS-PREFLIGHT.md). Its live bounded preflight validated all 18 catalog fields, observed `3,435,798` rows and source refresh `2026-08-29T08:25:23Z`, and wrote an immutable receipt proving that it issued zero row requests and acquired no taxpayer records. A separate exact-acknowledgement offline workflow can validate an operator-supplied file into immutable local-review-only staging, with high-risk address withholding, exact taxpayer-number reconciliation only, separate ZIP5/`postal_code` and ZIP+4, and no sites, geocodes, production pointer, registry, coverage, or Heatmap contribution. Automated bulk acquisition and production admission remain unimplemented and default-denied.

The [USDA Organic INTEGRITY connector](USDA-ORGANIC-INTEGRITY.md) establishes a governed offline conformance scaffold without claiming compatibility with the uninspected monthly workbook. Its only permitted network action is one 64 KiB streaming-capped Data History request; it makes zero workbook requests because the publisher ignores byte ranges. The current live Data History response is a Blazor shell whose monthly links are not present in server HTML, so live discovery fails closed. Workbook acquisition, official sheet/header validation, production publication, registry and coverage contribution, and Heatmap admission remain unimplemented. Explicitly acknowledged offline records remain local-review-only, preserve physical-versus-mailing evidence and separate ZIP5/ZIP+4, exclude direct contacts and free text, and create no sites, establishments, geometry, or geocodes.

### DH-001 — Run-scoped ownership and overlap control

Owner: orchestration platform

- Add immutable `runId` to every checkpoint, staging path, output set, and lock owner.
- Reject or serialize overlapping runs for connectors that cannot safely overlap.
- Publish each output set atomically through a run directory with its manifest written last.
- Prove 20 concurrent submissions of one fixture job cannot overwrite another run.

### DH-002 — Cooperative cancellation and startup recovery

Owner: orchestration platform with verification

- Send a cooperative abort request and allow bounded cleanup before worker termination.
- Replace fixed stale locks with renewable leases that include owner identity and liveness.
- Reconcile queued/running records, abandoned leases, staging directories, `.part` files, and child processes at startup.
- Add fault-injection tests that cancel downloads and kill the runner during each publish phase.
- A cancelled job must be immediately restartable and leave no live process or blocking lock.

Current runtime evidence: a cancelled NPPES attempt left a dead-PID lock, a staging directory, and a 226,430,590-byte partial archive under `data/pharmacy-sources`. This roadmap does not delete those user runtime artifacts.

### DH-003 — Authenticate the local control plane

Owner: security and governance

- Generate a random per-launch control token and expose it to the sandboxed Electron renderer through a minimal preload bridge.
- Require authentication on every endpoint except narrowly defined liveness.
- Allow only the exact desktop and configured development origins; validate `Host`.
- Refuse non-loopback binding unless a separately designed authenticated remote mode is enabled.
- Add negative tests for every management and output endpoint.

Implementation evidence: the loopback runner now creates or accepts one 256-bit per-launch control token, reduces unauthenticated health to `{ "ok": true }`, and requires timing-safe bearer authentication before every management, data, review, mutation, and output route. Exact Host and configured-origin checks run before routing; non-loopback listener configuration is rejected. The Electron main process passes the token only to its runner child and an exact-document, main-frame, sandboxed preload IPC method. It refuses an occupied runner port without sending HTTP data, waits for its own child to announce readiness over IPC, and loads the renderer only after authenticated proof. The coordinated web-development launcher supplies one fresh token and exact origins to both direct child processes without printing or persisting it, and awaits both children during coordinated shutdown. Authenticated fetch-based downloads replace direct unauthenticated links. Negative integration tests enumerate every endpoint family and cover missing/wrong tokens, forged Hosts, disallowed origins, preflight behavior, token redaction, authenticated reads and mutations, minimal liveness, hostile pre-bound ports, foreign local-file navigation, normal two-service shutdown, and one-child failure cleanup.

### DH-004 — Versioned connector registry and validation

Owner: platform architecture and connector engineering

- Define the connector manifest required by `AGENTS.md`.
- Register connectors through one registry consumed by API, scheduler, worker, and UI.
- Validate connector-specific configuration before a run is queued.
- Generate typed UI configuration from the same schema where practical.
- Port one low-risk connector first, then Google Places and pharmacy without changing their observable outputs.

Foundation implementation evidence: registry contract `1.0.0` now fail-closes runner startup unless all 41 existing connector manifests and their 39 exact source-policy profiles validate. The shared loader enforces connector identity/version, canonical lifecycle order, closed configuration schemas and defaults, named secret references without values, artifact types, allowed hosts, execution limits, recovery semantics, retention, and produced entities/identifiers. Authenticated catalog/detail/configuration-validation endpoints and a searchable operator catalog consume the same sanitized registry, and `npm run connectors:check` runs before the full verification suite. Existing job execution adapters and schema-generated editors still need to be migrated before DH-004 is complete; this foundation does not change dataset or production pointers. See [the connector registry contract](CONNECTOR-REGISTRY.md).

### DH-005 — Durable SQLite control metadata

Owner: orchestration platform

- Introduce versioned migrations and transactional tables for jobs, job versions, runs, steps, attempts, leases, events, artifacts, and settings.
- Import existing JSON state once and retain a rollback backup.
- Rehydrate resumable queued work and mark non-resumable interrupted work deterministically.
- Remove the implicit 500-run history cap; make retention explicit.
- Test migration, corruption detection, backup, restore, and process-kill recovery.

### DH-006 — Artifact catalog and provenance envelope

Owner: data engineering

- Assign every raw and published artifact an immutable ID, schema version, SHA-256, byte count, media type, source release, connector version, configuration hash, and retention policy.
- Make raw source releases immutable and publish replacements only after validation.
- Record input/output lineage for each workflow step.
- Update NPPES and pharmacy manifests to reference complete source-release identity rather than only a local path.

## Release 0.3: governed workflows

### DH-007 — Explicit workflow prerequisites

- Represent workflows as durable steps with dependencies, retries, leases, idempotency keys, and checkpoints.
- Convert NPPES acquisition into a reusable `AcquireSource` step required by pharmacy normalization.
- Expose the step graph, current attempt, and recovery action in the interface.

### DH-008 — Secrets and network/filesystem boundaries

- Replace stored secret values with named references backed by Windows credential storage.
- Redact credentials, cookies, signed URLs, and configured secret values everywhere.
- Validate redirects and resolved destinations; deny loopback, link-local, private, and metadata endpoints unless connector policy explicitly permits them.
- Use real-path validation against symlink, junction, UNC, and case-variant escapes.
- Pass child processes a minimal environment allowlist.

### DH-009 — Global resource, quota, and retry controls

- Add resource lanes for browser, network, CPU-heavy, and bulk-ingest work.
- Enforce provider-wide token buckets and request/spend budgets across all runs.
- Standardize capped exponential retry behavior and `Retry-After` handling.
- Add input, output, memory, disk, and execution-time limits per connector.

### DH-010 — Source policy, retention, and export enforcement

- Add versioned source-policy profiles describing license, allowed use, attribution, retention, redistributability, and restricted fields.
- Apply retention to raw files, normalized assertions, checkpoints, screenshots, logs, caches, and exports.
- Evaluate export permission at field level and produce auditable cleanup/export receipts.
- Keep Google, NCPDP, and other restricted assertions separated from public CMS or USDA records.

## Release 0.4: canonical data hub

### DH-011 — Canonical entity and assertion model

- Add organization, brand, physical site, establishment, service, external identifier, classification, relationship, and temporal assertion contracts.
- Preserve one-to-many relationships; never key pharmacy enrichment solely by NPI.
- Treat co-location separately from identity.
- Attach field-level provenance and policy to every assertion.

### DH-012 — Auditable entity resolution

- Resolve deterministic identifiers before scored name/address/phone/domain/geospatial evidence.
- Store the features, score, rule version, timestamp, and reversibility of every merge decision.
- Route ambiguous candidates to operator review.
- Establish a labeled benchmark and a release threshold for automatic-merge precision.

Implementation evidence: independently verified registry release `national-business-registry-20260902-043657439Z-c1eab4dd` emits 8,011,827 checksummed, source-preserving ZIP2 location profiles, including 24,230 conditional New York Agriculture and Markets retail-food-license premises, 84,485 California ABC Active issued-license premises grouped by File Number, 54,890 DC DLCP Active Basic Business License customer/site groups with 42,749 valid transformed MAR coordinates, 94,486 conditional Alaska DCCED active-license locations, 633,332 Los Angeles active-business locations, 885,097 Texas permit outlets linked to 700,705 taxpayer organizations, 42,864 Chicago active-license sites linked to 35,694 license-account organizations, and 31,163 NYC DCWP Active Premises-license sites and Business Unique ID organizations. All remain local-review-only. It separately preserves 72,783 Washington L&I active contractor-license organizations, 66,379 Delaware current-license organizations, and the other organization-only state layers without inferring sites. Independently verified resolution release `business-entity-resolution-20260902-075624674Z-881e778c` uses ruleset `business-entity-resolution@1.0.0` to publish 2,325,234 reversible exact-address site-alias decisions, 146,889 stricter exact-address-and-name establishment-alias decisions, and 106,011 unapplied scored review candidates. The verifier enforces dependency hashes, exact duplicate detection through a bounded partitioned identity index, decision uniqueness, scores, rule versions, counts, and local-review-only export policy.

Benchmark evidence: verified sample `business-entity-resolution-benchmark-sample-20260902-075956925Z-c5f3d239` deterministically selected 425 rows per stratum from 1,449,139 site-membership pairs, 74,733 establishment-membership pairs, and 106,011 review candidates. The documented proposed gate requires complete review, at least 384 conclusive labels per automatic stratum, no more than 10% exclusions, and a 95% Wilson lower bound of at least 0.99. Current submitted labels: zero; the gate is truthfully false.

Operator-review evidence: the local Co*Tive Collector page now serves verified packets with source comparisons, reviewer identity, controlled label values, required negative/exclusion notes, optimistic concurrency, an atomic working label copy, append-only proposed/committed audit events, download, filters, and live Wilson gate progress. The immutable benchmark sample itself is never edited. A separate checksummed label-snapshot publisher refuses an empty working set, recomputes rule and source-pair metrics, preserves corrections as later releases, and cannot authorize export.

Remaining gate: obtain independent labels, publish the first actual immutable label snapshot, evaluate the resulting source-pair-specific errors, formally approve the threshold, and authorize any record-level export under every contributing source policy.

### DH-013 — Validation, quarantine, and quality gates

- Produce counts for source, accepted, quarantined, duplicate, and rejected records plus null, uniqueness, and drift metrics.
- Quarantine invalid records with reasons instead of silently skipping them.
- Block publication on unexpected schema or material count drift while preserving the last known-good dataset.
- Show freshness, validation, licensing, and publishability separately in the UI.

### DH-014 — Dataset catalog and live observability

- Replace polling with a durable event stream such as SSE.
- Add workflow graph, structured logs, request/cost metrics, artifact lineage, source freshness, and quality views.
- Split the single page component into typed feature modules.
- Let an operator trace a published field back to assertion, source record, release hash, ingest run, and policy.

## First governed data program: national grocery registry

Start only after Release 0.2 foundations are complete.

1. Ingest USDA current and historical SNAP retailers while retaining their stated coverage limits.
2. Add state retail-food and business-license open datasets through policy-scoped connectors.
3. Add authorized chain store feeds or locators.
4. Add a licensed national POI source if required completeness justifies its cost and contract.
5. Use Google Places counts and durable Place IDs for gap detection and verification, not as unrestricted permanent content.
6. Use authoritative ZIP/ZCTA geography and adaptive spatial subdivision; do not iterate every number from `00100` through `99999` as though each were a physical ZIP area.
7. Report coverage, freshness, source contribution, conflicts, and unresolved gaps by state, county, and ZCTA.

Coverage-view evidence: verified publisher 2.7.0 release `national-business-coverage-views-20260902-115337634Z-ba689784` pins the registry, geography, ZCTA jurisdiction overlay, resolution, benchmark, and Census Nonemployer manifests. It publishes three national scopes, 56 state views, 3,235 county views, 48,217 ZIP views, 26 source views, and 28,127 first-class gap rows. New York retail food contributes 24,281 license organizations, 24,230 conditional site profiles, 24,280 ZIP-evidence addresses, and 22,999 usable platform geocodes; 22,948 site-profile coordinates receive one county assignment and retain their address-component-centroid limitation. Washington L&I contributes 72,783 contractor-license organizations and 74,005 ZIP-eligible mailing-address records without inferring physical sites. California ABC contributes 84,485 grouped premises profiles from 105,417 license activities and no coordinates; DC contributes 54,890 grouped customer/site profiles, including 42,749 valid source coordinates; Alaska contributes 94,818 active-license organizations, 94,486 conditional site profiles, 94,504 reported U.S. address/ZIP records, and no coordinates; Delaware contributes 66,379 current-license organizations and 66,215 eligible reported U.S. address records without inferring physical sites. NYC DCWP contributes 31,163 grouped license-site profiles and Business Unique ID organizations; Chicago contributes 42,864 grouped license-site profiles and 35,694 account organizations; Texas contributes 885,097 provisional permit-outlet profiles and 700,705 taxpayer organizations; Los Angeles retains 633,332 profiles and 566,943 source geocodes. All 8,011,827 location profiles were assessed; 995,268 coordinate-bearing profiles received one county assignment, while missing, unmatched, ambiguous, and conflicting geography remains explicit. The complete selected Census set of 33,791 ZCTA5 polygons is now the explicit spatial ZIP denominator; its manifest, ZCTA-index, and member-set hashes are pinned and exact member/gap sets are verified. Another 14,426 source-reported five-digit postal values remain visible outside it, ZIP+4 is non-geometric, optional USPS routing evidence is not a spatial gate, and polygon-area weights are never used as business-location weights. Registry 2.9.0 remains the pre-migration production dependency; the corrected isolated 2.11.0 chain verifies successfully, but no production pointer has been promoted. The production release remains partial, source-preserving, non-deduplicated, and local-aggregate-review-required.

Heatmap consumer evidence: Heatmap Builder exposes the coverage release's direct Census Nonemployer state and county aggregates as a separate enhancer and right-side entity statistic. It preserves reference year, receipts, source release, and publication status. ZIP/ZCTA responses are always null with `unavailable-not-published-at-zip-do-not-allocate`; these annual aggregates are never mixed into named-business evidence, employer-establishment counts, relative completeness alignment, or current-operation claims. Direct Census population/housing zeroes remain numeric, while missing values, jurisdictions without uniquely assigned ZCTAs, and incomplete ZCTA aggregates remain null with an explicit status. ZIP Business Patterns employer baselines follow the same complete-input rule. State percentages are null when the denominator is unavailable or zero; numeric zero is reserved for a measured zero numerator over a positive denominator.

Postal-migration control evidence: a versioned readiness inventory audits all 25 current source pointers, freezes pointer/manifest/configuration hashes into a deterministic plan hash, exposes production and isolated-candidate counts in the management API, and prevents a registry build from starting while any source is stale. All 25 independently verified isolated candidates are ready. Florida was rebuilt without a credential or network request from its retained selected-source snapshot only after pointer, manifest, path, artifact, layout, acquisition, and source-release verification. A separate cutover controller verifies every manifest artifact, copies immutable releases before changing pointers, uses an exclusive lock and exact-hash compare-and-swap, journals every transition, automatically restores promoted pointers on ordinary failure, recovers dead-owner interrupted runs, and supports guarded pointer-only rollback. The management API exposes safe lock and latest-run state without its ownership token. Refreshed cutover plan `62aae9436c0a91b8d930caa766f1b7b6603916612fa4bf82e432e61680ef4d79` reverified 539 artifacts totaling 18,769,192,318 bytes and binds all 25 live candidate and production pointers; it has not been executed, and no production pointer has been promoted. The older plan is retained as stale audit history and must not be used.

Isolated downstream proof: the first registry 2.10.0 proof remains immutable audit history but failed the physical split-postal-field contract. Corrected registry 2.11.0 release `national-business-registry-20260903-140103996Z-7a11fa50` independently verified 679 artifacts, 8,011,791 physical-site profiles, and 48,190 ZIP-union rows from the 25 corrected source candidates. Strict audit `zip-denominator-audit-91f2251924768ac8d4c94bc2` found zero missing or invalid `postal_code`/`zip4` fields and zero missing USPS-unverified reasons. Resolution release `business-entity-resolution-20260903-161820739Z-39052efa` independently verified 8,011,791 profiles, 2,325,188 site aliases, 146,891 establishment aliases, and 106,061 unapplied review candidates. Benchmark release `business-entity-resolution-benchmark-sample-20260903-162144534Z-1b676a6f` verified a deterministic 1,275-row sample; it has zero labels and its proposed statistical gate is correctly false. Coverage release `national-business-coverage-views-20260903-162245590Z-f3f086e7` independently verified 56 state, 3,235 county, 48,190 ZIP, 26 source, and 28,073 explicit-gap views after assessing all 8,011,791 profiles. These are candidate receipts only; production pointers remain unchanged.

Nonemployer-baseline evidence: verified Census release `census-nonemployer-2023-20260830-230249716Z-78268f89` preserves 1,153,323 official source rows and publishes 30,427,808 annual no-paid-employee establishments across the national and 51 state/D.C. totals plus 3,143 county totals. The coverage view carries published or explicit-null baselines on all state/county views and prohibits ZIP allocation. Its gap rows include five state equivalents, 92 county equivalents, the single national/county reconciliation difference, and the source-wide absence of ZIP-level Nonemployer data.

Regional-GDP evidence: independently verified BEA release `bea-regional-gdp-2024-20260903-115540751Z-212415ce` preserves 3,187 CAGDP1 source areas and publishes current-dollar GDP, real GDP in chained 2017 dollars, and the 2017=100 quantity index for all 51 state/D.C. totals and 3,091 exact county-equivalent matches. Its 53 current governed county gaps and 45 non-direct source areas remain first-class gaps; Virginia combination areas, Maui/Kalawao, historical areas, regional totals, and the national total are never duplicated or allocated. The governed [BEA CAGDP1 connector](BEA-REGIONAL-GDP.md) publishes no ZIP GDP. GDP is economic context only and is excluded from business-coverage completion and relative-alignment calculations.

Current ZIP evidence progress: the complete selected Census ZCTA5 release is the spatial polygon denominator. The `usps-operational-zip-assignments` connector remains available as optional supplemental routing evidence; it discovers and validates the current PostalPro Area/District and AISU files, excludes AISU-only routing rows, and can publish immutable local-restricted releases. A live USPS source remains gated on a truthful personal non-commercial use basis or reviewed USPS written permission, but that no longer blocks Census polygon coverage. Public redistribution is not inferred from download access.

ZIP-denominator audit evidence: read-only audit `zip-denominator-audit-d48230b5f34ec2e88b360a25` preserves the failure receipt for production publisher 2.9.0 and the flawed isolated 2.10.0 candidate. Production contains 48,217 ZIP5 rows, including 33,791 governed ZCTA members and 14,385 source-reported ZIP5 values outside that denominator; all 48,217 USPS statuses are unverified and 4,607 lack a reason under the legacy contract. The superseded isolated 2.10.0 release contains 48,190 ZIP5 rows but omits the required `postal_code` and `zip4` physical fields and 4,604 USPS-unverified reasons. Corrected read-only audit `zip-denominator-audit-91f2251924768ac8d4c94bc2` verifies publisher 2.11.0 manifest SHA-256 `d94e4a04cd25bec7aefded5d4440c263ceb65aa4ad4892ef81b0eaa8e1d368b8`, the same 33,791 governed ZCTA members, 14,358 source-reported ZIP5 values outside that denominator, and all 48,190 split postal rows with zero contract violations. It still correctly declines to claim a complete current USPS operational denominator because no governed authoritative USPS evidence is integrated. See [the ZIP denominator audit contract](ZIP-DENOMINATOR-AUDIT.md).

Temporal-governance evidence: the [business-source temporal audit](BUSINESS-SOURCE-TEMPORAL-STATUS.md) normalizes source reference date, record observation window, review threshold, and source-specific status scope without treating any legal/license/program status as general operating proof. The current 26-source coverage release has 26 configured policies and 26 usable reference dates; at the reproducible `2026-09-03T12:00:00.000Z` assessment, 25 are within their internal review windows and the New York retail-food license source is review-due. The audit and management view are read-only and do not alter releases or production pointers.

State-source readiness evidence: the [state readiness policy](BUSINESS-STATE-SOURCE-READINESS.md) classifies all 51 state/D.C. rows without confusing national sector presence with broad jurisdiction coverage. Eight jurisdictions have broad production organization layers; 43 do not. The current state scope carries 7,981,531 source-preserving location profiles, of which 994,523 (12.46%) have an assigned source coordinate. Every state row remains partial, business geometry remains prohibited, and the state/API views expose the exact scope classification and missing evidence.

State-source decision-catalog evidence: the versioned assessment catalog combines the five-state contract revalidation with every unique decision from Queue 4, [Queue 5](STATE-BUSINESS-SOURCE-DISCOVERY-QUEUE-5.md), [Queue 6](STATE-BUSINESS-SOURCE-DISCOVERY-QUEUE-6.md), and [Queue 7](STATE-BUSINESS-SOURCE-DISCOVERY-QUEUE-7.md). Queue 7 ran Maryland, Missouri, Indiana, and South Carolina in four concurrent, non-overlapping official-source streams. Twenty-nine state equivalents now have machine-validated management gates: 27 `HOLD` and two bounded-connector decisions for already-documented D.C. and Alaska sources. Zero autonomous acquisitions, payments, complete downloads, production-ready broad layers, or pointer changes are authorized. The catalog remains pinned to the current production coverage release, and its diagnostic ratios rank source-contract work only rather than claiming business completeness.

## Verification and release gates

### September 7 parallel industry collection increment

The national business goal remains active: every valid ZIP must retain authorized source evidence, reconciled identities, temporal status, provenance, and explicit coverage gaps. The following increment does not establish nationwide completeness.

- Source catalog: Queue 8 integrated and validated, bringing the historical decision catalog to 33 jurisdictions (31 holds and two existing bounded-connector decisions).
- Industry execution: standalone industry/state plan and run commands, capped at ten parallel source subprocesses, with national acquisition deduplication, explicit state-source gaps, prerequisite checks, isolated outputs, atomic receipts, and log hashes. See [industry segments](INDUSTRY-SEGMENTS.md).
- Data gathering: independently verified New York retail-food, California ABC, and Washington contractor refreshes; Washington exercised the new standalone command end to end. See [refresh evidence](INDUSTRY-REFRESH-2026-09-07.md). These sources remain isolated pending downstream integration.
- Heatmap Builder: national category share includes an explicit state-assigned evidence denominator and excluded ambiguous/unmatched evidence. True business-universe completeness remains unknown.
- Flat-file builder: selectable state/category/source/column CSV and JSONL exports with mandatory provenance, explicit local policy modes, output hashes, and independent verification. A live New York production-registry export verified 24,230 records in each format. See [flat-file export](BUSINESS-FLATFILE.md).
- Management controls: the local `/#data-operations` section now previews collection plans, selects publisher states, builds policy-scoped files with chosen fields, tracks source-task status and operation history, cancels work cooperatively, and downloads verified artifacts. Authenticated API tests execute a real fixture export and compare downloaded hashes. Terminal status waits for receipt persistence; restart ownership remains protected until source process evidence resolves it. See [managed operations](MANAGED-OPERATIONS.md).
- Refresh integration preparation: the [standalone candidate importer](INDUSTRY-CANDIDATE-IMPORT.md) copied and independently verified the three refreshed releases through their normal immutable publishers without another download. All 25 corrected sources now use candidate-scoped pointers, including the September 7 NY/CA/WA releases; all 29 production pointers remain unchanged. The increment passed 385 tests, build/typecheck, and a zero-vulnerability production dependency audit. This does not substitute for the live national dependency-chain verification or production cutover.
- Standalone source expansion: IRS EO BMF is now selectable in a sixth, explicitly cross-industry `tax-exempt-organizations` bucket (nine configured sources total). Validated source limitations persist in plan warnings and receipts and cannot be removed from an executable plan. Cooperative cancellation covers requests, local/network streams, CSV rows, gzip backpressure, writer closure, and the final pre-publication boundary; ordinary failures retain staging evidence. The increment passed 391 repository tests, build/typecheck, and a zero-vulnerability production dependency audit. Offline cancellation regressions publish no pointer; no live IRS download or production source update was performed for this increment.
- Candidate reconciliation automation: a [standalone controller](CANDIDATE-RECONCILIATION.md) now plans the complete 25-source candidate cohort and executes eight ordered build/verification stages into a new isolated run directory. It pins inputs, implementation files, and emitted dependencies; records process ownership and hashed logs; excludes concurrent controllers; and persists stage-boundary stop requests. Fourteen offline controller regressions cover ordering, lineage, drift, cancellation, ownership, and path boundaries. Planning against the actual candidate cohort passed without launching another national rebuild. This controller is not a scheduler, recovery engine, or production cutover mechanism; its full live national execution remains unproven.
- Food-processing readiness: the existing explicit-file FSIS workflow now supports cooperative IPC cancellation, event-loop yields, writer closure, and cancellation cleanup restricted to its own staging run. Seven offline FSIS tests include previous-publication and unrelated-staging preservation. The full repository check, TypeScript check, and zero-vulnerability production audit passed. No food-processing selector is exposed yet: its two official CSVs and explicit source date require a governed prepared-input profile before the generic industry runner can execute it. No source download or production update occurred.
- State/app work allocation: [flat state-access coordination](STATE-ACCESS-COORDINATION.md) now defines 50 state workstreams plus D.C., pins actual coverage evidence across six configured industry buckets, and distinguishes evidence from app-preflight readiness. Alabama, Georgia, and Texas peer inventories are complete. Agents validate and implement; Co*Tive owns routine acquisition after its own prerequisite checks. National downloads are shared, not repeated per state. The report does not create jobs or implement recurring scheduling. The increment passed 418 tests and build/typecheck/audit gates.
- Texas app-owned acquisition: a seventh, explicitly cross-industry `sales-tax-outlets` bucket exposes the existing Texas-only connector through managed industry operations (ten configured sources total). Cancellation now covers requests, retry waits, gzip backpressure/closure, normalization, and verification; cancelled builds clean only their owned staging, while ordinary failures and resumed staging remain recoverable. The final publication boundary completes without interruption. A read-only TX/GA plan produced one Texas task and an explicit Georgia gap. All 423 repository tests, lint, web/desktop builds, desktop smoke, TypeScript, and the production dependency audit passed. No duplicate Texas download, job submission, recurring schedule, or production pointer change was performed for this increment.
- Production execution handoff: the initial unlogged production process stopped before publishing; its incomplete 676-file staging tree remains intact. A separate [production controller](PRODUCTION-RECONCILIATION-CONTROLLER.md) now pins the production-only cohort and prior outputs, persists stage/child/log/publication evidence, blocks concurrent controllers, and accepts atomic durable stop requests for hidden Windows operation. Its fixed eight stages reuse local data and preserve exact direct plus inherited lineage. All 440 tests and build/typecheck/audit gates passed. The replacement was launched as an independent hidden local process with recorded live controller/child evidence; no agent is retained to supervise it. This is neither automatic crash resume nor recurring scheduling nor a UI integration.
- Acquisition ownership: [industry source reservations](INDUSTRY-SEGMENTS.md#cross-process-acquisition-exclusion) now prevent overlapping API/CLI runs from acquiring the same national or state source concurrently. Canonical executable reservations also block source-name aliases. Every source is reserved before any worker launches; faults drain active workers and keep locks until durable terminal persistence. Ambiguous ownership is never reclaimed based on age or PID alone. All 452 repository tests and build/typecheck/audit gates passed, including a real competing Node process and transient receipt-write fault injection. No source download or production input mutation occurred. This is concurrent exclusion, not scheduled occurrence deduplication or freshness reuse.
- App-owned interval scheduling: the [managed refresh scheduler](MANAGED-REFRESH-SCHEDULER.md) now persists default-disabled schedules, pins configured plans through child execution, records deterministic occurrences before dispatch, adopts linked terminal receipts after restart, and pauses uncertain or failed work. Authenticated API controls create/list/enable/pause; no production schedules were configured. Enabled overlaps are rejected, missed intervals coalesce, and busy-only deferral does not consume an occurrence. Graceful Windows IPC shutdown releases ownership. All 478 tests and build/typecheck/audit gates passed, including timer execution and real child plan rejection without live provider requests. UI controls, full occurrence indexing, stale-lock recovery, and freshness-based reuse remain incomplete.
- Schedule management: Data Operations now creates disabled schedules with explicit industry/state selections and exposes enable, pause, due time, latest occurrence, and inspection reasons. Polling is isolated from manual operations and rejects stale reads across mutations. All 483 tests and build/typecheck/audit gates passed, including five non-browser component-handler tests. No live schedule was enabled. Windows terminal-stop ownership recovery remains manual; the stopped empty scheduler's lock was preserved after process/state inspection, not automatically reclaimed.
- Remaining integration: source freshness reuse and explicit interrupted-run recovery; continue additional industry/state acquisition and gap closure. The complete isolated registry, entity-resolution, benchmark-sample, and coverage chain has independently verified. Following explicit user approval, the [September 7 cutover](NORMALIZED-US-POSTAL-CUTOVER.md) committed all 25 corrected production source pointers with verified rollback backups. Production readiness is 25/25. The separate [production downstream rebuild](PRODUCTION-RECONCILIATION-2026-09-07.md) is running and is not yet verified; its previous published views remain available until replacements publish. The benchmark still has zero independent labels and its accuracy gate remains false. See [isolated reconciliation evidence](REFRESH-RECONCILIATION.md).

Source handoff follow-up (September 7): a parallel Alaska peer audit and D.C. integrator audit found missing complete cancellation/cleanup lifecycles, so neither license source was enrolled prematurely. Alaska's first prerequisite repair fixes premature retry timing, supports publisher date/seconds delays, cancels native backoff on AbortSignal, and fails closed for waits beyond one day. See [handoff findings](states/AK-DC-HANDOFF-2026-09-07.md). Complete CLI cancellation, bounded network execution, and publication/cleanup fault tests remain next, followed by industry enrollment. No production pointer or acquisition was changed.

D.C. cancellation preparation: CLI IPC, native retry waits, stream backpressure/hash/reader cleanup, verifier abort propagation, and pre-publication checks now share the cancellation signal. Cancelled owned staging is removed without touching prior releases or sibling staging; ordinary failures stay inspectable. Six new offline tests include a real CLI child and pre-errored writer regression. Provider pacing/timeouts and updating the production-pinned connector JSON after the current run are still required before scheduler enrollment. See [D.C. cancellation boundaries](DC-BASIC-BUSINESS-LICENSES.md#cooperative-cancellation-preparation--september-7).

D.C. network safeguards: shared HTTP guards now enforce streamed/declared response byte limits, a per-attempt deadline through body reading, publisher Retry-After delays without truncation, and fail-closed excessive wait deferral. Six offline network regressions complement cancellation tests. The production-pinned connector contract update and scheduler enrollment remain pending; no new provider request or production data change was made.

Local development shutdown: an explicit [stop command](LOCAL-DEVELOPMENT-SHUTDOWN.md) now asks the unique local launcher to stop through IPC, confirms a terminal receipt, and avoids force-killing its runner on the file-request path. This replaces manual terminal termination for development checks; it does not claim crash recovery or Electron/Windows-service lifecycle coverage. No credentials are stored in stop receipts, and independent reconciliation controllers are not targeted.

State access refresh: the seven-bucket ledger now measures 357 jurisdiction/industry cells (202 national evidence, three direct state evidence, 100 missing, 52 unmeasured) against the still-published September 2 coverage artifact. Texas's new selector expands inventory scope, not actual acquisitions. Scheduling observation is explicitly unknown rather than falsely unimplemented; reports never imply a configured source is queued. See [current report evidence](STATE-ACCESS-COORDINATION.md#september-7-evidence).

Retained credential visibility (September 8): the coverage explorer now has a separate authenticated, read-only state/ZIP aggregate panel backed by checksum-verified Minnesota residential evidence. Its percentages describe the accepted credential cohort, not U.S. business completeness; national totals remain unchanged. It does not dispatch or supervise downloads. See [retained evidence and boundaries](MN-CONSTRUCTION-STATE-REPORT.md#read-only-dashboard-follow-up).

Selective app investigation (September 8): manual industry/API collection can select individual applicable source IDs, preserving default scheduled behavior and source reservations. This allows registrations-only Minnesota work without re-downloading retained residential data. CSV errors now have specific fixed classifications without retaining parser text or loosening validation. [Recovery evidence and limitations](MN-CONSTRUCTION-SELECTIVE-RECOVERY.md) distinguish partial frames from completed acquisitions; this is not automatic retry or national promotion.

Source recovery safeguards (September 8): Minnesota's registrations-only investigation confirmed malformed closing quotes, so a reviewed exact-ETag hold now prevents repeated full transfers of that version without affecting residential replay. In parallel, Delaware's existing license HTTP helper gained streamed byte caps, deadlines, body cleanup and cancellable publisher cooldowns. No source was repulled for these repairs. Delaware CLI/publication cancellation remains a prerequisite to managed enrollment. See [Minnesota recovery](MN-CONSTRUCTION-SELECTIVE-RECOVERY.md#verified-outcome-and-exact-version-hold) and [Delaware transport](DE-BUSINESS-LICENSES.md#bounded-transport-prerequisite-september-8-2026).

Delaware local lifecycle (September 8): build/verify/resume now honor cooperative cancellation, close compressed streams, preserve resumed and uncertain staging, and explicitly report incomplete publication after commit errors. Real CLI IPC and filesystem fault tests complement the transport regressions. The retained 66,667 license-based candidates were independently reverified without a download; no active-business count is inferred. Managed enrollment still requires the pinned contract migration and aligning commit timing with the worker's forced-cancel grace. See [lifecycle evidence](DE-BUSINESS-LICENSES.md#lifecycle-verification-and-retained-data-continuity).

Delaware acquisition budgets (September 8): builds now share cumulative request-attempt, consumed-response-byte and source-row ceilings across preflight, paging and final checks. Exhaustion stops acquisition without publication or automatic retry; historical offline verification remains unchanged. Thirty-five focused synthetic tests pass. The subsequent resource prerequisite is recorded below. Next app-handoff prerequisites remain a separately identified app connector contract that preserves existing production pins and a validated wrapper/receipt enrollment. No fresh download or managed enrollment is implied. See [budget limits](DE-BUSINESS-LICENSES.md#cumulative-acquisition-limits-september-8-2026).

Collection cancellation alignment (September 8): shared 60-second source-worker and 75-second collection-supervisor grace periods replace 10/15-second collection windows; exports retain 15 seconds. A real nested synthetic Delaware process completes 16 seconds of postcommit work after IPC cancellation, verifies its publication, closes both processes and removes the publication lock. Cancelled control receipts now preserve observed child-exit evidence and explicitly warn that published or partial output may exist; no rollback or automatic retry is inferred. Forty-one focused tests passed. This addresses cooperative timing, not OS-I/O bounds or managed Delaware enrollment. See [contract and limitations](COLLECTION-CANCELLATION.md).

Delaware resource prerequisite (September 8): fresh builds now fail closed before staging/baseline/network on insufficient or unavailable disk, RAM or heap observations. A 30-minute cooperative deadline covers precommit work and preserves the existing commit-boundary behavior. Native preflight passed on this host and the retained 66,667 license candidates reverified offline. These checks are not resource reservations or a peak-memory guarantee. Next is the separate app connector contract and fixed wrapper/receipt enrollment, with retained-release reuse distinguished from a needed refresh. See [resource evidence and limitations](DE-BUSINESS-LICENSES.md#resource-prerequisite-and-cooperative-deadline-september-8-2026).

Delaware app enrollment (September 8): a distinct app connector contract and strict run/verify CLIs now back the Delaware-only `local-business-licenses` source. Native acquisition uses job-specific output; retained mode independently verifies existing immutable releases without download or pointer changes. Standalone retained job `982fb5e2-9bd8-4f33-95c5-3f923e8b6ec5` succeeded and its receipt reverified the 66,667 license candidates. Synthetic native-path tests cover 60,000 rows, cross-job borrowing rejection and configuration drift without provider calls. No fresh managed collection, recurring schedule or national promotion was dispatched. See [receipt and scope evidence](DE-BUSINESS-APP.md).

Alaska handoff follow-up (September 8): current-state inspection superseded stale audit gaps and found remaining noncooperative header/cooldown waits and untyped postcommit failures. These are repaired with abort races, monotonic deadline checks, redacted transport errors and `AK_PUBLICATION_INCOMPLETE` outcomes preserved through concurrent cancellation. Forty-seven focused tests passed, and the retained 94,884 license-backed candidates reverified with unchanged pointer/manifest hashes. App contract/receipt enrollment remains next; no source repull or industry dispatch occurred. See [current evidence](AK-ACTIVE-BUSINESS-LICENSES.md#transport-completion-and-publication-outcomes--september-8-2026).

Alaska app ownership (September 8): the separate app connector now enrolls an Alaska-only local-business-license job with fixed native acquisition, a cooperative wrapper deadline and immutable start/terminal receipts. Retained standalone job `f67a2428-ced8-48e6-80a2-25226a058bca` succeeded and independently reverified 94,884 license-backed candidates without downloading again. Routine collection belongs to Co*Tive workers, not occupied Codex agents. No live refresh, schedule or national promotion was dispatched. See [app evidence and limitations](AK-BUSINESS-APP.md).

Pennsylvania childcare readiness (September 8): a new [metadata/count preflight](PA-CHILDCARE-PREFLIGHT.md) verified the complete portal notice, sanitized selected catalog schema and paired center-only aggregates through six bounded native requests. Its immutable receipt records 4,995 listed centers, 4,995 distinct location keys and 4,930 non-null points; the 65 missing points are an explicit gap. No facility rows, managed collection, schedule or national promotion occurred. Next is a bounded facility connector and tested standalone app operation/receipt handoff. Parallel [Florida/Oregon triage](states/FL-OR-SOURCE-TRIAGE-2026-09-08.md) records unresolved delivery/schema contracts without bypassing access controls or submitting paid records requests.

Pennsylvania acquisition layer (September 8): [bounded center acquisition and durable source reuse](PA-CHILDCARE-ACQUISITION.md) now implements ordered membership/page reconciliation, paired source preflights, privacy allowlists and drained cancellation. The immutable writer awaits prerequisites and observation journals before subsequent requests and supports offline replay, so later normalization can reuse a completed acquisition without repulling. Seven grouped synthetic tests pass. No live facility rows, managed operation, schedule or national promotion was dispatched. Next is normalized facility evidence and tested app-worker enrollment/handoff. npm test-file concurrency is capped at four after reproducible 28-way startup contention; app concurrency is unchanged.

Pennsylvania normalized facility evidence (September 8): the [offline normalization/release layer](PA-CHILDCARE-NORMALIZATION.md) now preserves separate ZIP5/ZIP4, nullable address points, typed source identifiers, license dates and capacity text, page-level observation lineage and accepted-plus-quarantine conservation. Immutable derived releases bind the exact acquired manifest and replay against it; reprocessing makes no network requests and preserves existing input/releases. Nine focused tests pass, including cancellation after output-job creation and rejected rehashed substitutions. No live facility collection, managed operation, schedule or national promotion occurred. Next is end-to-end app-worker enrollment with explicit acquired/normalized receipt linkage and retained-input reuse.

Pennsylvania app handoff (September 8): the standalone worker and managed industry entry shipped, then Co*Tive operation `b56d91bb-b781-4fe6-b7f9-be50bd4234ee` independently completed acquisition and normalization. Offline verification confirmed 4,995 accepted center candidates across 819 reported ZIPs, with 65 missing coordinates and no supplied ZIP4. Acquired and normalized manifests remain pinned and reusable; next reporting integration must not repull them or describe source-cohort coverage as national business completeness. See [live outcome and quality evidence](PA-CHILDCARE-APP.md#live-application-outcome--september-8-2026).

Parallel childcare source triage (September 8): [Georgia/North Carolina findings](states/GA-NC-CHILDCARE-TRIAGE-2026-09-08.md) distinguish Georgia's official but currently unverified export delivery contract from an incomplete North Carolina investigation. Neither was enrolled or downloaded. Historical schema documentation, indexed legal material and absent evidence are not substitutes for current source prerequisites.

Pennsylvania local reporting (September 8): a [verified retained-cohort projection](PA-CHILDCARE-REPORTING.md) now provides state, reported-county and ZIP summaries, source-cohort percentages and quality gaps. State-access reports separately expose the fixed native app receipt as local facility evidence. Missing local data is unavailable rather than zero; other states are outside this publisher's scope. No source download, national pointer migration or scheduler change is part of this reporting step.

Additional childcare source validation (September 8): the [NC follow-up](states/GA-NC-CHILDCARE-TRIAGE-2026-09-08.md) found a current dashboard maintenance/data-request notice and established the old NCDOT extract's June 2018 vintage. [SC triage](states/SC-CHILDCARE-TRIAGE-2026-09-08.md) verified the regulator and locator but not bulk delivery. [MD metadata validation](states/MD-CHILDCARE-TRIAGE-2026-09-08.md) verified the public state GIS organization/item and layer schema, while leaving the complete linked usage-policy context and center predicate unresolved. No dataset was mislabeled current, no rows downloaded and no dependent connector enrolled.

Connecticut childcare prerequisite (September 8): the [tested metadata/count preflight](CT-CHILDCARE-PREFLIGHT.md) completed four bounded native requests and retained an immutable sanitized receipt. It confirms 1,390 active-center source rows, 1,364 distinct credentials and one missing street-address value; repeated credentials must not collapse source rows. No facility acquisition, app enrollment, refresh schedule or national promotion occurred. Resolve address/key semantics and implement the bounded facility connector before handing routine downloads to standalone Co*Tive workers. Parallel [Maryland follow-up](states/MD-CHILDCARE-TRIAGE-2026-09-08.md) established an official public-use context and an aggregate center predicate, not a completed acquisition contract.

Connecticut selected-source acquisition (September 8): the [bounded acquisition and immutable replay layer](CT-CHILDCARE-ACQUISITION.md) preserves release-scoped opaque row keys, repeated credentials and role-unverified reported addresses. Prerequisite and observation journals complete before dependent requests, and completed acquisition can be reused offline. Maryland's parallel schema follow-up identified duplicate `USER_*` aliases and coordinate-type differences that a future collector must not confuse with original fields. No native CT facility rows, app operation, schedule or national promotion were dispatched in this increment; normalization and tested standalone worker enrollment remain next.

Connecticut source-candidate normalization (September 8): the [offline normalized release](CT-CHILDCARE-NORMALIZATION.md) preserves opaque row identity, repeated credentials, exact sparse selected input, reported-address gaps and separate ZIP5/ZIP4. Calendar validity does not invent a timezone; capacity parsing preserves raw evidence. Derivatives bind and replay acquired manifests, conserve accepted plus quarantined rows, and make no source requests. The app handoff review identifies remaining fixed-native/retained worker entry points, publisher exclusion, durable linked receipts and managed cancellation tests before collection dispatch.

Connecticut app ownership (September 8): the [standalone worker and managed enrollment](CT-CHILDCARE-APP.md) passed synthetic lifecycle/reuse/cancellation tests and full repository validation. Co*Tive accepted CT-only operation `efd15ce2-6bba-4096-9084-7ace88ea0695` with a persisted receipt. The app owns ongoing acquisition and normalization; agents do not wait on download progress. Accepted dispatch is not terminal success or measured coverage. Verify its eventual terminal child receipts before integrating local reporting, reusing retained acquisition rather than repulling. No national production or recurring schedule was launched.

Connecticut completed collection audit (September 8): Co*Tive operation `efd15ce2-6bba-4096-9084-7ace88ea0695` completed successfully. [Independent offline receipt and quality verification](CT-CHILDCARE-APP.md#verified-application-outcome--september-8-2026) confirmed 1,390 source candidates, zero quarantine, 230 ZIP5 values and 1,208 separate ZIP4 values. One street and all coordinates remain missing. Repeated credentials/licenses are retained rather than collapsed; capacity totals must not imply unique facilities. No repull was performed. Next local reporting must consume these verified retained manifests without claiming national completeness or unique active-business coverage.

Connecticut retained local reporting (September 8): [verified source-candidate summaries](CT-CHILDCARE-REPORTING.md) now expose nullable reported-state/ZIP groups, cohort-denominator percentages and reconciled quality gaps from the pinned native app receipt. A fresh state-access report includes 1,390 local CT candidates across 230 ZIP5 groups while retaining the separate unmeasured national status. Missing installed files are unavailable, not zero; repeated credentials do not become business identities or capacity totals. No source repull, national promotion or scheduler change occurred.

Maryland childcare prerequisites (September 8): a tested [standalone metadata/count preflight](MD-CHILDCARE-PREFLIGHT.md) now retains unchanged item notices, pins the exact nine-field contract and rejects unexpected record payloads, source drift and unsafe receipt replay. The native immutable receipt verified 1,772 center rows through six bounded requests without facility acquisition. The February 13 cohort date remains separate from observation/edit dates. Next: bounded ID-batch acquisition and immutable retained release, then normalization and app-owned collection enrollment; no national promotion or routine-download agent loop was started.

Maryland center acquisition (September 8): [bounded ID-batch acquisition and immutable retained evidence](MD-CHILDCARE-ACQUISITION.md) add prerequisite/page journals, count/membership/CRS reconciliation, cancellation and independent offline replay. A bounded delivery sample established the actual ArcGIS response schema without creating a bulk release. Normalization and app-worker enrollment remain next. The same validation cycle also surfaced and addressed the [CSV parser security advisory](CSV-PARSER-SECURITY-2026-09-08.md); retained data and national production pointers were not rewritten.

Maryland standalone processing (September 8): [offline source-candidate normalization](MD-CHILDCARE-NORMALIZATION.md) preserves separate postal fields, nullable points, reported-state conflicts and raw provenance with exact source conservation. The [app worker](MD-CHILDCARE-APP.md) adds publisher-wide exclusion, fixed native and retained modes, independently verified child bindings and durable terminal receipts. Industry enrollment is Maryland-only and remains separate from measured national coverage. Dispatch evidence and final validation are recorded in the app document; configuration or synthetic tests alone do not establish a live download.

Maryland retained reporting (September 8): the app completed its first collection independently. Offline verification confirms 1,772 accepted source candidates, no quarantine and one invalid-range ZIP gap. [Local cohort reporting](MD-CHILDCARE-REPORTING.md) adds pinned retained evidence, reported-state/ZIP grouping and explicit cohort denominators without a second download or national promotion. The publisher's February 13 cohort is not reinterpreted as September current-operation evidence.

Vermont prerequisites (September 8): the [metadata-only preflight](VT-CHILDCARE-PREFLIGHT.md) now verifies paired catalog and aggregate evidence, preserves ODbL and coordinate-jitter notices, and refuses provider-row payloads. Native execution verified 503 center-license candidates within 1,057 source rows; no facility records were downloaded. Acquisition, address-role validation, record-use policy and standalone app enrollment remain explicit next gates, not implied by this prerequisite success.

Vermont selected delivery (September 8): [bounded source validation](states/VT-CHILDCARE-SELECTED-DELIVERY-2026-09-08.md) found GET returned unwanted system columns despite suppression flags; documented POST JSON returned only the selected fields across two tiny pages. Unknown reporting period and address role remain explicit quality gaps, not artificial acquisition blockers. Next is the pinned internal collector with page journals, membership/count reconciliation and offline replay, followed by app-owned collection. No bulk release or operation was created by the sample.

Vermont bounded acquisition (September 8): the [internal collector and immutable release verifier](VT-CHILDCARE-ACQUISITION.md) add fixed POST bodies, baseline/selected/final traversal with empty terminal pages, prerequisite and page journals, composite-key and aggregate reconciliation, bounded transport and cancellation, and offline replay. Synthetic acceptance does not claim a native release or app dispatch. Next: source-candidate normalization and app-owned lifecycle, with separate postal fields and no inferred exact geocodes or current business identity.

Vermont standalone processing (September 8): [offline normalization](VT-CHILDCARE-NORMALIZATION.md) preserves composite release-scoped identities, split postal fields, raw dates/capacities, null geography and reconciled quarantine. The [app lifecycle](VT-CHILDCARE-APP.md) adds native publisher exclusion, durable child checkpoints, explicit retained reuse and independent verification. Industry enrollment selects one Vermont worker without promoting national coverage. Handoff requires a recorded accepted operation; configuration and synthetic tests alone do not prove native collection.

Every milestone must pass:

- unit, connector-conformance, server, store, worker, and integration tests;
- cancellation, concurrency, restart, fault-injection, retention, and secret-canary tests;
- Electron end-to-end tests for operator-critical workflows;
- `npm run check`, type checking, production dependency audit, secret scan, license scan, and SBOM generation;
- clean Windows VM packaging tests without preinstalled Node, npm, Git, or browser;
- migration, upgrade, backup, restore, rollback, and data-preservation checks;
- signed, checksummed release artifacts with release and migration notes.

Remote access, multi-user operation, or production data claims remain out of scope until the security, lifecycle, and release gates have passed.

Vermont retained completion (September 8): [independent offline verification](VT-CHILDCARE-APP.md#verified-retained-completion--september-8-2026) passed for the app-owned collection. The release contains 503 normalized source candidates, zero quarantine, 172 distinct reported ZIP5 values and two separate ZIP4 values. No selected geocodes or source-reported state are available. Next: integrate retained local reporting without conflating publisher scope with address geography or claiming national completeness; reuse the verified release rather than recollecting it.

Vermont local reporting (September 8): the [retained reporting adapter and native enrollment](VT-CHILDCARE-REPORTING.md) expose the verified 503-row publisher cohort separately in the VT childcare ledger cell, preserving unknown address state, missing points, ZIP5/ZIP4 metrics and source clocks. This consumes existing artifacts offline, not a second acquisition. Published national status and industry-completeness percentages remain unchanged. Next national integration must preserve this publisher-only basis rather than treating VT as a reported address assignment. A parallel [New Hampshire source review](states/NH-CHILDCARE-TRIAGE-2026-09-08.md) found public-search documentation but no verified center-only bulk contract.
