# Reviewed service integration queue

After the verified File Builder release `9b0d21a`, these reviewed services were integrated into main and passed combined validation. Root owns integration; source agents continue in isolated checkouts under `data/worktrees`. Heatmap UI `d68c8d8` passed native desktop acceptance, including keyboard selection, CTRL-wheel zoom and 100/200 percent text size. CMS lifecycle `26b8f43` has completed one native acquisition, independently verified below. Follow-on isolated source changes described later are not included in this validated batch.

Integrated commit mapping: aggregate `4969543`, Oklahoma status `a5bab0e`, credential view `408f4e4`, NES alignment `6e66223`, CMS source note `4e4d993`, CMS offline prerequisite `13dabda`. The table retains original isolated commit identities for traceability.

| Service | Isolated commit | Evidence | Next step |
| --- | --- | --- | --- |
| MN credential heatmap aggregate | `af0f740` | Eight focused tests and retained native proof conserve 11,456 rows across state and postal groups | Credential-specific app integration is in progress in its own checkout |
| MN credential map view adapter | `de111f7` | Twelve focused tests; native verification of 56 geography features, 51 displayed states/DC, fixed totals and shared snapshot; one outstanding loader pair maximum | Integrate credential-specific endpoint/UI after current app release |
| Oklahoma batch-status projection | `b8b7d0d` | Six focused tests; existing pinned implementation files untouched | Include standalone inspector in next service release; current historical plan replay remains unverified because an implementation pin changed |
| Census NES industry alignment | `ff08860` | Ten focused tests; 2023 retail NAICS 44-45 national/state counts reconcile at 2,081,566 | Integrate the service independently; UI selection/catalog is still needed before claiming an app feature |
| CMS hospital source contract | `962e8d2` | Official metadata/docs only; no hospital records downloaded | Implement/review bounded prerequisites before a separately reviewed national acquisition |
| CMS hospital offline prerequisites | `6fd3856` | Eight focused tests; metadata byte/schema and selected CSV field conformance, no transport authenticity claim | Review bounded app-owned acquisition lifecycle before native execution |
| CMS hospital acquisition lifecycle | `b0e0125` | Twenty combined focused tests; independent static review; one native run completed and replayed | Normalize/adopt the retained release without another download; include code in the next combined check |

Native CMS job `7140acea-6dfb-478e-8769-5dd991bc1875` completed with `inspectionRequired:false`; its manifest SHA-256 is `856992891a8ded15d0f924169991cd3c7d0bda6112de1a0702dd5600305e9239`. Root independently replayed its four-request evidence, notice/policy pins and CSV-to-selected transformation: 5,419 directory rows, 38 observed columns, 2,058,958 response bytes. Raw CSV SHA-256 is `f874f09fef895a1ccf5bb7392dcbb2be05c9860339261b093fe00f0c1b013480`; selected JSONL SHA-256 is `30cb62fac6c3c9a52e9cdba31423a138b65945beb8321cb48f3825f8506bb979`. Source modified date is July 22, 2026 and release date August 13, 2026, not the acquisition date. Raw files remain internal; selected fields remain local-review-only. No retries, production promotion or business-operation/geocode claims were introduced.

Heatmap and Oklahoma commits are in `data/worktrees/maine-provider-preflight`. NES and CMS commits are in `data/worktrees/retained-feed-integration`. Preserve those checkouts and ignored native proofs until integration is verified.

## Current main release boundary

File Builder's actual retained-only operation `8a46d759-8aa4-4c3a-98ef-fd63c28d079f` succeeded with one Wyoming-reported credential row. The UI download saved 1,593 bytes matching the managed artifact SHA-256 `df4d2d1c06a3b77d323262d1fd1d8dc9744fa6b2a711666cd296e21a0e5a4629`; restart visibility, mode switching and locked local-review policy passed. This validates that operation, not nationwide collection.

The first combined check ended with 1,887 passing tests, 60 skipped and one failure. Focused investigation confirmed that the legacy business-export API fixture omitted the new `business-flatfile-compatibility.mjs` dependency from its isolated child checkout (`ERR_MODULE_NOT_FOUND`). The fixture dependency repair and import smoke passed. Production imports and error redaction remain intact. Preserve the failed log `data/ui-verification/check-credential-file-builder-final.log` as diagnostic evidence.

The clean combined check passed: 1,948 tests, 1,888 passed, 60 skipped, zero failures; lint, builds and desktop control-plane smoke passed. TypeScript and dependency audit passed, with zero reported vulnerabilities. Evidence: `data/ui-verification/check-credential-file-builder-clean.log` and `credential-final-tsc-audit.log`. The tester ran `stop-collector.bat`, relaunched successfully and left Electron PID 6936 open; runner start was `2026-09-12T13:55:19.457Z`. Authenticated health, coverage, representation, schedules and credential-operation reads returned HTTP 200. Representation retained 51 states/DC and unknown all-business completeness; no schedules were enabled. These release checks do not include the isolated services above.

## Meaning of the new measures

Credential counts remain source credential rows, not unique businesses or verified operating sites. NES percentages describe an annual nonemployer industry universe, not collection completeness or employer establishments. Oklahoma terminal rejections are not accepted queries. The CMS acquisition independently confirms 5,419 rows: 5,354 reported in the 50 states/DC and 65 in territories. These are dated directory assertions, not a denominator for all active hospitals or businesses. None of these services changes production pointers or authorizes retrying held acquisitions.

## Parallel work after integration freeze

The app coding/testing agent owns combined-check failures, narrowly scoped fixes and stop/relaunch verification. Source-service agents work independently in their isolated checkouts; their new code is not part of the frozen release check.

- CMS reporting-input validation passed eight focused tests and retained-copy replay: 5,419 total, 5,354 states/DC, 65 territories, Illinois 193. Nationwide denominators remain fixed under filtering; all ZIP4 values are null and 164 alphanumeric facility IDs are preserved. Proof: isolated `data/tmp/cms-hospital-reporting-retained-copy-proof-20260912.json`, SHA-256 `7d54fc8757e0a6484460ea90c5945fff58601810a5ea94b05eebd5226ad3cf1c`. This reused byte-identical retained artifacts, not another acquisition. Independent review found no blocking defect; focused midstream-cancellation evidence remains a stated boundary.
- The same service lane is preparing an offline Census address-geocoding contract. No addresses have been submitted. Approximate provider points, datum, match status and original address linkage must remain explicit; no business polygons or invented confidence.
- The independent source/review lane is auditing national CMS nursing-home Provider Information prerequisites and checking for retained equivalents before any acquisition. Shared national sources are acquired once and partitioned locally, not downloaded once per state.

Isolated follow-on commits: `1140ae4` contains the reviewed CMS hospital retained reporting input; `01f2044` contains the Census geocoder and CMS nursing-home source audits. Neither is included in the current combined check. Nursing-home dataset `4pq5-n9py` documents geocoding footnote 22 as a ZIP-based estimate: it is not eligible as a facility point. Blank footnotes do not establish exact address matching. Offline source-specific parsing/policy implementation is now assigned independently of the app; no nursing-home CSV or address submission has been authorized by these documentation commits.

The initial heatmap combined check ended with exit 1: 2,014 tests, 1,947 passed, seven failed and 60 skipped. Six business-name UI fixture failures were reproduced as a missing sibling-component stub and the narrow fixture repair passed six focused tests. The development-supervisor failure reported an existing vinext process lock, not a readiness timeout; its focused rerun passed two tests, but fixture isolation and lock ownership remain under investigation. No arbitrary port owner was terminated. A clean combined check and final relaunch are required before publishing this batch.

Nursing-home offline prerequisite commit `6afdbf0` is isolated and reviewed: ten focused tests and lint passed. It preserves the publisher's dated currently-active directory assertion separately from unverified present operations, keeps raw coordinate/footnote evidence, and does not treat ZIP estimates as facility points. Its next application-owned acquisition lifecycle must enforce mutual exclusion with the existing hospital source, not merely declare the same budget label. No nursing-home CSV acquisition has run.

Geocoder offline contract `7815744` passed eleven focused tests after review corrected Puerto Rico's distinct City/Municipio layouts and removed forced Census2020 selection. It requires an explicit reviewed public benchmark and preserves ZIP4 separately. It is still an offline contract, not a runnable geocoding service or evidence of geocoded businesses.

The nursing-home dictionary-only research request retained 644,126 bytes with SHA-256 `2b55b0f65532e36fe7c49967993a37ddcb8c92e38234c94a2652c9a77feeb313`; root independently checked that file hash. This is documentation acquisition, not facility data acquisition. Its isolated receipt records the official fixed URL, one GET, no redirects/retries and bounded bytes/time.

The test-only development fixture isolation and UI stub repair passed eight focused tests. The clean combined check completed successfully: 2,014 tests, 1,954 passed, 60 skipped, zero failures; lint, web/desktop builds and desktop control-plane smoke passed. Evidence is `data/ui-verification/check-credential-heatmap-clean.log`; final relaunch is recorded below when complete. Production registry pointer SHA-256 `a5e5d58960e97ee51825ec558d1b59a356f7a98a15300db504a35df625ace1de` and MN credential selection SHA-256 `bda3ff50d5ec3ad4d66fa2c3dae0594fe96274f8698c4406d94e7ba002368154` remained unchanged in root's latest read-only check.

## Subsequent nursing-home acquisition and retained recovery

After lifecycle `3476734` passed 25 focused tests and root review, root dispatched exactly one bounded native CLI run from the isolated Maine checkout. Run `ffb1fac4-4eb4-4ea4-b11c-875ebff4de41` finished FAILED, with `inspectionRequired:true` and `sourceLeaseRetained:false`. All five restricted responses are retained; no success manifest was published and no retry ran. This supersedes the earlier pre-execution notes above, not their historical evidence.

The CSV is 9,151,628 bytes, SHA-256 `a5808fcded1ca73a629fde88fc23dcba3b6ff8ed75199883062a262e14ef8fe0`. Before/after metadata hashes both equal `da4626d917b85564425399018e1e5d0ee1e71dc9734764ac004fcf8322c4ea0d`; dictionary and notice match their reviewed pins. Root's offline structural scan found 99 columns, 14,690 rows, no malformed or duplicate CCNs, and one selected address with a control character. Source-agent diagnosis identified U+0009 TAB at logical row 14,600. The address value was not printed.

Recovery is being implemented as a new immutable retained-source operation, not a rewritten acquisition success. The narrow revised projection must preserve the source TAB verbatim with an explicit quality annotation, keep other field/control checks, conserve all accepted source rows, and reference the original failed run's exact evidence. It must make no network requests or modify the failed receipt. These rows are retained source evidence, not yet a successfully published nursing-home release or verified business count.

## Validated heatmap release restoration

After the successful combined check, the coding agent ran `stop-collector.bat` and `launch-datahub.bat`; both succeeded. Final authenticated health, coverage, eight-source representation, schedules and credential heatmap reads returned HTTP 200. The app was left open as Electron PID 27820, which root independently confirmed running. The restarted credential view verified at `2026-09-12T14:26:31.710Z`, retaining 11,456 rows, 51 states/DC and 10,374 MN residential-building-contractor rows. Schedules remained empty and all-business completeness remained null. Type checking and dependency audit passed with zero reported vulnerabilities. No production pointer was changed by these checks.
