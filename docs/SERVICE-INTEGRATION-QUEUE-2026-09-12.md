# Reviewed service integration queue

After the verified File Builder release `9b0d21a`, these reviewed services were integrated into main and passed combined validation. Root owns integration; source agents continue in isolated checkouts under `data/worktrees`. Heatmap UI `d68c8d8` passed native desktop acceptance, including keyboard selection, CTRL-wheel zoom and 100/200 percent text size. CMS lifecycle `26b8f43` has completed one native acquisition, independently verified below. Follow-on isolated source changes described later are not included in this validated batch.

Integrated commit mapping: aggregate `4969543`, Oklahoma status `a5bab0e`, credential view `408f4e4`, NES alignment `6e66223`, CMS source note `4e4d993`, CMS offline prerequisite `13dabda`. The table retains original isolated commit identities for traceability.

| Service | Isolated commit | Evidence | Next step |
| --- | --- | --- | --- |
| MN credential heatmap aggregate | `af0f740` | Eight focused tests and retained native proof conserve 11,456 rows across state and postal groups | Credential-specific app integration is in progress in its own checkout |
| MN credential map view adapter | `de111f7` | Twelve focused tests; native verification of 56 geography features, 51 displayed states/DC, fixed totals and shared snapshot; one outstanding loader pair maximum | Integrate credential-specific endpoint/UI after current app release |
| Oklahoma batch-status projection | `b8b7d0d` | Six focused tests; existing pinned implementation files untouched | Include standalone inspector in next service release; current historical plan replay remains unverified because an implementation pin changed |
| Census NES industry alignment | `ff08860` | Ten focused tests; 2023 retail NAICS 44-45 national/state counts reconcile at 2,081,566 | Integrate the service independently; UI selection/catalog is still needed before claiming an app feature |
| CMS hospital source contract | `962e8d2` | Official metadata/docs only; no hospital records downloaded | Implement/review bounded prerequisites before a separately reviewed national acquisition |
| CMS hospital offline prerequisites | `6fd3856` | Eight focused tests; metadata byte/schema and selected CSV field conformance, no transport authenticity claim | Review bounded app-owned acquisition lifecycle before native execution |
| CMS hospital acquisition lifecycle | `b0e0125` | Twenty combined focused tests; independent static review; one native run completed and replayed | Normalize/adopt the retained release without another download; include code in the next combined check |

Native CMS job `7140acea-6dfb-478e-8769-5dd991bc1875` completed with `inspectionRequired:false`; its manifest SHA-256 is `856992891a8ded15d0f924169991cd3c7d0bda6112de1a0702dd5600305e9239`. Root independently replayed its four-request evidence, notice/policy pins and CSV-to-selected transformation: 5,419 directory rows, 38 observed columns, 2,058,958 response bytes. Raw CSV SHA-256 is `f874f09fef895a1ccf5bb7392dcbb2be05c9860339261b093fe00f0c1b013480`; selected JSONL SHA-256 is `30cb62fac6c3c9a52e9cdba31423a138b65945beb8321cb48f3825f8506bb979`. Source modified date is July 22, 2026 and release date August 13, 2026, not the acquisition date. Raw files remain internal; selected fields remain local-review-only. No retries, production promotion or business-operation/geocode claims were introduced.

Heatmap and Oklahoma commits are in `data/worktrees/maine-provider-preflight`. NES and CMS commits are in `data/worktrees/retained-feed-integration`. Preserve those checkouts and ignored native proofs until integration is verified.

## Current main release boundary

File Builder's actual retained-only operation `8a46d759-8aa4-4c3a-98ef-fd63c28d079f` succeeded with one Wyoming-reported credential row. The UI download saved 1,593 bytes matching the managed artifact SHA-256 `df4d2d1c06a3b77d323262d1fd1d8dc9744fa6b2a711666cd296e21a0e5a4629`; restart visibility, mode switching and locked local-review policy passed. This validates that operation, not nationwide collection.

The first combined check ended with 1,887 passing tests, 60 skipped and one failure. Focused investigation confirmed that the legacy business-export API fixture omitted the new `business-flatfile-compatibility.mjs` dependency from its isolated child checkout (`ERR_MODULE_NOT_FOUND`). The fixture dependency repair and import smoke passed. Production imports and error redaction remain intact. Preserve the failed log `data/ui-verification/check-credential-file-builder-final.log` as diagnostic evidence.

The clean combined check passed: 1,948 tests, 1,888 passed, 60 skipped, zero failures; lint, builds and desktop control-plane smoke passed. TypeScript and dependency audit passed, with zero reported vulnerabilities. Evidence: `data/ui-verification/check-credential-file-builder-clean.log` and `credential-final-tsc-audit.log`. The tester ran `stop-collector.bat`, relaunched successfully and left Electron PID 6936 open; runner start was `2026-09-12T13:55:19.457Z`. Authenticated health, coverage, representation, schedules and credential-operation reads returned HTTP 200. Representation retained 51 states/DC and unknown all-business completeness; no schedules were enabled. These release checks do not include the isolated services above.

## Meaning of the new measures

Credential counts remain source credential rows, not unique businesses or verified operating sites. NES percentages describe an annual nonemployer industry universe, not collection completeness or employer establishments. Oklahoma terminal rejections are not accepted queries. The CMS acquisition independently confirms 5,419 rows: 5,354 reported in the 50 states/DC and 65 in territories. These are dated directory assertions, not a denominator for all active hospitals or businesses. None of these services changes production pointers or authorizes retrying held acquisitions.

## Parallel work after integration freeze

The app coding/testing agent owns combined-check failures, narrowly scoped fixes and stop/relaunch verification. Source-service agents work independently in their isolated checkouts; their new code is not part of the frozen release check.

- CMS reporting-input validation passed eight focused tests and retained-copy replay: 5,419 total, 5,354 states/DC, 65 territories, Illinois 193. Nationwide denominators remain fixed under filtering; all ZIP4 values are null and 164 alphanumeric facility IDs are preserved. Proof: isolated `data/tmp/cms-hospital-reporting-retained-copy-proof-20260912.json`, SHA-256 `7d54fc8757e0a6484460ea90c5945fff58601810a5ea94b05eebd5226ad3cf1c`. This reused byte-identical retained artifacts, not another acquisition. Independent review found no blocking defect; focused midstream-cancellation evidence remains a stated boundary.
- The same service lane is preparing an offline Census address-geocoding contract. No addresses have been submitted. Approximate provider points, datum, match status and original address linkage must remain explicit; no business polygons or invented confidence.
- The independent source/review lane is auditing national CMS nursing-home Provider Information prerequisites and checking for retained equivalents before any acquisition. Shared national sources are acquired once and partitioned locally, not downloaded once per state.

Isolated follow-on commits: `1140ae4` contains the reviewed CMS hospital retained reporting input; `01f2044` contains the Census geocoder and CMS nursing-home source audits. Neither is included in the current combined check. Nursing-home dataset `4pq5-n9py` documents geocoding footnote 22 as a ZIP-based estimate: it is not eligible as a facility point. Blank footnotes do not establish exact address matching. Offline source-specific parsing/policy implementation is now assigned independently of the app; no nursing-home CSV or address submission has been authorized by these documentation commits.

The initial heatmap combined check ended with exit 1: 2,014 tests, 1,947 passed, seven failed and 60 skipped. Six business-name UI fixture failures were reproduced as a missing sibling-component stub and the narrow fixture repair passed six focused tests. The development-supervisor failure reported an existing vinext process lock, not a readiness timeout; its focused rerun passed two tests, but fixture isolation and lock ownership remain under investigation. No arbitrary port owner was terminated. A clean combined check and final relaunch are required before publishing this batch.

Nursing-home offline prerequisite commit `6afdbf0` is isolated and reviewed: ten focused tests and lint passed. It preserves the publisher's dated currently-active directory assertion separately from unverified present operations, keeps raw coordinate/footnote evidence, and does not treat ZIP estimates as facility points. Its next application-owned acquisition lifecycle must enforce mutual exclusion with the existing hospital source, not merely declare the same budget label. No nursing-home CSV acquisition has run.

Geocoder offline contract `7815744` passed eleven focused tests after review corrected Puerto Rico's distinct City/Municipio layouts and removed forced Census2020 selection. It requires an explicit reviewed public benchmark and preserves ZIP4 separately. It is still an offline contract, not a runnable geocoding service or evidence of geocoded businesses.

The nursing-home dictionary-only research request retained 644,126 bytes with SHA-256 `2b55b0f65532e36fe7c49967993a37ddcb8c92e38234c94a2652c9a77feeb313`; root independently checked that file hash. This is documentation acquisition, not facility data acquisition. Its isolated receipt records the official fixed URL, one GET, no redirects/retries and bounded bytes/time.

The test-only development fixture isolation and UI stub repair passed eight focused tests. The clean combined check completed successfully: 2,014 tests, 1,954 passed, 60 skipped, zero failures; lint, web/desktop builds and desktop control-plane smoke passed. Evidence is `data/ui-verification/check-credential-heatmap-clean.log`; final relaunch is recorded below when complete. Production registry pointer SHA-256 `a5e5d58960e97ee51825ec558d1b59a356f7a98a15300db504a35df625ace1de` and MN credential selection SHA-256 `bda3ff50d5ec3ad4d66fa2c3dae0594fe96274f8698c4406d94e7ba002368154` remained unchanged in root's latest read-only check.

## Subsequent nursing-home acquisition and retained recovery

After lifecycle `3476734` passed 25 focused tests and root review, root dispatched exactly one bounded native CLI run from the isolated Maine checkout. Run `ffb1fac4-4eb4-4ea4-b11c-875ebff4de41` finished FAILED, with `inspectionRequired:true` and `sourceLeaseRetained:false`. All five restricted responses are retained; no success manifest was published and no retry ran. This supersedes the earlier pre-execution notes above, not their historical evidence.

The CSV is 9,151,628 bytes, SHA-256 `a5808fcded1ca73a629fde88fc23dcba3b6ff8ed75199883062a262e14ef8fe0`. Before/after metadata hashes both equal `da4626d917b85564425399018e1e5d0ee1e71dc9734764ac004fcf8322c4ea0d`; dictionary and notice match their reviewed pins. Root's offline structural scan found 99 columns, 14,690 rows, no malformed or duplicate CCNs, and one selected address with a control character. Source-agent diagnosis identified U+0009 TAB at logical row 14,600. The address value was not printed.

Recovery is being implemented as a new immutable retained-source operation, not a rewritten acquisition success. The narrow revised projection must preserve the source TAB verbatim with an explicit quality annotation, keep other field/control checks, conserve all accepted source rows, and reference the original failed run's exact evidence. It must make no network requests or modify the failed receipt. These rows are retained source evidence, not yet a successfully published nursing-home release or verified business count.

## Validated heatmap release restoration

After the successful combined check, the coding agent ran `stop-collector.bat` and `launch-datahub.bat`; both succeeded. Final authenticated health, coverage, eight-source representation, schedules and credential heatmap reads returned HTTP 200. The app was left open as Electron PID 27820, which root independently confirmed running. The restarted credential view verified at `2026-09-12T14:26:31.710Z`, retaining 11,456 rows, 51 states/DC and 10,374 MN residential-building-contractor rows. Schedules remained empty and all-business completeness remained null. Type checking and dependency audit passed with zero reported vulnerabilities. No production pointer was changed by these checks.

## Next integration batch after published release 9517011

The heatmap release was pushed and its remote commit verified as `9517011065bdd2241bd856b270e46ad51174c92a`. The following source changes are now integrated on main for the next combined check, not covered by that published release's test result: CMS reporting `672e42c`, offline geocoder `e2d0852`, hospital registry admission `e2f8880`, source audits `ba23ccb`, nursing-home prerequisite `899d7e4`, acquisition `6c8b63e`, retained recovery `d94d052`, adoption evidence `a87b2ed`, and hospital production pinning `3768fee`.

Nursing-home recovery `9fe3aa54-dd38-42be-b28e-b1d363300a45` succeeded with manifest SHA-256 `89ee608067aa5b97833957b753be61003dc2efcfa22cfaedbb3b78f020396d7e`. Selected output is 30,397,131 bytes, SHA-256 `677b1dc7b294f72feb0d6a0803d27c9f0f074887e5b9c5a59a6837c87a2320d8`, 14,690 rows. The 12 original failed-source files and two recovery files were copied byte-identically into main without overwriting existing files. Root independently replayed recovery on main with network disabled. State/DC rows total 14,680; Puerto Rico nine and Guam one. One source TAB is preserved and flagged. All ZIP4 fields are null. There are 13,878 publisher-estimated coordinate pairs of unspecified quality and 812 ZIP-based estimates; none is currently eligible as a verified facility point. This is retained-source recovery, not another acquisition or production registry promotion.

Hospital production pinning passed focused CMS-only, legacy MN and combined CMS+MN controller cases. The combined case rejects either changed input, either missing dependency or either wrong declaration; its eight synthetic stages succeed only with both correct inputs. Actual thirteen-file retained pin replay passed separately. No native production plan/run was created. Hospital coverage projection and nine-source presentation remain pending; the production pin declares that limitation.

## Parallel work allocation — 2026-09-12

Application and source-service work advance independently in isolated checkouts. Four available agent slots mean one integrator and three active specialists, not ten simultaneous agents. Ten state/industry workstreams are prioritized as a rolling backlog; national sources are acquired once and partitioned locally, never downloaded once per state merely to show concurrency.

- App coding/testing: dedicated existing coding agent, disclosed Astra fallback while Spark is unavailable. Owns combined main verification, retained-only managed adoption acceptance, stop/relaunch and restoration evidence.
- Nursing-home service: independent production-input pinning module/tests/documentation using retained evidence. Shared planner integration waits for a reviewed handoff; no new download.
- Feed prioritization: independent ten-workstream state/industry assessment against retained releases and remaining source-contract gaps. No app dependency for research or connector preparation.
- Root integrator: review, non-overlapping merges, release gate and final Git publication. Routine acquisition belongs to the standalone app, not a waiting agent.

Reviewed app adoption `8894714`, hospital coverage extension `a915f08`, nursing reporting `8cd4a92`, and nursing registry admission/stability fixes `9f07397` are integrated. Main is frozen at `9f07397` for the coding agent's combined acceptance; subsequent source work remains isolated. Focused isolated tests passed, but combined validation and native adoption acceptance are pending. Hospital coverage implementation is now present, superseding the preceding implementation-pending note; no new production coverage release, catalog expansion or production pointer change is claimed.

Root independently read and hashed the nursing admission fixture proof `data/worktrees/maine-provider-preflight/data/tmp/nursing-registry-proof-cc052ce5-3ce8-4317-9593-b947b5e5dc8d.json`: SHA-256 `698eb120474d15ba4e84e26105df9b63849d475edddf8365c2fbae07f101fce7`. It records nursing-only and hospital-combined tiny-SNAP fixture builds with 14,690 nursing rows, unchanged legacy counts and all 100 legacy profile hashes, preserved twelve-file failed-source evidence, and rejected orphan/membership/output-mutation cases. This is actual retained-input fixture evidence, not a production build or current-business count.

The next ten workstreams are reconciled in `docs/NEXT-TEN-SERVICE-WORKSTREAMS-2026-09-12.md`; completed adapters are excluded from new-build work. The independent NES lane confirmed that the retained release publishes grocery/convenience at `4451`, pharmacies at `45611`, and childcare at `62441`; consumer selection must use available source granularity, not assume absent six-digit detail or equivalent application categories. Native selection proof and reviewed implementation remain in progress.

## Native retained-hospital app acceptance

Managed operation `c6a20066-c2b7-4c9c-906e-eb85ab78b22f` succeeded at `2026-09-12T14:43:50.965Z`; its adoption manifest SHA-256 is `ade8191688046665ea795dd22c2679621872848b1e495f6ca6bce02d835c6ddb`. Root independently invoked the native adoption verifier against its persisted descriptor and operation ID: exit 0, 5,419 directory rows, 5,354 states/DC rows, 65 territory rows, original source manifest hash unchanged, zero acquisition requests. This is application-owned retained-source inspection, not national production promotion.

The coding agent's UI acceptance log records one authenticated dispatch, 100/200 percent text checks and restart-visible history. Root inspected the 200 percent screenshot and requested correction of ambiguous wording (`5,354 reporting states/DC` could imply a count of states rather than rows). The acceptance script and label correction remain part of the pending combined release. The focused log records 116 tests, 115 passing, one skipped, zero failures; authoritative process exit and skip explanation are still being confirmed by the owning agent. Full repository validation and final launch restoration remain pending.

The owner subsequently confirmed focused exit 0; the skipped case was the gated native hospital production-pin fixture. Running that gate against retained files also completed exit 0: three tests passed, zero skipped (`data/ui-verification/cms-native-production-pin.log`). Type checking and dependency audit passed, with zero reported vulnerabilities. Full check handle 52479 was confirmed live by its owner; its outcome is not yet known. The UI wording fix will follow this unchanged full check with explicit targeted verification, not a repeated adoption. Main native optional registry/coverage fixtures are also queued after the full check to confirm the isolated proofs after integration.

Next-batch isolated commits, not yet integrated or covered by this full check: `6c6ad33` (explicit NES industry selector; 16 focused tests and five native retained code reads) and `0361908` (nursing production-input helper; three retained tests, fifteen exact input pins). Nursing production planner/CLI integration and nursing coverage projection proceed on separate service lanes. These are not new source acquisitions or production promotions.

## Additive production-promotion audit

Root inspected the actual preceding production plan, `data/reconciliations/production-runs/production-mn-credentials-20260910-01/plan.json`, SHA-256 `fb3906f5f7dba24d0858d6065850cf20d9dc6b6a744bc0878ce3d5a59943b597`. Its registry stage includes the 25 canonical sources **and** optional MA, NJ, recovered TN, OH app receipt, retained-childcare selection and MN credential selection. A new hospital-only CLI example does not inherit these optional inputs: the current planner adds them only when explicitly selected. Therefore the next additive CMS promotion must preserve and revalidate all six optional selections, not silently drop already promoted evidence.

The prior plan selected memory profile `national-12g` (12,288 MiB old-space, minimum 16,384 MiB free / 24,576 MiB total). This describes that historical plan, not a new budget approval or proof of current available memory. Its recorded retained-childcare selection hash is `622a8bdf41b5456e41a33ebf45c4daac936c152b004159ca35271073ac3d82b6`; MN selection hash is `bda3ff50d5ec3ad4d66fa2c3dae0594fe96274f8698c4406d94e7ba002368154`. The next plan must revalidate current intended bytes rather than copy historical fingerprints as new proof.

Before production dispatch: finish combined checks; integrate typed nursing projection and explicit coverage/catalog contract as appropriate; correct stale hospital helper capability metadata; create a fresh reproducible plan with unchanged prior inputs plus chosen CMS inputs; compare all retained declarations and projected scopes; and follow the production approval workflow for that exact plan. No historical approval ID, failed acquisition receipt or existing production plan is rewritten. Root confirmed the current registry pointer still names `national-business-registry-20260911-022652067Z-1ec656c3`; no replacement production run was launched during this audit.

## Combined release gate results

The coding agent confirmed full check handle 52479 exited 0: 2,110 tests, 2,047 passed, 63 skipped, zero failures, with lint, both builds and desktop control-plane smoke passing. This full check covers the executable baseline at `9f07397`; root's subsequent `e75211f` changes documentation only. The sole subsequent app change clarifies row-unit wording. Its three focused tests, type checking, lint, build and native existing-receipt UI verification passed separately; no repeated adoption or new source acquisition occurred.

Main gated retained-input checks also exited 0: hospital/nursing registry admission two tests, hospital coverage parent/subtest two tests, all without skips. These checks close the new CMS gated cases skipped by the ordinary suite; they do not claim all historical optional suite skips were executed. The updated UI proof uses the same `c6a20066-c2b7-4c9c-906e-eb85ab78b22f` receipt and confirms the corrected labels, visible heading at 100 percent text, no horizontal page overflow at 200 percent, and restart-visible history. Root visually inspected the final 100 percent screenshot.

After the coding agent's final stop/relaunch, root independently confirmed Electron PID 26668 running. Its attached launcher shell is not a failed launch; final health evidence is being checked separately without another stop. Prior authenticated native UI checks and final public health/process observations must remain distinguished in release claims.

Final restoration is now confirmed: the owner and root independently read the final public health endpoint successfully (`{"ok":true}`); the app remains open. Authenticated operation-history checks belong to the preceding native acceptance/restart. The final attached launcher invocation is intentionally ongoing, not claimed to have exited. Coding handoff commit `7863143` contains only the reviewed wording, focused assertions, reproducible native acceptance script and exact validation evidence. No source acquisition, production reconciliation or next-batch service code is included in that handoff.

Reviewed next-batch source commits remain isolated and are not covered by this release gate: NES selector `6c6ad33`, nursing production helper `0361908`, nursing planner/CLI `229f1b8`, nursing coverage helper `3bb3eef`, shared nursing coverage `aea259a`, and retained chain assertions `b5954e7`. A ten-source reporting consumer and bounded public-demo Census geocoder prerequisite are still being implemented independently. None is a new production promotion.

## Subsequent integration and independent service lanes

The preceding release was published as `54f97ea9982b895e33f41587e95fbef62a5e9fa8`. The next batch is now integrated, superseding the isolated status above: NES selector `5243fea`, nursing production helper `207c292`, planner `530203c`, nursing coverage helper `e6c3d14`, shared coverage hook `a046b4a`, chain assertions `c42d606`, capability/publication separation `f12b2a1`, ten-source reporting contract `6244cd0`, and Census demo prerequisite `5298d62`. Their combined release gate is pending; the prior release's test result does not cover them.

The fixed ten-source reader currently returns `production-enrollment-absent`, with all-business completeness null. Its pending app consumer preserves the original eight-source default. A ten-source production release and enrollment must be independently verified before counts become available; adding code does not promote retained sources.

The independent additive readiness audit found 25/25 sources ready and no drift across 91 pointer, manifest and configuration comparisons. MA, NJ, recovered TN, OH, retained-childcare and MN inputs must all be explicitly retained alongside both new CMS selections in a fresh plan. Thirty-five small retained/MN evidence files matched; ten larger artifacts were not replayed by that audit. This is readiness evidence, not a completed production build or blanket verification of every source row. No source repull is indicated merely because implementation hashes changed.

The single Census public-demo run `c1d9ca28-069e-4e58-b0a4-2aeb957aee3b` was copied byte-identically into main (11 files, 9,300 bytes). Root replayed its native verifier on main with fetch forbidden: exit 0, manifest SHA-256 `6763d425b5126c6e09cf800ddf0dc6aa20d024c9b570bc0d7cce774b59157827`. An initial forward-slash path argument was rejected by the strict native-path contract; the normalized absolute Windows path passed. No repeat POST occurred. The observation proves one public-example Match/Exact response, not facility-location accuracy or broad geocoding readiness.

Work is separated into non-overlapping lanes: the existing app agent owns the ten-source consumer, full checks and stop/relaunch acceptance; one service agent owns the retained hospital-address geocoding policy and bounded lifecycle; the other owns Utah future-edition discovery without redownloading the retained roster. Root owns review and integration. Source implementation continues in isolated checkouts while main is tested. Four actual concurrency slots remain four; the ten-feed rolling backlog is not presented as ten live agents. Routine validated acquisitions belong to application workers rather than agent polling loops.
