# Utah app-owned retained adoption

The `state-ut-childcare-centers-retained` industry source is an offline adoption workflow, not a fresh acquisition connector. It selects Utah childcare only and passes through Co*Tive's existing industry worker and managed-operation machinery. It requires no running AI session.

The input is the already verified September 2026 normalized release `6aaca68b-f0cc-4ada-a301-0ada860574b8`, manifest SHA-256 `1ff46a94e79a75629707438492f0caa6e9cbc8961fa282748406d38f0c2aec43`. Adoption verifies that exact release, including full local source-PDF and record replay with the app-owned Python runtime. It makes no network requests and does not rebuild unchanged records. The original source and historical manifests remain unchanged, including their historical `app_enrolled: false` statements.

## Operation and evidence boundaries

The new app receipt records a new execution, its industry run identity and its linkage to retained evidence. Neither successful adoption nor membership in the industry catalog proves current business operations, unique entities, physical premises or national completeness. The cohort contains 422 center candidates across 113 reported ZIP5 values. ZIP4 and coordinates are not supplied; they are not fabricated. Address role remains unknown and license dates remain distinct from operating dates.

Managed execution uses the dedicated `scripts/build-ut-childcare-app.mjs` entry point. Select industry `childcare`, state `UT`, and source `state-ut-childcare-centers-retained`. The plan displays explicit no-download and coverage warnings. The existing executor supplies `--output` and `INDUSTRY_SEGMENT_RUN_ID`; a successful child exit must follow source-specific receipt verification, not just file existence.

The existing managed service persists operation and industry receipts, reserves source/executable ownership and supports cooperative IPC cancellation. It does not silently resume an interrupted run: missing processes are reported as failed, while unresolved ownership is unknown and blocks duplicate dispatch. Inspect receipts and retained outputs before retrying. Enrollment activates no recurring schedule or automatic retries, and does not change national production pointers.

Adding this source changes applicable industry-plan fingerprints. Existing schedules whose selected plan changes require review; the scheduler must not silently expand an already approved plan. No schedule is created or enabled by this increment.

The state-access ledger explicitly maps this source to no national reporting profile. It remains `unsupported-evidence-not-measured`, even when local prerequisites exist. The ledger's configuration view does not claim a submitted operation or validate prerequisite contents; actual execution is proved by the separate managed/app receipts. This prevents source enrollment from manufacturing national counts.

## Validation and rollback

Acceptance includes Utah-only plan selection, strict CLI input, actual app-runtime replay, immutable input hashes, linked receipt verification, cancellation, ownership and failure behavior. The real industry-executor integration test verifies the CLI output/run identity, final receipt, retained-file hashes and release of its source reservations. Its temporary output is separate from retained evidence.

Rollback removes the Utah retained source from the industry catalog and its new app wrapper/connector; preserve all source, normalized and operation evidence. Future editions need reviewed acquisition and version pins before app-owned refresh can be enabled. This workflow must not be described as current or ongoing publisher downloading.

Next local reporting should bind the completed app receipt, stream the verified records and preserve original observation time separately from adoption time. Any state/ZIP percentages must use the 422 accepted center candidates as their cohort denominator, not the 1,961 mixed-program report rows. Nationwide completeness and unique active-business totals remain unknown. Existing state-ledger reporting is not itself heatmap UI integration; visible business summaries require their own read-only app integration.

## Verified implementation — September 8, 2026 (Central)

Full `npm run check` passed: 1,436 tests, 1,425 passed, 11 skipped and zero failures; lint, web/desktop builds and desktop control-plane smoke passed. The local log is `data/tmp/ut-app-handoff-full-check-final.log`. TypeScript passed and the production Node dependency audit reported zero vulnerabilities. All 82 pending production pins remain unchanged. The first regression run exposed the missing state-ledger mapping; the explicit unmeasured mapping and regression test were added before the successful full rerun.

Co*Tive accepted the Utah-only managed operation `c82c25f1-4dcc-4a68-aebe-fd91d81cefe0` at `2026-09-09T00:55:30.021Z`. Its plan contains exactly `state-ut-childcare-centers-retained:UT`, with explicit no-download warnings. This operation is local retained adoption, not publisher acquisition or national promotion.

The managed operation completed `SUCCEEDED` at `2026-09-09T00:55:46.217Z`. Its industry task exited zero without forced termination. Independent source-specific verification checked the industry's log hash, app/industry identity and the entire retained semantic chain. The app receipt is `data/industry-segments/runs/c82c25f1-4dcc-4a68-aebe-fd91d81cefe0/state-ut-childcare-centers-retained-UT/jobs/e62e5abb-52f9-4cb7-8984-4917de926293/receipt.json`, SHA-256 `79e3303235bbcf04dc366cc4b85c7c22de5b05a896f8d5f910bbd8d17424e190`. It links the unchanged normalized manifest above and records 422 accepted candidates, `refetch_performed: false` and `normalization_rebuilt: false`.

The refreshed local state report is `data/state-access/reports/20260909005721-6c6281fc-ccb1-407c-82b5-bbb6a91acb3b.json`: 51 jurisdictions and 459 industry cells, comprising 202 national-state evidence cells, 8 direct-state reporting cells, 60 unmeasured cells and 189 missing cells. Utah's new configuration remains unmeasured; this report does not silently fold its local cohort into national coverage or assert running agent capacity.
