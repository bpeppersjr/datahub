# D.C. app enrollment: pending repository evidence refresh

Update: the pending gate documented below was resolved by the explicit hash-pinned coverage reassessment, without modifying historical source reviews. The final full check passed 521 tests, lint, builds and desktop smoke; TypeScript and the zero-vulnerability production dependency audit passed. See `docs/STATE-COVERAGE-REASSESSMENT.md` and `docs/DC-BASIC-BUSINESS-LICENSES.md` for the completed integration. The original diagnosis below is retained as history, not the current enrollment state.

The current uncommitted increment adds `state-dc-basic-licenses` under the cross-industry `local-business-licenses` bucket, restricted to D.C. publisher selection. `state_filter_supported` is false: out-of-District premises remain source records, not silently filtered business locations. It maps published state-access evidence to `dc-dlcp-active-basic-business-licenses` and updates connector version 1.0.2's retry/deadline/cancellation contract to match the previously implemented safeguards. Normalization transformation version and existing immutable releases are unchanged.

A read-only peer checked the official layer metadata/count and catalog: 70,276 active Business License activity rows, matching selected schema, 2,000-record page limit, public access and CC BY 4.0. The existing complete Census ZBP prerequisite's 88,395,583-byte ZIP artifact passed its checksum check. The builder accepts the industry's isolated `--output` and IPC cancellation. This is access/prerequisite evidence, not a completed refresh.

Forty-one focused D.C./industry/state-ledger tests passed, along with TypeScript and a production dependency audit reporting zero vulnerabilities. `npm run check` failed before repository tests because the discovery queue requires September 2 coverage to equal the current production pointer. The independent assessment check has the same prior-release freshness requirement. The verified current reporting release is September 7. These are genuine stale-evidence failures, not permission to rewrite historical observation dates or weaken the checks.

A subsequent comparison found substantive state-profile changes, including Alaska reported profiles 101,859 to 101,915. Historical source-policy decisions and acquisition holds must remain dated; current coverage projections and rank selections require explicit recalculation/revalidation rather than a blind release-ID replacement. Finish this evidence refresh before committing the enrollment increment or dispatching its acquisition. No D.C. job or recurring schedule was created. The local app was restored and returned HTTP 200.

The peer's complete comparison counted 371 leaf differences across the 56-row state artifacts: 168 lineage IDs, 112 observation timestamps and 91 substantive count differences affecting 32 jurisdictions. Baseline and ZCTA counts did not change. In-memory projection of current coverage passed the existing queue 6/7/8 ranking validators with unchanged scope/order; their historical coverage values differ for MI/TN/MA/AZ, MD/MO and LA/MN/WI respectively. Source-readiness classifications remain unchanged. This supports an explicit dated coverage/ranking reassessment carrying forward historical source-policy evidence, not claiming all source decisions were freshly reviewed.

State ledger report `20260907174845-64dae418-c409-41e3-9740-07a89c42521c` reflects the proposed eighth bucket: 408 cells, 202 national evidence, four direct-state publisher, 150 missing and 52 unmeasured. The 51 additional cells result from a new bucket, not new downloads or newly missing data. It still truthfully reports that the historical assessment catalog does not match current coverage.
