# New Hampshire retained visible results: ZIP 03755

## Verified outcome

The fixed Licensed Group Child Care Program / ZIP5 `03755` native lookup retained **six selected internal source records**. This is a successful visible-result prerequisite, not statewide collection, a production promotion or proof of six active physical businesses.

- Run ID: `07c41c61-02f7-4bbc-9312-f0540890a82d`.
- Started: `2026-09-10T17:57:53.562Z`.
- Observed: `2026-09-10T17:57:56.067Z`.
- Finished: `2026-09-10T17:57:56.117Z`.
- Manifest: `data/business-sources/nh-childcare/visible-result-probes/07c41c61-02f7-4bbc-9312-f0540890a82d/manifest.json`.
- SHA-256: `370c09ff1082f2961742c249e9327831d051d684a12818d7234c63dc76a6dcae`.
- Status: `visible-results-observed-not-collection-ready`.

The selected names, observed detail links and visible shipping-address lines are retained inside the manifest under the internal source policy. Business records themselves are not copied into this Git report. No contacts, directions content, hidden provider objects, map coordinates, raw HTML, cookies, CSV export or screenshots were retained.

## Independent inspection

After native publication, a separate offline read confirmed the expected manifest SHA-256 and rederived the complete selected-field projection with the versioned parser. All five recorded implementation hashes and the policy hash matched their current files. The run directory contained only `manifest.json`; browser scratch was absent.

The selected-object contract verified six rows. There was one search submission, 72 routed requests and zero downloads. Eight nonessential resource requests were deliberately aborted without fetching. Seven in-flight handlers existed when browser closure began; seven transport failures were classified at that boundary. All handlers settled before final verification, with zero pending handlers, no fatal network-policy failure, verified browser cleanup and no retained browser scratch. This does not establish successful completion of every background request or independent source authenticity.

## Normalization and coverage limits

All six visible addresses use the full state label `New Hampshire`. Version 1.0.0 recognizes the two-letter state grammar, so it truthfully retained the source lines while leaving structured address fields unresolved. Counts at this stage:

- Selected source rows: **6**.
- Parsed structured addresses: **0**.
- Available latitude/longitude pairs: **0**.

The raw evidence includes the address postal text, but the query ZIP must not be substituted for unparsed address ZIPs. Keep ZIP5 and ZIP4 separate in the next normalization version. Preserve original source lines, source identifiers, policy, run linkage and the original observation timestamp. Do not rewrite this successful manifest or repull the source to repair normalization.

The next implementation step is a separately versioned **offline normalization** of this retained evidence, recognizing the observed full state label without guessing missing address components. Geocoding, current operating status, physical-premises verification, identity matching, national integration and complete state/ZIP coverage remain unverified. No app enrollment, recurring schedule or production pointer was changed by this prerequisite.

## Validation boundary

This turn performed native source validation and independent retained-evidence inspection; no application code changed. The implementation's preceding full regression check passed 1,782 tests with 1,771 passed, 11 skipped and zero failures, plus lint/build/smoke checks, as detailed in the [prerequisite report](NH-VISIBLE-RESULT-PREREQUISITE-2026-09-10.md). Those tests do not by themselves prove this native result; the run and inspection evidence above do.

Oklahoma's failed approved batch was not retried. Historical failed NH manifests are preserved separately rather than replaced by this successful run.
