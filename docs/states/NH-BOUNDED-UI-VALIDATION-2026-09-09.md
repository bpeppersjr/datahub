# New Hampshire: bounded public-search validation

Observed September 9, 2026. This extends [static export discovery](NH-SOURCE-FOLLOWUP-2026-09-09.md) with an ordinary browser search. It is not a statewide acquisition, CSV-delivery verification or active-business release.

## Executed scope

Opened the [official public search](https://new-hampshire.my.site.com/nhccis/NH_ChildCareSearch), selected `Licensed Group Child Care Program`, entered ZIP `03755`, and clicked the normal Search button once. The selected program type and ZIP remained visible. The completed UI reported six results and six displayed table entries on one page. No statewide provider-download control, pagination, radius search or route search was used.

The visible results contained program-name links and address strings retaining the leading zero in ZIP5. This verifies one bounded interactive result, not all ZIPs, all center subtypes, complete statewide enumeration, stable source identities or unambiguous premises roles. The six programs were not installed as a dataset and their contact details were not copied into repository artifacts.

The page itself rendered a Google map during the ordinary search. No direction link, map control or geocoding endpoint was separately invoked. Do not claim the browser performed zero map-related background requests or repurpose the site's embedded map credentials. Map imagery, directions, coordinates and API keys are outside this validation artifact.

## Export result and tooling boundary

After search, the UI exposed `Download Your Search Results`, separate from `Download Provider Results`. DOM inspection found an anchor without `href` or `download` attributes; its handler is `downloadSearchResults(); return false;`. Thus the search export is generated by page code, not a verified static CSV URL.

The available browser download interface did not expose a destination-path control. To preserve the user's requirement that local artifacts remain inside `datahub`, the export was **not clicked**. Consequently there is no observed CSV, header, row count, date formatting, quoting, redaction or first-click success/failure evidence from this pass. This is a validation-tool limitation, not proof that the publisher export fails or forbids downloads. The earlier static asynchronous-export concern remains unconfirmed.

Next runtime validation needs a controlled download destination under `datahub` before triggering this same search-only export. Do not invoke hidden remoting methods or substitute an unfiltered provider download. A fixed app-owned browser workflow can supply the destination, source budget and receipt once its contract is implemented and tested.

## One detail-page observation

Followed the visible [Dartmouth College Child Care Center detail link](https://new-hampshire.my.site.com/nhccis/NH_childcaresearchaccountdetail?id=001t000000UiY3NAAV), without entering an inspection detail or opening documents. This is a selected public program sample, not an identifier enumeration.

The detail explicitly displayed the licensed-group care type. Its status label had no populated value in the observed accessible content, while availability described a wait list. Several last-updated labels likewise had no visible date value. Do not transform those blanks into current status or use availability, displayed schedules, licensed capacity or inspection history as `active=true`. The published type demonstrates the UI selection for this sample, not the complete statutory subtype crosswalk or a stable API enum.

No personal-contact fields, complaint narratives, inspection findings, website API keys or exact map coordinates were retained in this document. General public-page notices remain applicable; this observation establishes no new redistribution or automated bulk-use permission.

## Next step

Implement and verify the app-controlled, ZIP-bounded export prerequisite, then compare its selected/redacted fields with this ordinary-UI evidence. Establish current-status meaning and completeness separately. Preserve separate ZIP5/ZIP4 and retain unknown dates/status. Do not count these six observed results as national coverage or an acquired production release.

This increment is documentation only. No job, refresh schedule, production pointer, source configuration, account, payment, terms acceptance, CAPTCHA action or external communication was created. Rollback removes the note without changing retained datasets.
