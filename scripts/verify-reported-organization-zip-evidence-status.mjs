import { verifyReportedOrganizationZipEvidenceStatus } from '../runner/reported-organization-zip-evidence-status.mjs';
try { if (process.argv.length !== 2) throw new Error('No arguments accepted.'); const result = await verifyReportedOrganizationZipEvidenceStatus(); process.stdout.write(`${JSON.stringify({ status: 'verified', release_id: result.release_id, manifest_sha256: result.manifest_sha256, artifact_count: result.artifact_count, totals: result.conservation })}\n`); } catch { process.stderr.write('Reported organization ZIP evidence status verification failed.\n'); process.exitCode = 1; }
